# Repository Guidelines

## 协作与沟通要求
> **语言要求（必须）**：必须使用中文，并在对话中通俗易懂地用大白话讲述；面向新手，避免过于专业的表述。
> **必须使用 context7 MCP**：当不知道如何编写代码，或代码需要修复 bug 时，先调用 context7 MCP 查询权威资料再继续。  
> **特别注意（必须）**：修改或优化内容前必须查看目录树和项目架构，检查所有可能关联的地方，必要时同步更新相关文件或文档，确保整体一致性与可维护性，避免引入新 bug 或出现描述不一致。
> **代码编写规范**：编写代码的时候必须要写通俗易懂的详细注释，以供新手查看了解代码逻辑和实现细节，注释内容要清晰、准确、简洁，避免冗长或模糊的描述。

## 开发与验证原则
- 编写代码时，优先查 `docs/api/` 中的 DLL API/类/方法/字段名，避免靠猜测堆叠降级与兜底逻辑，保持代码干净、简单、可维护。
- 当文档信息不足、需要了解更底层行为时，可通过反编译相关 DLL 理解内部逻辑，再据此编写正确实现。
- 遇到复杂问题要做充分推演与自检：明确假设、验证结论、反思边界，必要时进行 debug，目标是“可上线”的稳定品质。
- 不能直接运行游戏时，尽量用 `tools/EraWheel.SelfTest/` 增加可执行的自检覆盖。

## 文档编写规范

### 具体要求

#### 1. 长度控制
- 每段不超过3-4句话
- 每个句子不超过100字
- 整体文档长度：根据复杂度控制

#### 2. 语言风格
- 使用主动语态（例：推荐"系统会验证输入"而非"输入会被系统验证"）
- 删除无意义的修饰词（如"非常"、"特别"、"显著"等）
- 用精确动词替代模糊表达（例：用"配置"代替"设置相关参数"）
- 避免口语化表达和套话

#### 3. 结构要求
- 使用标题（#）分段，不超过三级，标题不超过10个字
- 优先使用列表展示多个要点，而非长段落
- 每个列表项只说一件事
- 复杂流程用编号列表（1, 2, 3），否则用项目符号（-）

#### 4. 内容标准
- 每句话必须包含可执行的具体信息
- 禁止写"例如这样可以提高效率"等空洞总结
- 如需举例，使用具体内容或操作步骤
- 删除所有"总之"、"综上所述"等总结性段落

#### 5. 内容组织原则
- 同一概念只在一处完整说明，不要分散到多个章节
- 在首次提到概念时，立即给出完整定义和说明
- 禁止"稍后会详细介绍"、"详见XX章节"等跳转语句
- 按操作/理解的自然顺序组织内容
- 遵循：概念定义 → 使用方法 → 注意事项
- 同一主题的所有信息必须在同一章节内完成

- **仅在以下情况使用引用**：
  - 引用代码示例："参考上述代码中的第3行"
  - 引用具体配置项："使用前面配置的数据库连接"
- **禁止引用概念性内容**，例如：
  - ❌ "关于认证机制的原理见第2章"
  - ❌ "这与前面提到的XX特性相关"
  - ✅ 直接重新简述关键信息："认证通过后，系统会..."

### 反面示例（禁止这样写）
❌ "本系统提供了非常强大的配置功能，可以让用户灵活地进行各种参数的设置和调整，从而显著提高工作效率。"
❌ 系统使用JWT进行认证。详细的Token格式见"安全机制"章节。

### 正面示例（应该这样写）
✅ "配置步骤：
1. 打开config.json文件
2. 修改port参数为8080
3. 重启服务生效"

1. 用户登录后获取JWT Token（包含用户ID和过期时间）
2. 调用API时在Header添加：`Authorization: Bearer <token>`
3. Token有效期24小时