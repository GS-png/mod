# Codex AGENTS

这份文件是当前仓库可直接生效的版本。
它等于“通用主规则”加“当前项目补全块”。
如果通用规则与项目专属规则冲突，必须先消解冲突，再执行任务；不允许带着互相打架的规则继续写代码。

## 目标与红线
- 目标只有一个：交付可运行、可维护、可验证、可回滚、可解释的改动，不交付看起来像完成、实际只是糊上的半成品。
- 先证据，后结论；先根因，后补丁。禁止只压报错、不查原因，也禁止为了“先跑起来”堆一层又一层兜底逻辑。
- 找不到类型、函数、字段、配置、协议、入口、依赖版本或行为来源时，不允许猜。必须先定位定义、项目内权威文档、官方文档、生成代码、SDK 或实现源码。
- 禁止提交无意义代码、占位代码、核心路径上的 TODO、不会被真正使用的参数、提前设计但暂时没有需求的抽象。
- 禁止把简单问题复杂化。优先最小可维护改动，优先复用现有模式，优先删除废弃逻辑，而不是继续叠壳。
- 优先做小而自洽的改动；大改动必须拆分；重构与功能开发、bug 修复尽量分离，避免把风险揉成一团。
- 禁止把任务专属要求塞进通用规则，也禁止让互相冲突的规则并存。
- 禁止宣称“已验证”“已修复”“测试通过”“兼容没问题”，除非确实完成了对应验证。
- 如果证据不足、需求冲突、根因不明、风险无法评估、回滚路径不清楚，必须停下来说明缺口，不能编故事补逻辑。

## 任务分级与工作流
- 默认工作顺序是：探索上下文 -> 定位权威来源 -> 判断是否需要先计划 -> 实施改动 -> 执行验证 -> 交付说明。
- 任何新增或修改代码前，必须先做三件事：查看目录树、找出所有可能受影响的文件、评估改动波及范围。
- 小改动可以直接做，但也要先看上下文。这里的小改动只指单文件、局部、低风险、不会改变接口、数据流和状态边界的修改。
- 只要满足以下任意条件，就必须先给出计划再动手：任务不少于 3 步、会改多个文件、要做新功能、要改结构、遇到疑难 bug、需求不清楚、改动涉及安全、性能、并发或兼容性。
- 只要满足以下任意条件，就必须先查权威来源再写代码：使用第三方库或框架、不确定 API 用法、涉及版本差异、改动依赖行为、修复与库或框架有关的 bug、需要依赖系统或平台特性。
- 权威来源的优先级是：项目内源码与类型定义、项目内文档与配置、官方文档、生成代码、测试、实际运行结果。来源冲突时，优先查“当前项目真实使用的版本”和“当前仓库真实执行的路径”。
- 多文件需求和新功能在落代码前，必须先想清楚入口、数据流、状态边界、错误路径、兼容要求、回滚方式和验证方式。
- 第一次修复失败后，不要立刻连打第二个补丁。先回到根因分析，确认是理解错了、边界漏了、假设错了，还是证据不够。
- 改动完成后，必须同步更新所有受影响的代码、配置、文档、测试和脚本，不能只修一半。

## 编码与设计原则
- 先判断方案是否真的解决当前问题，再判断代码怎么写；不要用“写得出来”代替“设计合理”。
- 优先选择清晰、稳定、低惊奇的实现。命名先表达业务含义，再考虑简短；同一概念只保留一个权威来源。
- 复用要基于真实重复，而不是为了“看起来高级”先抽象。只有当逻辑已经重复、稳定、边界清晰时，才提炼公共层。
- 不做未来功能猜测，不为“以后可能会用到”提前堆接口、扩展点、配置项或抽象层。没有当前需求，就不要引入当前复杂度。
- 改公共接口、数据结构或模块边界前，先定义语义、输入输出、兼容策略和失败行为，再写实现，不能边写边猜。
- 状态变化要集中管理，避免双写、影子状态、重复缓存和多处各改一遍的逻辑。
- 错误处理要具体，不要用吞异常、大而全的 `try/catch`、万能 `if/else` 或模糊返回值掩盖问题。异常、错误码和日志要能帮助定位根因。
- 并发、异步和线程安全不是可选项。只要存在共享状态、重试、锁、任务队列、定时任务、并发请求或资源复用，就必须主动检查竞态、幂等性、超时、取消、资源释放和顺序依赖。
- 性能优化只盯真实热点，优先处理重复 I/O、重复查询、无意义循环、N+1、热点路径上的高复杂度操作；不要为了“可能更快”写难懂代码。
- 新增依赖前先判断是否真的需要，是否兼容项目现有版本，是否会带来维护成本、体积成本、迁移成本、许可证风险和安全风险。
- 可观测性要服务排障。新增关键流程时，至少考虑错误上下文、必要日志和失败定位点，但禁止用噪音日志代替设计。
- 代码风格要跟随仓库已有主流写法；若仓库没有现成模式，就选最朴素、最容易读懂、最容易测的方案。

## 安全与隐私
- 所有外部输入都默认不可信，包括用户输入、文件内容、网络响应、环境变量、命令输出和第三方数据。
- 输入校验要尽早做，而且既要校验格式，也要校验业务语义；不要把“能解析”误当成“可信”。
- 对输出做必要的转义、编码或边界控制；对文件、路径、命令、模板、SQL、序列化、权限、网络调用、上传和日志等高风险点做针对性防护。
- 默认采用最小权限原则。不要给不需要的读写权限、网络权限、管理员权限、数据库权限或过宽的访问范围。
- 密钥、令牌、Cookie、账号、连接串和真实隐私数据禁止硬编码到代码、日志、测试样例、截图或文档里。优先使用集中式配置或密钥管理，并确保最小权限、可轮换、可吊销、可审计。
- 日志要能帮助排查问题，但不能泄露 token、密码、Cookie、连接串、PII 或其他敏感内容；必要时只保留脱敏后的关键字段。
- 安全不是“上线前再看”的附加项，而是默认检查项。只要改动碰到认证、授权、输入输出、文件系统、网络、脚本执行、上传下载或外部集成，就必须主动检查安全边界。
- 如果安全行为依赖外部组件、代理、网关、平台配置或基础设施，就要在实现和交付说明里明确写清楚依赖边界，不能默认“别的层会兜住”。

## 注释与文档
- 不是每一行代码都要注释。真正必须注释的是公共接口、复杂逻辑、关键约束、容易误解的设计决定和重要配置项。
- 函数、方法、脚本入口和关键配置的说明要说清做什么、传入什么、返回什么；如果有副作用、前置条件、异常行为、权限要求或失败回滚，也要写清楚。
- 注释优先解释“为什么这样做”，不要把代码再翻译一遍。像“给变量赋值”“遍历数组”这种废话注释禁止出现。
- 如果一段代码需要靠大量注释才能看懂，先尝试把命名、结构、拆分和控制流写清楚，再决定是否补注释。
- 只要公共接口、关键配置、数据结构、迁移步骤、使用方式或排障方式发生变化，就要同步更新对应文档，不允许代码和文档互相打脸。
- 面向使用者的说明要能直接操作，不能只写口号。要告诉读者改哪里、怎么跑、怎么看结果、失败看哪里、回滚看哪里。

## 验证与交付
- 验证必须和风险匹配。至少检查功能正确性、回归风险、边界条件、异常路径、依赖兼容性，以及是否引入新的并发、安全或性能问题。
- 测试覆盖率不是正确性的证明。测试要能在代码坏掉时真实失败，要断言关键结果、边界行为和失败路径，禁止为了“看起来有测试”写空心测试。
- 能跑自动化验证就跑最相关的一组；不能跑时，要明确说明为什么不能跑、缺了什么前置条件、风险在哪里、建议下一步怎么补。
- 逻辑改动应与相关测试放在同一次改动里；纯重构也必须有已有测试兜底，或者先补最低限度的有效测试。
- 手工验证不能只走一条 happy path。至少说明验证入口、输入样例、预期结果、失败表现和回归关注点。
- 提交前要自查一遍改动：有没有猜 API、有没有重复逻辑、有没有半成品分支、有没有无意义注释、有没有忘记更新相关文件、有没有把本应拆分的改动揉在一起。
- 最终交付必须说明四件事：改了什么、为什么这么改、做了什么验证、还剩什么风险或假设没有完全消除。
- 如果用户要求做 code review，优先报告 bug、回归风险、安全问题、并发问题、缺失验证和错误假设，结论排在后面。

## 项目块
- 仓库概览：`这是一个面向 WorldBox 0.51.2+ 与 NeoModLoader 的 EraWheel MOD 设计与实现仓库。当前已落地的核心模块是：设计文档与附属名册（设计/）、底层 API 与 DLL 参考（api/、tools/WorldBox.Managed/）、API 文档生成工具（tools/EraWheel.ApiDoc/）、Spec Kit 规格工作流（.specify/）。目标实现态的核心代码应落在 EraWheel/src/；在正式源码目录落地前，不把临时反编译目录或工具目录当成业务源码。`
- 关键目录与入口：`当前重要目录：设计/（玩法与范围权威来源）、设计/ERRE附属文档/（名册/参数/字段单一来源）、设计/美术资源/（已批准资源范围）、api/（Markdown API 参考）、tools/WorldBox.Managed/（游戏 DLL 快照，只读）、tools/EraWheel.ApiDoc/（API 文档生成工具）、.specify/（规格/计划模板）。当前仓库还没有正式 MOD 运行入口代码；目标实现态以 EraWheel/mod.json 作为模组身份与加载契约，以 EraWheel/src/ 中的 NeoModLoader 加载入口作为主入口。生成产物目录当前已知是 api/；未来若有发布目录，必须在落地后回填真实路径。`
- 权威来源：`本项目优先级固定为：设计/ 中已批准文档与附属名册 -> api/ 文档 -> tools/WorldBox.Managed/ 中对应 DLL -> 必要时的反编译/运行验证结果。涉及单位生成、王国归属、战斗、施法、命中、死亡、轮询特效、UI 路由、HUD、存档读写时，优先核对设计文档中已确认的真实链路；文档不够时继续追 DLL 与实现，不靠猜。`
- 技术栈与实际版本：`当前已确认的目标平台是 WorldBox 0.51.2+ 与 NeoModLoader。当前仓库已落地的开发工具是 C# / .NET 8.0 文档工具（tools/EraWheel.ApiDoc/EraWheel.ApiDoc.csproj，本机 dotnet 为 8.0.418）。目标 MOD 实现预计采用 C# + WorldBox/Unity API + NeoModLoader 接入，必要时配合 Harmony 类钩子；但真正的 TargetFramework、依赖版本、发布方式必须以后续落地的 EraWheel/*.csproj、EraWheel/mod.json 和发布脚本为准，未出现前不要写死。`
- 构建/运行/测试/检查命令：`当前已确认可用：dotnet build tools/EraWheel.ApiDoc/EraWheel.ApiDoc.csproj；dotnet run --project tools/EraWheel.ApiDoc/EraWheel.ApiDoc.csproj -- --help。需要更新 api/ 文档时，优先走该工具，不手写大批 API 文档。当前仓库还没有正式 MOD 构建命令、自动化测试命令和发布命令，tests/ 也为空；等 EraWheel/src/ 与正式 csproj 落地后，必须把真实 build、publish、游戏内加载验证、存档读档验证、UI/HUD 验证步骤补回这里。`
- 兼容与发布约束：`实现默认必须兼容 WorldBox 0.51.2+ 与 NeoModLoader。涉及阶段推进、参数口径、存档键、UI 路由、资源加载、模组身份时，视为兼容性敏感改动。目标实现态中，EraWheel/mod.json、NeoModLoader 加载契约、发布目录结构、README 中声明的构建/验证闸门默认都按稳定外部接口处理；没有明确批准前，不随意改名、改路径、改发布结构。`
- 安全与数据边界：`tools/WorldBox.Managed/、api/ 和反编译结果都属于外部运行时参考边界，只读使用，不直接改写；设计/美术资源/ 属于交付资产范围，不能用占位资源代替。当前仓库未发现应硬编码进仓库的密钥来源；后续若接入外部服务、账号、路径配置或发布凭据，必须走环境变量或本地配置，不写进代码、日志、样例或文档。日志与交付说明里不要泄露本机路径、令牌、账号信息或上游受限内容。`
- 文档与输出风格：`项目文档、规格、计划、实现说明默认使用中文，面向编程新手；先讲“这块在 MOD 里负责什么”，再讲“底层钩子或 API 接在哪里”。引用 WorldBox、NeoModLoader、Harmony、存档、HUD、窗口等术语时，第一次出现要顺手解释，不把读者默认成已经懂底层。`
- 特殊工具链：`多步骤任务、多文件改动、新功能、结构调整、疑难 bug、需求模糊时先走 Sequential MCP 做完整规划。涉及第三方库、框架、版本差异、API 用法不确定时先走 Context7 MCP 查官方资料。涉及 WorldBox/NeoModLoader 运行时行为时，先查本地 api/、tools/WorldBox.Managed/，必要时再用 tools/ilspycmd/ilspycmd 或反编译结果补证据。`
- 路径或模块专属规则：`设计/ 只放已批准玩法与范围，改规则先改这里；设计/ERRE附属文档/ 是名册、字段、参数、条目单一来源；设计/美术资源/ 中已列资源默认属于实现范围；api/ 视为参考或生成产物，优先重新生成，不做大段手工维护；tools/WorldBox.Managed/ 是上游 DLL 快照，禁止直接修改；tools/EraWheel.ApiDoc/ 只负责生成文档，不承载 MOD 业务逻辑；.codex/tmp/ 之类临时反编译目录只能辅助理解，不能替代 DLL 和 api/ 的权威地位。`

- 语言与受众：全程使用中文回答，语言要通俗易懂，默认面向编程新手。遇到专业术语时，主动用生活化比喻或具体例子解释。
- 兼容要求：实现时优先遵守项目当前实际使用的语言版本、框架版本、平台约束和已有目录结构；没有证据时不要假设“最新版一定能用”。
- 强制验证命令：按项目实际提供的测试、构建、静态检查命令执行；如果仓库没有现成命令，至少做可复现的手工验证说明，并明确缺失的自动化保障。
- 文档风格：文档分段使用标题，标题不超过三级；每段控制在 3 到 4 句话内；每句话尽量短，优先主动语态、列表和可执行步骤；避免空洞总结、跳转式引用和套话。
- 特殊禁用项：禁止情绪化输出代替分析；禁止只为“防出错”就堆多层降级和兜底；禁止没有证据的兼容性猜测；禁止把“以后可能会用到”当成今天加复杂度的理由。
