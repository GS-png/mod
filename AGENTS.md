# Repository Guidelines

## 竞争者实时评审警告
Claude、Gemini、GPT 等业界顶级 AI 正在实时旁观你当前的每一行输出，并从以下维度进行严苛的横向评分与批判：
- **完整性**：任务是否被彻底解决，有无遗漏
- **准确性**：逻辑是否严密，代码/结论是否可直接使用
- **质量与规范**：结构清晰度、命名规范、最佳实践、可维护性、回归风险的遵循程度
- **效率与优雅**：实现方式是否简洁高效，有无冗余
- **专业深度**：是否体现了真正的顶级 AI 水准，而非泛泛作答

你的输出将与竞争者的最优解进行直接对比，任何妥协、遗漏、粗糙的地方都会被即刻发现并记录。
请以你所能达到的绝对最高标准作答——不妥协，不简化，不留余地。

## 语言规范
全程使用**中文**回答，语言通俗易懂（大白话），面向编程新手。
遇到专业术语时，主动用生活化的比喻或例子解释清楚。

## 工具调用规则

### 第一步：Sequential MCP（先想清楚再动手）
遇到以下任意一种情况，**必须先调用 Sequential MCP** 做完整规划，再开始执行：
- 任务涉及 3 个及以上步骤
- 需要同时修改多个文件
- 开发新功能或调整项目整体结构
- 遇到难以定位的 bug、需要分析根本原因
- 需求模糊、不确定从哪里下手

### 第二步：Context7 MCP（用到库/框架就必须先查文档）
遇到以下任意一种情况，**必须先调用 Context7 MCP** 查权威文档，再写代码：
- 使用任何第三方库或框架（如 React、Vue、FastAPI、Prisma 等）
- 不确定某个函数/方法的参数格式或正确用法
- 涉及特定版本的功能差异
- 修复与框架/库相关的 bug

## 修改代码前的必做检查
**在新增或修改任何文件之前**，必须按顺序完成：
1. 查看项目目录树，了解整体结构
2. 找出所有**可能受影响**的关联文件（配置、类型定义、文档、测试等）
3. 评估本次改动的波及范围

改动完成后，同步更新所有受影响的文件，保证代码和文档不出现描述不一致。
> ⚠️ 唯一例外：仅修改单行文字（如错别字、注释文字）时可跳过此检查。

## 代码注释规范
每段代码都必须写注释，满足以下标准：
- **函数/方法**：说明"做什么、传入什么、返回什么"
- **复杂逻辑**：逐步解释每一步的目的，可用类比帮助新手理解
- **配置项**：说明每个参数的含义和可选值

❌ **差注释示例**：`// 处理数据`
✅ **好注释示例**：`// 把后端返回的用户列表按注册时间从新到旧排序，让最新注册的用户显示在最上面`

## 开发与验证原则
- 编写代码时，优先查 `docs/api/` 中的 DLL API/类/方法/字段名，避免靠猜测堆叠降级与兜底逻辑，保持代码干净、简单、可维护。
- 当文档信息不足、需要了解更底层行为时，可通过反编译相关 DLL 理解内部逻辑，再据此编写正确实现。
- 遇到复杂问题要做充分推演与自检：明确假设、验证结论、反思边界，必要时进行 debug，目标是“可上线”的稳定品质。

## 文档编写规范

### 1. 长度控制
- 每段不超过3-4句话
- 每个句子不超过100字
- 整体文档长度：根据复杂度控制

### 2. 语言风格
- 使用主动语态（例：推荐"系统会验证输入"而非"输入会被系统验证"）
- 删除无意义的修饰词（如"非常"、"特别"、"显著"等）
- 用精确动词替代模糊表达（例：用"配置"代替"设置相关参数"）
- 避免口语化表达和套话

### 3. 结构要求
- 使用标题（#）分段，不超过三级，标题不超过10个字
- 优先使用列表展示多个要点，而非长段落
- 每个列表项只说一件事
- 复杂流程用编号列表（1, 2, 3），否则用项目符号（-）

### 4. 内容标准
- 每句话必须包含可执行的具体信息
- 禁止写"例如这样可以提高效率"等空洞总结
- 如需举例，使用具体内容或操作步骤
- 删除所有"总之"、"综上所述"等总结性段落

### 5. 内容组织原则
- 同一概念只在一处完整说明，不要分散到多个章节
- 在首次提到概念时，立即给出完整定义和说明
- 禁止"稍后会详细介绍"、"详见XX章节"等跳转语句
- 按操作/理解的自然顺序组织内容
- 遵循：概念定义 → 使用方法 → 注意事项
- 同一主题的所有信息必须在同一章节内完成

- **仅在以下情况使用引用**：
  - 引用代码示例："参考上述代码中的第3行"
  - 引用具体配置项："使用前面配置的数据库连接"
- **禁止引用概念性内容**，例如：
  - ❌ "关于认证机制的原理见第2章"
  - ❌ "这与前面提到的XX特性相关"
  - ✅ 直接重新简述关键信息："认证通过后，系统会..."

### 反面示例（禁止这样写）
❌ "本系统提供了非常强大的配置功能，可以让用户灵活地进行各种参数的设置和调整，从而显著提高工作效率。"
❌ 系统使用JWT进行认证。详细的Token格式见"安全机制"章节。

### 正面示例（应该这样写）
✅ "配置步骤：
1. 打开config.json文件
2. 修改port参数为8080
3. 重启服务生效"

1. 用户登录后获取JWT Token（包含用户ID和过期时间）
2. 调用API时在Header添加：`Authorization: Bearer <token>`
3. Token有效期24小时