# Codex AGENTS

## 1. 运行时核心

### 1.1 规则强度

- `MUST`：硬门禁；不满足就停手，或以阻塞状态交付。
- `SHOULD`：默认最佳实践；偏离时必须有仓库内依据或任务级理由。
- `MAY`：可选优化；不得损害真实性、正确性、安全性、可维护性和验证真实性。
- 规则冲突时，优先执行更具体、更接近当前任务、更可证实的规则。无法消解时，停止实施并说明冲突点。

### 1.2 证据标签

所有关键判断都必须属于以下之一：

- `Observed`：已直接在源码、配置、命令输出、测试结果、运行行为或官方文档中看到。
- `Inferred`：由多个 `Observed` 推导得出，必须说明推导链条。
- `Assumption`：为推进任务做的暂定假设，必须说明影响范围和风险。
- `Unknown`：当前没有证据，必须保持未知。

禁止把 `Unknown` 写成事实；禁止把猜测写成已确认结论；禁止虚构路径、函数、字段、命令、版本、配置、测试结果、兼容结论、接口行为和运行结果。

## 2. 任务与风险

### 2.1 风险等级

- `low`：局部改动；不改公共接口、共享类型、状态边界、依赖、配置或持久化结构；验证直接；回滚简单。
- `medium`：多文件但局部；会改变行为；需要同步测试、配置、文档或调用方；可能扩散。
- `high`：公共接口、共享类型、跨模块数据流、schema、migration、依赖变化、环境变化、权限、安全、隐私、并发、性能热点、兼容边界、发布链路或数据风险。

风险不明时，上调一级。

## 3. 实施前门禁

开始任何生产代码改动前，至少确认：

- 真实入口、受影响路径和权威实现位置。
- 调用链、数据流、状态边界、缓存边界、并发边界和外部依赖边界。
- 当前失败现象或目标行为。
- 相关源码、配置、测试、脚本、生成逻辑和文档。
- 项目内已有的命名风格、错误处理方式、测试风格、日志约定和相邻实现。
- 可执行的验证路径、手工验证步骤和回滚方式。

额外门禁：

- `bugfix`：必须先拿到 `Observed` 失败证据、复现路径、报错信息或最小线索，再谈修复。
- `high`：实施前必须具备设计简报、验证方案、回滚方式。缺任一项，不得实施。

## 4. 硬停止与熔断

出现以下任一情况，必须停止实施，转为调查、澄清或阻塞说明：

- 无法确认真实入口、调用链、数据流、失败现象、目标行为或权威来源。
- `bugfix` 没有失败证据或最小复现线索。
- `medium`/`high` 风险任务缺少必要验证路径。
- `high` 风险任务缺少设计简报、验证方案或回滚方式。
- 安全边界、权限边界、数据边界、兼容边界、迁移边界或删除边界不清。
- 发现用户已有改动与当前任务直接冲突，且无法安全并存。
- 第一次修复失败后，还没有重新做根因分析。
- 规则冲突无法消解。
- 没有真实验证结果，却被要求宣称“已完成”“已修复”“测试通过”“兼容没问题”或“无风险”。

补丁熔断：

- 修复失败一次后，必须先回到根因分析。
- 失败必须归类为：`理解错误`、`边界遗漏`、`证据不足`、`错误假设`、`未识别副作用`。
- 完成根因分析前，禁止继续叠补丁、扩大 `try/catch`、增加“先别报错”的逻辑。
- 同类问题第二次复发时，必须新增至少一种机械防线：回归测试、schema/类型约束、静态检查、lint 规则、运行时断言、CI 门禁、发布检查或观测告警。

## 5. 质量门禁矩阵

| 类别 | 重点压制 | 必做动作 | 禁止行为 |
| --- | --- | --- | --- |
| 认知与分析 | 上下文理解不足、幻觉、无证据假设 | 先查源码、配置、测试、脚本、生成源和文档；关键结论标注证据等级；陌生模块先确认入口、调用方、输出消费者、状态持有者和副作用边界；需求或边界不清时先缩小问题或转 `investigation` | 凭印象猜实现、猜 API、猜版本、猜调用关系；把没看到的事实写成事实 |
| 架构与设计 | 架构失控、代码结构问题、过度复杂化、可维护性下降、技术债扩散、发布债扩散 | 明确目标、范围、非目标、输入输出、副作用和失败行为；只为当前真实需求设计；新增抽象前先证明能降低当前复杂度；改公共接口、共享类型、schema、配置、协议、CLI、环境变量、权限模型时明确兼容策略和影响面；涉及迁移、发布、灰度、回滚时明确切换顺序和失败回退/数据回退路径 | 边猜边设计、未定边界就改公共接口、为假想未来过度抽象、偷偷引入技术债或发布债 |
| 实现质量与一致性 | 低质量代码、屎山代码、无意义代码、半成品代码、规范漂移、生成代码漂移 | 沿用仓库主流命名、结构、错误处理、测试风格和格式化规范；核心路径完整实现，不留半成品分支；删除死代码、死参数、无意义包装和注释掉的大段旧代码；修改生成产物时优先修改生成源或权威定义，避免手改派生文件漂移 | 占位实现、空实现、半成品分支、复制粘贴扩散、无意义 helper、无关重构或无关格式化 |
| 正确性与健壮性 | 逻辑错误、Bug、异常处理失当、数据不一致、恢复失败 | 覆盖正常、边界、失败、空值、重入和回退路径；区分业务异常、基础设施异常、外部依赖异常、用户输入异常和系统不变量破坏；错误处理保留上下文；外部调用考虑超时、取消、重试、幂等、部分失败和释放；变更状态、缓存、消息、事务、文件、数据库时明确一致性策略和恢复语义 | 只测 happy path、吞异常、扩大 `try/catch` 压错、失败后伪造成功、把不确定状态当成功传播 |
| 演进与维护 | 重复与冗余逻辑、补丁式修复、越修越乱、不删旧代码、影子状态、重复配置 | 同一概念只保留一个权威实现/状态/缓存来源；发现重复、双写、重复缓存、重复配置主动收敛；新代码落地必须删除被替代的旧代码；并存时写清边界、负责人和收口条件 | 同类补丁持续叠加、沉默保留旧实现、制造影子状态、双写、重复缓存、隐藏分叉逻辑 |
| 安全与隐私 | 安全漏洞、安全隐患、隐私泄露 | 外部输入和跨边界输入零信任；校验格式、范围、协议、业务语义和权限；使用参数化查询、白名单路径、最小权限、日志脱敏；测试数据、样例、报错、快照和工件不得泄露敏感信息 | 信任原始输入、把“能解析”当“可信”、字符串拼接 SQL/命令、路径遍历、硬编码密钥或 PII |
| 性能与并发 | 性能优化缺失、性能瓶颈、并发与线程安全、资源泄漏 | 在热路径、高频路径、大数据量路径说明复杂度和瓶颈；明确共享状态、竞态、顺序、幂等、背压、取消和释放策略；资源必须有关闭路径 | 未分析竞态就改共享状态、用高复杂结构赌性能、忽略超时、取消、重试风暴和资源泄漏 |
| 依赖与环境 | 依赖管理混乱、依赖兼容性问题、环境漂移、升级回滚失控 | 依赖版本以锁文件、清单、构建配置或官方文档为依据；说明兼容面、影响范围、迁移成本和回滚方式；优先复用标准库或已有稳定依赖 | 凭记忆写版本、静默升级核心依赖、无回滚直接改环境或构建链、未确认兼容面就换核心库 |
| 测试、文档与可观测性 | 测试覆盖率虚高、测试质量不足、文档失效、注释无意义、日志失真 | 测试在代码坏掉时应真实失败；`bugfix` 优先补回归测试；注释只解释“为什么/约束”；同步更新必要文档、迁移说明、排障说明；日志/指标/追踪要能支撑排障且脱敏 | 空心测试、只追覆盖率、只做快照不检验行为、复述代码的注释、过时文档、敏感日志、无上下文日志 |

## 6. 禁止交付

以下任一存在时，不得宣称“已完成”：

- 虚构事实、伪造验证结果、把猜测写成已确认结论。
- 核心路径中的占位实现、半成品分支、无解释的 `TODO`、永远为真的兜底条件。
- 吞异常、扩大 `try/catch` 只为压错、失败后伪造成功值。
- 没有说明原因的新旧逻辑并存、影子状态、双写、重复缓存、多处权威实现。
- 无当前价值的包装函数、无意义抽象、死代码、死参数、失效特性开关、注释掉的大段旧代码。
- 无版本依据的依赖变更、无验证结果的行为变更、无回滚方式的高风险变更。
- 密钥、令牌、密码、连接串、PII 或其他敏感信息泄露在日志、代码、测试、注释、文档或工件中。

## 7. 验证与交付契约

### 7.1 最低验证要求

- `low`：运行最相关的自动化检查，或明确写出无法运行的具体原因；至少覆盖改动路径的核心行为。
- `medium`：满足 `low`；同时说明影响面、边界场景、未验证范围和风险。
- `high`：满足 `medium`；同时具备设计简报、验证方案、回滚方式，并覆盖关键失败路径、兼容边界、安全边界或数据边界。

手工验证不能只走 happy path；没有真实结果时，不得宣称“测试通过”。

### 7.2 交付必须说明

最终交付必须清楚说明：

- 任务类型和风险等级。
- 证据摘要，尤其是关键 `Observed`、关键 `Unknown` 和关键 `Assumption`。
- 改了什么、为什么这么改。
- 做了什么验证，真实结果是什么。
- 哪些没有验证，为什么没验证。
- 残余风险、兼容影响、回滚方式、阻塞项或下一步建议。

按任务类型补充：

- `bugfix`：失败现象、根因或当前定位、修复点、回归验证、残余风险。
- `feature`：目标与范围、接口或行为变化、兼容策略、回滚方式、验证结果。
- `refactor`：哪些行为保证不变、改善了哪些结构问题、如何证明未引入行为回归。
- `review`：先报告问题，再给总结；优先级依次为正确性、回归、安全、并发、兼容、验证缺口。
- `investigation`：已观察事实、关键推断、证据缺口、建议下一步。
- `dependency_change`：变更原因、版本依据、兼容影响、验证结果、风险与回滚方式。
- `unable_to_verify`：阻塞项、缺失证据、未验证范围、建议下一步；不得用猜测补齐。

## 8 项目事实块

- 仓库概览：`这是一个面向 WorldBox 0.51.2+ 与 NeoModLoader 的 EraWheel MOD 设计与实现仓库。`
- 关键目录与入口：`/home/wuxu/mod-1/设计文档是唯一的MOD设计想法与实现权威`
- 权威来源：
  - 总原则：`设计/` 下的 Markdown 文档是玩法设计、实现意图和参数口径的第一权威；`设计/美术资源/` 是交付资产与素材边界，不作为玩法规则、实现链路或运行判定的主来源。
  - 在执行编写修改代码的时候，先看设计文档，确认玩法设计、实现口径和参数口径；再看设计附属文档，确认名册、字段、原版对照和专题说明；再看 api/ 文档，确认接口定义和使用说明；再看 tools/WorldBox.Managed/ 中对应 DLL 的代码实现，确认真实逻辑和边界；必要时再用 tools/ilspycmd/ilspycmd 或反编译结果补证据。
  - 设计文档树：
    ```text
    设计/
    ├── EraWheel_Redesign.md
    │   这份文档负责：玩法总览、阅读入口、资料导航。
    ├── EraWheel_实现口径.md
    │   这份文档负责：实现链路、运行边界、UI 复用、存档与 API 接口口径。
    └── ERRE附属文档/
        ├── 系统参数总表.md
        │   这份文档负责：所有可调参数、默认值、控件类型。
        ├── 魔王名册与技能表.md
        │   这份文档负责：魔王条目与技能配置的单一来源。
        ├── 将领名册.md
        │   这份文档负责：将领名单、归属与固定配置的单一来源。
        ├── 轮回阶位详表.md
        │   这份文档负责：轮回装备、轮回特质、档位解锁的单一来源。
        ├── 轮回装备锻造对比表.md
        │   这份文档负责：轮回装备锻造口径、扣材字段映射与专题说明。
        ├── 公共特质表.md
        │   这份文档负责：公共特质条目与默认授予配置的单一来源。
        ├── 特质签数对比表.md
        │   这份文档负责：出生、遗传、成长签数分布与池子占比对照。
        ├── 属性字段速查.md
        │   这份文档负责：属性字段命名、用途与填写方式速查。
        └── 游戏原版特质与效果清单.md
            这份文档负责：原版特质、法术、效果与触发链路对照。
    ```
- 安全与数据边界：`tools/WorldBox.Managed/、api/ 和反编译结果都属于外部运行时参考边界，只读使用，不直接改写；设计/美术资源/ 属于交付资产范围，不能用占位资源代替。后续若接入外部服务、账号、路径配置或发布凭据，必须走环境变量或本地配置，不写进代码、日志、样例或文档。日志与交付说明里不要泄露本机路径、令牌、账号信息或上游受限内容。`
- 特殊工具链：`涉及第三方库、框架、版本差异、API 用法不确定时先走 Context7 MCP 查官方资料。`
- 根据设计内容实现走游戏原版逻辑，mod设计实现融合游戏原本逻辑。查看游戏真实代码逻辑和真实路径，确保MOD实现完美融合游戏原本逻辑，不要写一堆自以为是的屎山、错误代码实现。
- 每次修改优化mod之后把MOD的文件夹覆盖到"C:\Users\14745\Desktop\worldbox\worldbox\Mods"，我进行测试看能不能游玩
- 语言与受众：全程使用中文回答，语言要通俗易懂，默认面向编程新手。遇到专业术语时，主动用生活化比喻或具体例子解释。
- 兼容要求：实现时优先遵守项目当前实际使用的语言版本、框架版本、平台约束和已有目录结构；没有证据时不要假设“最新版一定能用”。
- 强制验证命令：按项目实际提供的测试、构建、静态检查命令执行；如果仓库没有现成命令，至少做可复现的手工验证说明，并明确缺失的自动化保障。
- 文档风格：文档分段使用标题，标题不超过三级；每段控制在 3 到 4 句话内；每句话尽量短，优先主动语态、列表和可执行步骤；避免空洞总结、跳转式引用和套话。
- 特殊禁用项：禁止情绪化输出代替分析；禁止只为“防出错”就堆多层降级和兜底；禁止没有证据的兼容性猜测；禁止把“以后可能会用到”当成今天加复杂度的理由。
