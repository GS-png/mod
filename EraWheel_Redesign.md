# 纪元之轮：魔王轮回 MOD - 全新设计文档
<a id="top"></a>

| 字段 | 值 |
|---|---|
| 项目编号 | ERA-WHEEL-DEMON-MOD |
| 版本 | 2.0（完全重新设计） |
| 最后更新 | 2026-01-28 |
| 开发状态 | 重新架构阶段 |
| 目标平台 | WorldBox 0.51.2+ (NeoModLoader) |

---

## 阅读指引

- **快速理解玩法闭环**：先看「[1.2 游戏循环流程](#s12)」→「[2.1 轮回系统](#s21)」→「[2.4 军团波次系统](#s24)」→「[2.3 纪元遗产系统](#s23)」
- **只想了解“是什么”**：优先看每个系统小节的「核心概念 / 阶段流程 / 可配置参数 / 保护机制」
- **只想落地实现“怎么做”**：直接理解「[第五部分：技术可行性与实现方案](#p5)」，再回头按需查系统细节
- **只查默认值/关键参数**：跳到「[7.3 关键参数默认值速查](#s73)」
- **长表格已折叠**：魔王/将领/军团的技能大表默认折叠，可按需展开（便于阅读与检索）

## 术语与缩略语（速查）

| 术语 | 含义 |
|---|---|
| 轮回（Cycle） | 从“魔王被再封印”到“下一次魔王被再封印”的完整周期 |
| 阶段（Stage） | 轮回内的进程阶段（预发展/预兆/苏醒/降临/衰弱/封印战/重建） |
| 文明（Kingdom） | 游戏中的“王国/国家”实体（`Kingdom`）：拥有城市、单位、国王与外交。本文提到的“文明数量/文明城市/文明人口”，默认都按 `Kingdom` 口径统计。 |
| 封印强度 | 封印剩余强度（影响阶段推进与魔王出现节奏） |
| 魔王/将领/军团 | 三层敌对单位结构：魔王（核心）→ 将领（精英）→ 军团（波次单位） |
| 纪元遗产（Legacy） | 轮回结算后给予文明的永久增益/诅咒（跨轮回继承） |
| P0 / S1-S6 | 技能标记口径：P0=被动；S1-S6=主动技能（按序号区分） |
| CSI | Civilization Strength Index（文明强度指数，用于自适应难度） |
| LLM | 大语言模型；用于叙事增强（可选，不是运行前置条件） |
| NeoModLoader | WorldBox 的 MOD 加载框架（本文目标平台） |

<a id="conventions"></a>
## 文档约定（必读）

- **时间单位（系统节奏）**：轮回/阶段/事件池仍用游戏内“年份”描述（对齐 `Date.getCurrentYear()`）。  
  **技能部分不做“每年结算/定时器轮询”**：系统层面（轮回推进/波次调度）可用低频定时，但技能效果只允许走下面这三种 action 钩子触发（命中/受击/死亡点）。
- **事件触发（统一口径，技能强制）**：所有“命中/受击/死亡点”都统一走 **trait/status 的 action 钩子**，不写“全局监听死亡/靠距离猜命中/定时器轮询”。
  - **命中触发**：攻击者挂自定义 `ActorTrait`，用 `ActorTrait.action_attack_target`（`AttackAction`）直接拿到 `pSelf/pTarget/pTile`。
  - **受击触发**：受击者挂自定义 trait/status，用 `action_get_hit`（`GetHitAction`）直接拿到 `pSelf/pAttackedBy/pTile`。
  - **死亡点触发**：需要“死亡位置”的效果，一律用“命中时给目标挂短时标记 trait → 标记 trait 的 `action_on_object_remove`（`WorldActionTrait`）在目标死亡/移除时生成效果”，避免依赖“谁击杀”的不稳定信息。
  - **层数/持续/限频存储**：优先写在对象的 `custom_data_int/custom_data_float`（例如 `era_contract_stacks` / `era_contract_expire_year` / `era_proc_count`）。到期清理与限频检查也只在这三种钩子里做（不额外开“每年扫描名单/定时器表”）。
  - **范围效果（允许但要写清楚）**：只能在钩子触发时，以 `pTile` 为中心**枚举有限半径**内对象（例如半径 8/12）；禁止写“全图扫描/维护感染名单/每年遍历全体单位”。
- **数值写法**：百分比按 `0%-100%` 或 `+15%` 口径表述；伤害若写 `X% 当前HP/最大HP`，默认都带“最低扣到 1HP”的安全兜底（除非另有说明）。
- **可配置口径**：标记为“默认值/可配置”的参数，默认进入配置文件与控制面板；非法值（空/NaN/≤0 间隔/超大值）需做安全修正并提示（不崩溃）。
- **技能槽位口径**：魔王为 `P0 + S1-S6`；将领/军团为 `P0 + S1`。除特别声明外，控制类效果应短时且有上限（见各节“平衡约束/保护机制”）。
- **生成口径**：魔王/将领/军团均使用独立 `ActorAsset` 模板自定义生成（可从原生模板 **clone** 后修改 `id/sprite/数值` 形成新模板）；不直接改写原生资产，也不通过“现场改造现存单位”的方式生成；模板需选用“原生可见法力条”的基底（避免 `force_hide_mana`），以便技能直接使用游戏内置 `Actor.mana`。
- **技能挂载口径（防误扩散）**：技能 trait **不加入随机特质池/出生池**，仅在生成魔王/将领/军团或晋升命定英雄时由代码手动 `addTrait(...)` 挂载；普通自然诞生单位不会获得这些技能。
- **文档排版**：超长技能表使用 `<details>` 折叠；导航建议从“目录”进入，配合搜索（Ctrl/⌘+F）定位单位/ID/技能名。

<a id="toc"></a>
## 目录（可点击）

1. [第一部分：项目概述](#p1)
2. [第二部分：核心系统设计](#p2)
   1. [2.1 轮回系统](#s21)
   2. [2.2 魔王状态机系统](#s22)
   3. [2.3 纪元遗产系统](#s23)
   4. [2.4 军团波次系统](#s24)
   5. [2.5 文明与英雄系统](#s25)
   6. [2.6 将领系统](#s26)
   7. [2.7 军团系统（单位库）](#s27)
3. [第三部分：游戏数值与平衡](#p3)
4. [第四部分：用户交互设计](#p4)
5. [第五部分：技术可行性与实现方案](#p5)
6. [第六部分：版本规划与交付](#p6)
7. [第七部分：附录与快速参考](#p7)

---

<a id="p1"></a>
## 第一部分：项目概述
[↩ 目录](#toc) · [↑ 顶部](#top)

### 1.1 核心理念

**一句话概括**：将 WorldBox 变成一个"自我讲述的史诗叙事引擎"，通过 AI 驱动的动态轮回系统，让每场游戏都是独一无二的文明兴衰史。

### 1.2 游戏循环流程
<a id="s12"></a>

```
玩家启动游戏
   ↓
[预发展阶段] 观看文明自然演化
   ↓ (达到繁荣度阈值)
[预兆阶段] 诡异事件、英雄觉醒
   ↓ (10-50年)
[苏醒阶段] 魔王现身、据点形成
   ↓ (30-100年)
[入侵阶段] 大规模战争、军团波次
   ↓ (100-200年)
[衰弱阶段] 魔王逐渐虚弱、英雄突起
   ↓ (50-100年)
[封印战阶段] 关键决战窗口
   ↓ (胜利 / 失败/灭绝)
[战后重建] 轮回结算 → 获得遗产 → 回到预发展
   ↓ (轮回计数 +1)
重复...（可无限循环或至终焉）
```

### 1.3 核心特色

| 特色 | 说明 |
|------|------|
| **轮回系统** | 每次"被再封印"为一个完整轮回；双方都变强；故事有记忆 |
| **十大魔王** | 虚无/瘟疫/机械/时空/炎魔/深渊/死亡/灵魂/自然/终焉，机制完全不同 |
| **自适应难度** | 根据文明强度自动调整魔王强度；难度平滑，无忽强忽弱 |
| **纪元遗产** | 轮回结算后文明获得永久增强（军事/经济/知识/传奇/诅咒） |
| **英雄系统** | 命定英雄由系统“晋升产生”（每文明/每轮回有上限）；以属性/特性/事件塑造故事主角，不依赖接管底层 AI 行为树 |
| **后备事件池** | 200+事件，无LLM也能运行；有LLM则故事更丰富 |
| **完整控制面板** | 玩家对 MOD 关键参数与运行状态有控制权；参数可改、可导出、可调试（对游戏底层状态不做“全量可控”承诺） |

### 1.4 成功指标

| 指标 | 目标 |
|------|------|
| **完整轮回** | 同一存档完成至少 5 次完整轮回，计数正确，无崩溃 |
| **故事感** | 即使不配 LLM，也有 200+ 事件产生丰富叙事 |
| **可玩性** | 高轮回（5+）仍有解法，不会无解死档 |
| **易用性** | 玩家可通过 UI 调整关键参数；配置可导入/导出，遇到错误自动回退默认并提示（不崩溃） |
| **鲁棒性** | 配置错误自动回退默认，游戏不崩溃，有错误提示 |

---

<a id="p2"></a>
## 第二部分：核心系统设计
[↩ 目录](#toc) · [↑ 顶部](#top)

本部分按系统优先级讲解。每个系统包含：**关键概念** → **游戏流程** → **参数配置** → **保护机制**

### 2.1 轮回系统（核心驱动）
<a id="s21"></a>

#### 2.1.1 核心概念

**轮回** = 从"魔王被再封印"到"下一次魔王被再封印"的完整周期。

- **轮回计数** 从 0 开始，每次被再封印时 +1
- **轮回数据** 写入游戏存档（建议存到 `World.world.map_stats.custom_data`），所以玩家只需用游戏自带的存档/读档，不需要额外按钮
- **轮回历史** 可在 UI 查看时间线和详细对比

#### 2.1.2 轮回阶段详解

##### [1] 预发展阶段

**触发条件**：
- 游戏开始时自动进入
- 第 1 轮回时，需要文明达到"繁荣度"阈值：
  - 任一文明人口 ≥ 3000，**或**
  - 任一文明城市 ≥ 15，**或**
  - 世界出现传奇英雄，**或**
  - 任一文明发展等级 ≥ 5（MOD 内部计算：人口/城市/英雄/文化繁荣等综合得到的 0-10 等级）
- 第 2+ 轮回时，自动进入（无额外触发条件）

**发展等级（0-10）计算口径（可实现）**：
- 这是 MOD 自己维护的“发展指数”，不依赖游戏内置科技树
- 示例算法（可配置权重与满级阈值）：
  - `人口比 = clamp01(当前人口 / 10000)`
  - `城市比 = clamp01(当前城市数 / 50)`
  - `英雄比 = clamp01(传奇英雄数 / 10)`
  - `发展指数 = (人口比 + 城市比 + 英雄比) / 3`
  - `发展等级 = round(发展指数 × 10)`

**持续规则**：
- MOD 监控文明发展，每 10 年检查繁荣度阈值一次
- 玩家也可通过控制面板"强制进入预兆阶段"

**状态表现**：
- 魔王处于封印状态，封印强度 100%
- 封印强度每 10 年自动衰减 5%
- 周围逐渐生成魔气节点（可视化）

**结束条件**：
- 繁荣度条件满足 → 进入预兆阶段

##### [2] 预兆阶段

**触发条件**：
- 繁荣度达成 → 自动进入

**持续规则**：
- 系统随机从事件池选择"预兆事件"触发（每 3-5 年一次）
- 预兆事件示例：先知梦境、邪异天象、英雄觉醒、野兽袭击等

**状态表现**：
- 封印强度继续衰减（每 10 年 -5%）
- 出现封印衰减视觉效果（魔气节点增多、地形扭曲）

**结束条件**：
- 封印强度衰至 20% 以下 → 自动进入苏醒阶段
- 或玩家手动"强制苏醒"

##### [3] 苏醒阶段

**触发条件**：
- 封印强度 ≤ 20%

**持续规则**：
- 魔王实体在地图上生成，强度为魔王最大强度的 30%
  - 生成口径：使用自定义单位模板 ID 生成新的魔王单位（`ActorManager.spawnNewUnit(assetId, tile, ...)`），不通过“随机挑选/改造现存单位”的方式产生魔王
- 魔王实体位置优先选择符合特性的地形（若无则在有限半径内进行地形调整/铺设对应地形，避免大面积改造与性能风险）
  - 实现方式：使用 `WorldTile.setTileType(...)` / `setTopTileType(...)` 在半径内少量改地形，并将改造次数/频率做上限保护
- 据点逐渐扩大，污染向外扩散
- 第一批将领开始激活（通常 1-2 个）
- 尚不生成军团（或生成极少）

**状态表现**：
- 魔王头像/标记出现在 UI
- 根据魔王类型改变地形视觉（虚无→灰白、瘟疫→绿毒、炎魔→熔岩等）
- 世界出现"异象"事件提示

**结束条件**：
- 封印强度衰至 10% 以下 → 进入降临阶段
- 持续 30-100 年内自动触发

##### [4] 降临阶段（正式入侵）

**触发条件**：
- 封印强度 ≤ 10%

**持续规则**：
- 魔王实体强度达到 100%（全盛）
- 军团开始按波次生成（默认每 10 年一波，可配置；`<=0` 的间隔会被修正为最小正值）
- 将领逐个激活（按轮回数量）
- 魔王主动发起进攻，攻击附近城市、英雄、文明据点
- 事件频繁触发（战役、城市陷落、英雄阵亡等）

**状态表现**：
- 魔王强度条显示 100%
- 军团波次进度条显示当前波数
- 地图上出现大量敌军
- 事件日志频繁更新

**结束条件**：
- 入侵持续 100-200 年，**或**
- 魔王生命值 < 30% → 进入衰弱阶段

##### [5] 衰弱阶段

**触发条件**：
- 魔王 HP < 30% 或入侵持续超 200 年

**持续规则**：
- 魔王优先逃离战场，修复/恢复
- 军团生成暂停
- 部分将领可能撤退/失控（离场或脱离据点防线）
- 英雄乘机追击
- 系统判断："是否应该进入封印战？"

**状态表现**：
- 魔王标记显示"虚弱"状态
- 进度条提示"离封印战还有多久"

**结束条件**：
- 立即进入封印战窗口（自动触发）

##### [6] 封印战阶段（决战窗口）

**触发条件**：
- 魔王进入衰弱 → 自动触发，持续 50-100 年

**持续规则**：
- 系统自动生成封印遗迹（自定义建筑）与“仪式区域”（范围检测 + 进度条逻辑）
  - 资产口径：封印遗迹是本 MOD **新注册的 `BuildingAsset`**（可以用 `AssetManager.buildings.clone(newId, fromId)` 从“中立世界建筑（例如 monolith）”克隆一份再改 `id/sprite/base_stats`），不是把现有建筑“选中后改成遗迹”
  - 生成口径：进入封印战阶段后由 MOD 自动选点，在随机点位**直接生成遗迹建筑**（通过反射调用 `World.world.buildings` 上的 `internal BuildingManager.addBuilding(string pID, WorldTile pTile, ...)`）；封印后统一移除这些建筑（`World.world.buildings.removeObject(building)`）
- 可通过三种方式之一胜利：
  1. **击杀封印**：直接击杀魔王
  2. **仪式封印**：友方单位在遗迹范围内持续“占据”（人数/战斗力达到阈值且未被敌方打断）→ 仪式进度累积至 100%
  3. **时间窗口**：窗口倒计时结束，自动胜利
- 如果魔王侥幸恢复到 > 70% HP，则回到降临阶段继续战争

**状态表现**：
- 封印遗迹标记在地图上
- 三种胜利条件进度条并行显示
- 倒计时提示

**结束条件**：
- 满足任意胜利条件 → 魔王被再封印
- 或魔王恢复→回到降临阶段
- 玩家也可主动放弃（导致世界灭绝或文明全灭）

##### [7] 战后重建阶段

**触发条件**：
- 魔王被再封印

**持续规则**：
- **轮回结算**：计算本轮数据、统计伤害、评估胜利程度
- **遗产发放**：文明获得纪元遗产（见 2.3 节）
- **魔王处理**：
  - 重置位置到世界某处
  - 强度根据轮回倍率提升
  - 据点清除或净化
- **转移状态**：进入预发展阶段
- **轮回计数** +1

**结果呈现**：
- UI 弹窗显示"轮回总结"
  - 本轮轮回次数、耗时、伤亡数据
  - 获得的遗产列表
  - 下轮对手强度预估

#### 2.1.3 轮回触发参数（可配置）

参数不做“建议范围”限制（兼容其他 MOD 的极端数值）。系统仅做硬限制/安全修正以保证可运行：
- 时间/数量类：小于 0 的输入按 0 处理
- 间隔类：`<= 0` 会被修正为最小正值（避免除零/死循环）
- 概率类：按 `0%-100%` 归一（超过 100% 视为 100%；小于 0 按 0 处理）
- 任意数值：若出现 `NaN/Infinity` 自动回退为安全值

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `繁荣度_人口阈值` | 3000 | 达成则繁荣 |
| `繁荣度_城市阈值` | 15 | 达成则繁荣 |
| `繁荣度_发展阈值` | 5 | 达成则繁荣（发展等级为 MOD 内部指数） |
| `预兆_持续年数` | 30 | 预兆持续时间 |
| `苏醒_持续年数` | 50 | 苏醒持续时间 |
| `降临_持续年数_最短` | 100 | 最短入侵时间 |
| `降临_持续年数_最长` | 200 | 最长入侵时间 |
| `衰弱_持续年数` | 20 | 衰弱持续时间 |
| `封印战_持续年数` | 50 | 决战窗口持续 |
| `封印_初始强度` | 100% | 初始封印强度（可超过 100%） |
| `封印_衰减速率` | 5%/10年 | 衰减速度（可超过 100%） |

#### 2.1.4 保护机制

- **人口灭绝保护**：世界人口 < 500 时，强制降低魔王强度至 50%，给文明喘息机会
- **文明灭绝保护**：存活文明 ≤ 1 时，自动开启"失败不死档"
- **失败不死档**：文明全灭后，可选"重启轮回"，保留轮回数+部分遗产，从预发展阶段重新开始
- **封印胜利保底**：若所有特殊封印条件关闭，自动启用"击杀封印"保底规则

---

### 2.2 魔王状态机系统
<a id="s22"></a>

#### 2.2.1 核心概念

每个魔王有 **7 个状态**，对应轮回的 7 个阶段。状态决定了魔王的行为、属性、技能激活。

#### 2.2.2 状态转换图

```
[封印] 
  ↓ (封印强度衰减)
[预兆信号]
  ↓ (繁荣度达成)
[苏醒准备] → 实体生成 30%强度
  ↓
[正式降临] → 强度达 100%，军团开始
  ↓
[全盛期] → HP > 70%，所有技能激活，军团加速
  ↓
[衰弱期] → HP < 30%，部分将领可能撤退/失控
  ↓ (自动)
[再封印] → 轮回计数 +1，回到预发展

(侧线：逃离战场恢复 → 回到降临)
```

#### 2.2.3 每个状态的详细行为

| 状态 | HP 范围 | 技能激活 | 军团生成 | 将领数量 | 其他特征 |
|------|--------|---------|--------|--------|--------|
| 封印 | 100% | 被动仅 | ❌ | 0 | 据点虚弱，可被侦测 |
| 预兆 | 100% | 被动仅 | ❌ | 0 | 预兆事件触发 |
| 苏醒 | 30% | 被动+1个主动 | ❌ | 1-2 | 建立据点，污染扩散 |
| 降临 | 100% | 所有技能 | ✅ 每10年 | 按轮回 | 发动进攻，大规模战争 |
| 全盛 | >70% | 所有技能 | ✅ 每5年（加速） | 按轮回 | 法力恢复 +50%，军团×2 |
| 衰弱 | <30% | 被动仅 | ❌ | 减半 | 优先逃离，部分将领撤退/失控 |
| 再封印 | 0% | ❌ | ❌ | 0 | 轮回结算，状态重置 |

#### 2.2.4 十大魔王技能表（每魔王：1 被动 + 6 技能）

> 你要的“写代码能直接照着做”的版本建议这样写：把“机制”落成**技能表**。  
> 统一规则：每个魔王 **1 个被动（P0）+ 6 个技能（S1-S6）**；所有数值都可配置。  
> **实现约束（强制）**：技能只允许落在三种钩子里（命中/受击/死亡点），不写“每年结算/定时器轮询/全图扫描”。  
> **实现手段（可直接照做）**：优先用 `ActionLibrary.*`（眩晕/传送/瘟疫/雷击等）；没有现成动作时就用自定义 trait + `custom_data_*` 存状态；需要范围效果时在钩子里以 `pTile` 枚举半径内对象。
>
> **技能资源口径（魔王/将领/军团/英雄一致）**：不使用“冷却计时器”；主动类技能用“法力（Mana）”作为资源（默认消耗 `5% MaxMana`，可配置）。  
> **命中/受击/死亡点触发口径（可直接实现，不靠猜）**：  
> - “命中触发/普攻附带效果”：给魔王/将领挂自定义 `ActorTrait`，用 `ActorTrait.action_attack_target`（`AttackAction`）拿到 `pSelf/pTarget/pTile`，当场对目标加 trait/status、叠层、刷新持续时间。  
> - “受击触发/反击/减伤/保命”：给受击者挂自定义 trait/status，用 `action_get_hit`（`GetHitAction`）拿到 `pSelf/pAttackedBy/pTile`，当场做减伤/传送/反击/上限计数。  
> - “死亡点生成效果/击杀触发（需要死亡位置）”：在命中时给目标挂一个短时标记 trait，在该 trait 的 `action_on_object_remove`（`WorldActionTrait`）里用“目标当前 tile”生成死亡点效果。这样不用全局监听，也不依赖“谁击杀”的不稳定信息。

**魔王索引**： [1 虚无](#demon-1-void) · [2 瘟疫](#demon-2-plague) · [3 机械](#demon-3-mech) · [4 时空](#demon-4-time) · [5 火焰](#demon-5-fire) · [6 深渊](#demon-6-abyss) · [7 亡灵](#demon-7-death) · [8 契约](#demon-8-soul) · [9 自然](#demon-9-nature) · [10 终焉](#demon-10-judge)

<a id="demon-1-void"></a>
##### 魔王1：虚无之主（虚空）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 虚空领域 | 战斗光环 | 常驻；命中/受击触发 | 当虚无之主**命中或受击**时：以当前 `pTile` 为中心半径 `R=12`，对敌对单位结算一次“虚空侵蚀”：损失 `5% 当前HP`（最低扣到 `1HP`），并附加 2 年“虚空迟滞”（移速 -20%）。 | 给虚无之主挂 `trait_era_void_aura`：在 `action_attack_target` 与 `action_get_hit` 中调用同一逻辑；用 `pTile` 半径枚举单位并 `changeHealth(-dmg)`；迟滞=给目标加 `trait_era_void_slow` 并刷新 `custom_data_int["era_void_slow_expire_year"]`。 |
| S1 | 存在抹除 | 死亡点触发（标记） | 无消耗；目标死亡触发 | 虚无之主命中目标时：给目标附加 5 年“残响标记”。带标记的目标死亡时：在死亡点爆发一次“残响冲击”（半径 6）：敌对单位损失 `20% 最大HP`（最低扣到 `1HP`），并附加 2 年“迟滞”（移速 -20%）。 | 命中：在虚无之主 trait 的 `action_attack_target` 给目标 `addTrait("trait_era_void_echo_mark")` 并写入到期年。死亡：在 `trait_era_void_echo_mark.action_on_object_remove` 里用目标 tile 枚举半径 6 单位并处理（扣血+迟滞）。 |
| S2 | 世界收缩 | 累计触发 | 每累计击杀 `K=100` 触发 | 每当虚无之主累计击杀达到 `K=100`：在世界边缘随机选 `N=8` 个地块“虚空化”（危险地形/贫瘠地形），并对附近单位附加 2 年减速。`N` 随阶段提升（苏醒 6 / 降临 8 / 全盛 12）。 | “击杀计数”严格走三钩子：命中时给目标写 `custom_data_int["era_last_hit_attacker"] = self.id` 并挂短时 `trait_era_last_hit_mark`；在该标记的 `action_on_object_remove` 若确认 `last_hit_attacker==虚无之主` 则给虚无之主 `custom_data_int["era_void_kill_count"]++`；达到 `K` 后触发一次地形改造并清零。 |
| S3 | 相位突袭 | 主动（命中施放） | 命中英雄时尝试；法力消耗 `5% MaxMana` | 当虚无之主**命中英雄**且法力足够时：瞬移到该英雄附近 3 格内，追加一次 `200% 普攻伤害` 的打击；并给目标附加 10 年“破碎”（受伤 +25%）。 | 在 `action_attack_target` 中：若 `pTarget` 为英雄则 `spendMana(cost)` → `singularityTeleportation(...)` → 直接扣血；“破碎”用 `trait_era_void_shatter` + `custom_data_int["era_void_shatter_expire_year"]`。 |
| S4 | 虚空裂隙 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana` | 当虚无之主命中目标且法力足够时：以 `pTarget` 的 `pTile` 为中心半径 10 触发一次“裂隙撕扯”：敌对单位损失 `20% 最大HP`，并有 `15%` 概率被随机传送到附近荒地。 | 在 `action_attack_target` 中：`spendMana(cost)` 后以 `pTarget` 的 tile 枚举半径 10 单位；扣血 `changeHealth`；传送用 `ActionLibrary.teleportRandom(self, target, tile)`。 |
| S5 | 虚无洪流 | 主动（直线） | 命中时尝试；法力消耗 `5% MaxMana` | 当虚无之主命中目标且法力足够时：沿“自身→目标”方向取 30 格直线，对路径附近半径 3 的敌对单位造成 `35% 最大HP` 伤害；并附加 3 年“压制标记”：带标记单位**每次受击**都会额外触发一次 0.3 年眩晕（最多触发 6 次）。 | `action_attack_target`：`spendMana` 后取 tile 串并半径枚举；给命中的单位加 `trait_era_void_suppress`，在该 trait 的 `action_get_hit` 里做“剩余触发次数”递减并调用 `addStunnedEffectOnTarget(...)`。 |
| S6 | 反物质爆发 | 终极技 | 仅全盛可用；法力消耗 `5% MaxMana` | 以自身为中心半径 18：单位受到 `50% 当前HP` 伤害（对英雄减半）；建筑受到高额伤害（优先打到“废墟阈值”）；并清空区域内 30% 地块植被。 | 单位用 `changeHealth`；建筑用 `Building.getHit(...)` 口径可替换为 `changeHealth(-dmg)`（Building 继承 BaseSimObject）；植被用 `WorldTile.removeTrees/removeGrass`。 |

</details>

<a id="demon-2-plague"></a>
##### 魔王2：瘟疫母神（瘟疫）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 瘟疫传播 | 被动 | 常驻；命中触发 | 普攻命中时：给目标叠加 1 层“感染”（最多 5 层）；感染目标在**后续每次受击**时会额外损失 `2% 最大HP × 层数`，并在感染存在期间降低攻击/移速。 | 给瘟疫母神挂 `ActorTrait`：`action_attack_target` 命中时给目标加 `trait_era_infection` 并写入 `custom_data_int["era_infection_stacks"]`（上限 5）与 `custom_data_int["era_infection_expire_year"]`。感染的额外伤害在 `trait_era_infection.action_get_hit` 里结算（不需要“每年 DOT”）。 |
| S1 | 感染转化 | 条件触发（感染者） | 感染满层后触发 | 单位感染层数达到 5 后：在其接下来累计受击 `H=20` 次时触发一次“转化判定”：`60%` 概率转化为瘟疫仆从（替换模板）；失败则进入“重病期”（接下来 10 次受击额外掉血）。 | 在 `trait_era_infection.action_get_hit` 中维护 `custom_data_int["era_infection_hits_taken"]` 与 `custom_data_int["era_infection_sick_hits_left"]`；满足阈值时执行 `metamorphInto(...)` 或 spawn+remove（不走定时器）。 |
| S2 | 毒雾弥漫 | 死亡点触发（感染者） | 无消耗；目标死亡触发 | 任意感染单位死亡：在死亡点触发一次“毒雾冲击”（半径 8）：敌对单位损失 `12% 当前HP`（最低扣到 `1HP`），并附加 5 年中毒效果。 | 用 `trait_era_infection.action_on_object_remove`：拿到目标 tile 后枚举半径 8 敌对单位，扣血 + `addPoisonedEffectOnTarget(...)`（或中毒 trait + 到期年）。 |
| S3 | 基因突变 | 随机触发（感染者） | 感染者受击时判定 | 感染者每次受击有 `10%` 概率触发一次突变：获得随机强化（攻击/防御/移速 +20%）或随机弱化（-20%），持续到其后续 `M=20` 次受击结束（最多叠 2 层）。 | 在 `trait_era_infection.action_get_hit` 里做概率判定；把突变层数与剩余受击次数写入 `custom_data_int`，并用 buff/debuff trait 表现（只靠受击事件推进与清理）。 |
| S4 | 疫病领主 | 累计触发 | 每转化 `T=50` 单位触发 | 每累计成功转化 50 个仆从：在母神据点生成 1 个“疫病领主”（精英单位），并立即带队向最近城市进军。 | `ActorManager.spawnNewUnit("plague_lord", tile, ...)`；用 `Actor.goTo(...)` 引导其移动到城市周边。 |
| S5 | 疫潮爆发 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana` | 当母神命中目标且法力足够时：以目标 tile 半径 20 内敌对单位立刻获得 1 层感染，并附加“疫潮标记”（持续 20 年）：带标记单位在其后续 **3 次受击**时，每次额外 +1 层感染；感染满层时每次受击有 `25%` 概率触发转化。 | 在母神 `action_attack_target`：`spendMana` 后以 `pTarget` tile 枚举半径 20 单位并加 `trait_era_infection` + `trait_era_plague_tide_mark`；在 `trait_era_plague_tide_mark.action_get_hit` 里做“剩余触发次数”递减并加层/判定转化。 |
| S6 | 黑疫降临 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana` | 当母神命中目标且法力足够时：以母神当前位置半径 30 触发一次“黑疫浪潮”：敌对单位获得 50 年“虚弱”（攻击/防御 -15%），并立刻在该范围内随机 5 个点各触发一次“毒雾冲击”（同 S2 的爆发效果）。 | 只做“局部范围枚举”，不做全图。逻辑放在母神 `action_attack_target`：`spendMana` 后枚举半径 30 敌对单位加 debuff trait；随机点用 tile 抽样并复用 S2 的爆发扣血/中毒。 |

</details>

<a id="demon-3-mech"></a>
##### 魔王3：机械暴君（机械）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 反魔法立场 | 战斗光环 | 常驻；命中/受击触发 | 当机械暴君**命中或受击**时：以当前 `pTile` 半径 `R=14`，对“有法力/施法倾向”的敌对单位刷新 3 年“干扰”（法力回复 -50%，攻击 -15%，且本 MOD 的“主动技能触发概率”降低）。 | 给机械暴君挂 `trait_era_mech_antimagic`：在 `action_attack_target`/`action_get_hit` 中以 `pTile` 半径枚举单位；判定“有 mana/法师 trait/职业”后给目标加 `trait_era_mech_jam` 并刷新 `custom_data_int["era_mech_jam_expire_year"]`。 |
| S1 | 纳米修复 | 受击触发（自愈） | 常驻；受击触发 | 当机械暴君受击时：立刻恢复 `5% 最大HP`；若站在机械据点范围内则改为 `8%`。 | 在机械暴君的 `action_get_hit` 中：根据位置是否在据点半径内选择比例，然后 `restoreHealthPercent(...)`（不做“按年回血”）。 |
| S2 | 逻辑病毒 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana` | 当机械暴君命中目标且法力足够时：以目标 `pTile` 半径 12 触发一次“系统冲击”：随机打击最多 3 个建筑（若范围内存在），并使范围内敌军有 `20%` 概率眩晕 0.3 年。 | 在 `action_attack_target`：`spendMana(cost)` 后，建筑=在半径内抽样 `Building` 并 `changeHealth(-dmg)`；单位=枚举半径内敌对单位，概率调用 `addStunnedEffectOnTarget(...)`。 |
| S3 | 同化协议 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana` | 当机械暴君命中目标且法力足够时：以目标 `pTile` 半径 10，随机选择最多 5 个敌对单位进行“同化”（替换为机械仆从模板/变形为机械种）；英雄同化概率减半（默认只施加 debuff）。 | 在 `action_attack_target`：`spendMana` 后范围枚举 + 抽样；非英雄 `metamorphInto(...)` 或 spawn+remove；英雄默认给 `trait_era_mech_assimilated`（攻击/移速 -20% 等）而不强制替换。 |
| S4 | 轨道电弧 | 主动（点杀/清场） | 法力消耗 `5% MaxMana` | 对目标 tile 连续释放 3 次闪电：每次对半径 4 的单位造成固定伤害（例如 80/120/160，随轮回成长）。 | `ActionLibrary.castLightning(demonActor, null, tile)` 连发；若要稳定伤害，用 `changeHealth` 作为补偿。 |
| S5 | 生产线 | 主动（召唤） | 法力消耗 `5% MaxMana` | 在据点周围生成 20 个机械兵 + 2 个机械精英；优先补足军团上限。 | `ActorManager.spawnNewUnit(...)` 批量生成；生成数量受“同时存在上限”限制。 |
| S6 | 天网觉醒 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana`；持续 50 年 | 当机械暴君命中目标且法力足够时：所有机械单位获得 50 年“超载”（攻击/防御/移速 +30%）；并获得“战斗自修复”：机械单位**命中或受击**时额外恢复 `3% 最大HP`。同时立即生成 1 波“天网军团”（规模=当前轮回×系数）。 | 给机械单位批量加 `trait_era_mech_overload` 与 `trait_era_mech_repair_on_combat`（修复逻辑放在其 `action_attack_target`/`action_get_hit`）；军团生成复用 2.4 波次系统。 |

</details>

<a id="demon-4-time"></a>
##### 魔王4：时空扭曲者（时空）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 命运窥视 | 被动 | 常驻 | 获得“预知”：闪避率 +30%（用等效属性实现：受到伤害时有概率无效/减半）；并使控制持续时间 -50%（仅对 MOD 施加的控制生效）。 | 受击用 `action_get_hit`（`GetHitAction`）做概率“无效/减半”；控制缩短用 trait 标记，在 MOD 施加控制时按该标记缩短到期年（写入 `custom_data_int`）。 |
| S1 | 时间禁锢 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana`；持续 10 年 | 当时空扭曲者命中英雄/将领且法力足够时：为其附加 10 年“禁锢标记”。带标记单位在其后续每次受击时都会被短暂眩晕 0.5 年（最多触发 8 次），并可被“净化”移除标记。 | 在 `action_attack_target`：判断目标类型后 `spendMana`，给目标加 `trait_era_time_prison` 并写入到期年与 `custom_data_int["era_time_prison_procs_left"]=8`；在 `trait_era_time_prison.action_get_hit` 里调用 `addStunnedEffectOnTarget(...)` 并递减次数，到期或次数归零即移除。 |
| S2 | 因果锚点 | 受击触发（保命） | 遭遇致命伤时触发；法力消耗 `5% MaxMana` | 时空扭曲者会在战斗中不断“刷新锚点”（记录当前位置与当前 HP）。当其遭遇致命伤且法力足够时：触发一次“因果回避”——瞬移回锚点位置并恢复至锚点 HP（至少 30% 最大HP），清除负面控制。 | 锚点只在三钩子里刷新：例如在自身 `action_attack_target`/`action_get_hit` 中把 `custom_data_float["era_time_anchor_hp"]` 与 `custom_data_int["era_time_anchor_x/y"]` 更新为当前值。致死判定在 `action_get_hit`：若 `incomingDamage >= currentHP` 且有锚点且 `spendMana` 成功，则 `singularityTeleportation(anchorTile, self)` + `setHealth(...)`/`restoreHealthPercent(...)` + 移除控制类 trait。 |
| S3 | 时空裂隙 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana` | 当时空扭曲者命中目标且法力足够时：以目标 `pTile` 半径 12，随机选择最多 10 个敌对单位立刻“错位传送”到附近荒野（距离原地 30-80 格）。 | 在 `action_attack_target`：`spendMana` 后范围枚举+抽样；每个被选中的目标调用 `teleportRandom(self, target, tile)`。 |
| S4 | 时间加速 | 主动（增益） | 法力消耗 `5% MaxMana`；持续 20 年 | 自身与 2 名最近将领获得“加速”：移速 +40%，攻击速度/攻击力 +20%，并获得“回蓝加速”（法力恢复 +50%）。 | 增益用 trait（含法力恢复加成）。 |
| S5 | 时间悖论 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana`；持续 15 年 | 当时空扭曲者命中目标且法力足够时：以目标 `pTile` 半径 10 触发一次“悖论爆发”：对范围内敌军随机施加一种效果（眩晕 0.5 年 / 随机传送 / 固定伤害）。并给被命中的敌军附加 15 年“悖论标记”：其后续每次受击都有 `25%` 概率再次触发一次（最多触发 3 次）。 | 在 `action_attack_target`：`spendMana` 后范围枚举并对每个敌军 roll；标记用 `trait_era_time_paradox_mark`，在该 trait 的 `action_get_hit` 里做概率与“剩余次数”递减。 |
| S6 | 永恒回环 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana`；持续 50 年 | 当时空扭曲者命中目标且法力足够时：以自身 `pTile` 半径 25 给敌军附加 50 年“时间风暴标记”（受击时更易触发短眩晕 0.2 年或随机传送，最多各触发 5 次）；同时获得“回环”：在下一次遭遇致命伤时，可额外触发一次“小型因果回避”（复用 S2 的致死回避，但恢复量减半）。 | 标记= `trait_era_time_storm`（逻辑在其 `action_get_hit`，只靠受击推进次数）；“回环”= 在时空扭曲者自身 `custom_data_int` 写一个“可用次数=1”，在其 `action_get_hit` 的致死分支里优先消耗并执行简化 S2。 |

</details>

<a id="demon-5-fire"></a>
##### 魔王5：混沌炎魔（火焰）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 烈焰灼烧 | 战斗光环 | 常驻；命中/受击触发 | 当炎魔**命中或受击**时：以当前 `pTile` 半径 `R=12`，对敌对单位结算一次：损失 `6% 当前HP`（最低扣到 `1HP`），并叠 1 层“灼烧”（最多 5 层；灼烧存在期间该单位在受击时会额外承受 +10% 火焰系追加伤害）。 | 给炎魔挂 `trait_era_fire_aura`：在 `action_attack_target`/`action_get_hit` 中范围枚举并 `changeHealth`；灼烧=给目标加 `trait_era_burn_stack` + 写入 `custom_data_int["era_burn_stacks"]`（上限 5）。追加伤害逻辑放在 `trait_era_burn_stack.action_get_hit`：若攻击者为火焰阵营/带火焰 trait，则额外扣一段 `10%×层数` 的“追加伤害”。 |
| S1 | 地狱化 | 累计触发（地形） | 每累计击杀 `K=30` 触发 | 炎魔累计击杀达到 `K=30` 时：将据点周围半径 8 的随机 20 个地块变为熔岩/焦土，并清除树木与草（每次触发都有改造上限，避免破图）。 | “击杀计数”走“命中标记 → 目标 `action_on_object_remove` 记账”的三钩子口径；触发后做一次性地形改造：`setTileType` + `removeTrees/removeGrass`。 |
| S2 | 爆燃 | 死亡点触发（标记） | 不耗法力 | 炎魔命中目标时：给目标附加 3 年“爆燃标记”。带标记的目标死亡时：在死亡点半径 5 造成爆炸伤害（目标最大HP 的 20%），并对附近地块施加“烧焦”。 | 命中：炎魔 trait 的 `action_attack_target` 给目标 `addTrait("trait_era_fire_burst_mark")`。死亡：`trait_era_fire_burst_mark.action_on_object_remove` 里做 AOE 扫描 + `changeHealth`；地形用 `WorldTile.setTileType(...)` 或 `removeTrees/removeGrass` 表现“焦化”。 |
| S3 | 熔岩喷发 | 主动（区域） | 法力消耗 `5% MaxMana` | 目标区域半径 10：立即喷发，单位受到固定伤害并被击退/眩晕 1 年；同时生成 12 个熔岩地块。 | 伤害 `changeHealth`；眩晕用 `ActionLibrary.addStunnedEffectOnTarget(...)`；地形用 `setTileType`。 |
| S4 | 灼烧烙印 | 主动（命中施放） | 命中英雄时尝试；法力消耗 `5% MaxMana`；持续 20 年 | 当炎魔命中英雄且法力足够时：给目标施加 20 年“烙印”。带烙印目标在其后续每次受击时额外损失 `8% 最大HP`；若其所在 tile 为熔岩/火焰地形则该次额外伤害翻倍。 | 在炎魔 `action_attack_target`：`spendMana` 后给目标加 `trait_era_fire_brand` 并写到期年；在 `trait_era_fire_brand.action_get_hit` 里做额外扣血（含地形判定）。 |
| S5 | 火焰复苏 | 受击触发（地形条件） | 受击时判定 | 当炎魔受击时：若其所在 tile 为熔岩/焦土，则立刻恢复 `10% 最大HP`，并刷新 5 年“火焰护体”（减伤 20%）。 | 在炎魔 `action_get_hit` 里检测 tile type：满足则 `restoreHealthPercent` + 给自身加 `trait_era_fire_guard` 并刷新到期年；减伤逻辑放在 `trait_era_fire_guard.action_get_hit`。 |
| S6 | 末日烈焰 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana`；持续 30 年 | 当炎魔命中目标且法力足够时：以炎魔当前位置半径 30 随机选 8 个点各触发一次“烈焰雨爆发”（对范围内单位/建筑造成一次性伤害）；并给该范围内敌军附加 30 年“热浪”（移速 -10%）。 | 只做“局部范围枚举”，不做全图区域系统：在炎魔 `action_attack_target` 中 `spendMana` 后做 tile 抽样，表现可用 `castFire/throwTorchAtTile`，伤害用 `changeHealth`；热浪用 trait + 到期年。 |

</details>

<a id="demon-6-abyss"></a>
##### 魔王6：深渊邪神（疯狂）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 精神污染 | 战斗光环 | 常驻；命中/受击触发 | 当深渊邪神**命中或受击**时：以当前 `pTile` 半径 `R=16`，对敌对单位结算一次“疯狂侵染”：疯狂值 +10（0-100）。疯狂值越高，单位越容易失控（用后续技能触发表达）。 | 疯狂值写入单位 `custom_data_int["era_madness"]`（0-100）；逻辑放在深渊邪神的 `action_attack_target`/`action_get_hit`：范围枚举后对敌军 `madness = clamp(madness+10)`。 |
| S1 | 疯狂爆发 | 条件触发（标记） | 疯狂 ≥ 100 时触发；持续 10 年 | 单位疯狂值达到 100 时触发一次“失控标记”（持续 10 年）：带标记单位在其后续每次受击时都会短暂 `makeConfused(...)`（最多触发 8 次），并获得“狂乱”（攻击 +30%，防御 -20%）。标记结束时将疯狂值降到 40。 | 触发点放在深渊邪神的 `action_attack_target`：把目标疯狂值加到阈值后，给目标加 `trait_era_madness_burst`（写到期年 + `procs_left`）。在 `trait_era_madness_burst.action_get_hit` 里调用 `makeConfused(...)` 并递减次数；移除时把 `era_madness` 设回 40。 |
| S2 | 邪教渗透 | 主动（命中施放/召唤） | 命中时尝试；法力消耗 `5% MaxMana` | 当深渊邪神命中目标且法力足够时：在目标 `pTile` 周围召唤 8-12 个邪教徒（临时单位），邪教徒会优先传播疯狂值并攻击英雄。 | 在 `action_attack_target`：`spendMana` 后以 `pTile` 为中心找可行走点并 `spawnNewUnit("cultist", tile, ...)`；必要时对邪教徒做一次性 `goTo(...)` 引导靠近最近英雄。 |
| S3 | 深渊凝视 | 主动（单体） | 法力消耗 `5% MaxMana` | 指定英雄：立刻 +50 疯狂值，并眩晕 1 年；若目标疯狂值 ≥ 80，则额外掉血 `20% 当前HP`。 | 疯狂值加成由 MOD；眩晕用 `ActionLibrary.addStunnedEffectOnTarget`；掉血 `changeHealth`。 |
| S4 | 低语诱导 | 主动（命中施放/牵引标记） | 命中英雄时尝试；法力消耗 `5% MaxMana`；持续 15 年 | 当深渊邪神命中英雄且法力足够时：为其附加 15 年“低语标记”。带标记英雄在其后续每次受击时会被强制 `goTo(深渊据点附近 tile)` 并短暂 `makeConfused(...)`（最多触发 10 次），稳定把其拖向战场核心。 | 在 `action_attack_target`：`spendMana` 后给目标加 `trait_era_abyss_lure`（到期年+次数）。在该 trait 的 `action_get_hit`：调用 `goTo(...)` + `makeConfused(...)` 并递减次数。 |
| S5 | 深渊触手 | 主动（命中施放/召唤） | 命中时尝试；法力消耗 `5% MaxMana` | 当深渊邪神命中目标且法力足够时：在目标 `pTile` 周围召唤 4 个“深渊触手”（临时单位），优先攻击建筑与密集人群；触手持续到深渊邪神被封印/死亡或触手被击杀。 | 在 `action_attack_target`：`spendMana` 后 `spawnNewUnit("abyss_tentacle", tile, ...)`。清理由深渊邪神自身的 `action_on_object_remove`（死亡/移除时批量移除仍存活的触手）。 |
| S6 | 旧日降临 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana` | 当深渊邪神命中目标且法力足够时：以自身 `pTile` 半径 35 内所有敌对单位疯狂值 +30；并在附近生成 1 个“旧日化身”（Boss）加入战场，持续到封印战结束或被击杀。 | 只做“局部范围枚举”，不做全图：在 `action_attack_target` 中 `spendMana` 后枚举半径 35 单位并加疯狂；Boss 用 `spawnNewUnit`，并通过 trait 强化其高血/范围伤害。 |

</details>

<a id="demon-7-death"></a>
##### 魔王7：死亡君王（亡灵）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 亡灵领域 | 战斗光环 | 常驻；命中/受击触发 | 当死亡君王**命中或受击**时：以当前 `pTile` 半径 `R=14`，范围内亡灵单位攻击 +30%（刷新 5 年），敌对单位附加 5 年“凋零”（生命回复 -50%）。 | 给死亡君王挂 `trait_era_death_domain`：在 `action_attack_target`/`action_get_hit` 中范围枚举；对亡灵单位加 buff trait（到期年），对敌军加 `trait_era_death_withered`（到期年）。 |
| S1 | 死亡审判 | 死亡点触发（范围标记） | 不耗法力 | 当死亡君王发生命中/受击时：会“宣判”其周围半径 25 的敌对单位（刷新 2 年标记）。带标记单位死亡时：有 `25%` 概率在原地生成 1 个骷髅仆从；若死者是“命定英雄”（`trait_era_destined`）则概率减半但生成精英骷髅。 | 在死亡君王的 `action_attack_target`/`action_get_hit`：以 `pTile` 半径 25 枚举敌军并加 `trait_era_death_sentence_mark`（写到期年）。在该标记的 `action_on_object_remove`：按概率 `spawnNewUnit("skeleton_thrall"/"skeleton_elite", tile, ...)`。 |
| S2 | 灵魂收割 | 累计触发 | 每击杀 10 单位叠 1 层；最多 20 层 | 每层：死亡君王自身最大HP +1%，攻击 +1%（可衰减）；封印后保留 30% 层数作为下一轮起始。 | 计数器 + 给魔王添加 trait（或 MOD 内部倍率）；结算时固化层数。 |
| S3 | 死亡凋零 | 主动（命中施放） | 命中时尝试；法力消耗 `5% MaxMana` | 当死亡君王命中目标且法力足够时：以目标 `pTile` 半径 12，敌对单位立刻损失 `30% 当前HP`（英雄减半），并附加 10 年“凋零标记”：带标记单位在其后续每次受击时额外损失 `5% 最大HP`。 | 在 `action_attack_target`：`spendMana` 后范围枚举并立刻扣血；给目标加 `trait_era_death_decay`（到期年）。额外伤害逻辑放在 `trait_era_death_decay.action_get_hit`。 |
| S4 | 冥界大门 | 主动（命中施放/召唤波次） | 命中时尝试；法力消耗 `5% MaxMana` | 当死亡君王命中目标且法力足够时：立刻额外生成一波亡灵（规模=当前轮回×系数），从据点或死亡君王附近集结出发。 | 在 `action_attack_target`：`spendMana` 后直接调用 2.4 波次生成（单位模板换亡灵），不做“每 N 年自动出兵”。 |
| S5 | 死亡印记 | 主动（单体） | 法力消耗 `5% MaxMana`；持续 20 年 | 给目标英雄施加“死亡印记”：20 年内若目标掉血到 30% 以下，则触发一次“处决”（直接击杀/或强制掉到 1HP 并眩晕 3 年），并消耗该印记。 | 施放时给目标添加 `trait_era_death_mark` 并写入到期年；目标受击用 `trait_era_death_mark.action_get_hit` 检查血量阈值，达成则执行 `ActionLibrary.deathMark(target)`（若可用）或 `setHealth(1)` + 眩晕，并移除标记。 |
| S6 | 终末审判 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana` | 当死亡君王命中目标且法力足够时：以自身 `pTile` 半径 35 触发一次死亡能量爆发：敌对单位获得 50 年“恐惧”（移速 -15%）；并立刻在该范围内 3 个“战场热点”（按密集单位 tile 抽样）生成亡灵大军。 | 只做“局部范围枚举”：在 `action_attack_target` 中 `spendMana` 后枚举半径 35 单位加恐惧 trait；热点=从半径内 tile 抽样；亡灵生成用 `spawnNewUnit`。 |

</details>

<a id="demon-8-soul"></a>
##### 魔王8：灵魂编织者（契约）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 契约烙印 | 被动 | 常驻；命中触发 | 普攻命中英雄时：叠加 1 层“契约”（最多 5 层）。带契约的英雄在其后续每次受击时额外损失 `2% 最大HP × 层数`，并在契约存在期间降低抗性。 | 在灵魂编织者 `action_attack_target`：把层数写入英雄 `custom_data_int["era_contract_stacks"]`（上限 5）并刷新到期年；额外掉血逻辑放在英雄身上的 `trait_era_contract.action_get_hit`（不需要“每年结算”）。 |
| S1 | 侵蚀冲动 | 条件触发（契约者） | 契约≥3 的英雄受击时判定 | 契约层数 ≥3：英雄每次受击有 `5% × 层数` 概率触发“侵蚀冲动”（持续 5 年）：立刻短暂混乱（0.5 年），并获得 5 年易伤（受伤 +15%）。 | 在 `trait_era_contract.action_get_hit`：读取 `era_contract_stacks`，按概率触发 `makeConfused(...)` + 给目标加易伤 trait（写到期年）。 |
| S2 | 傀儡牵引 | 主动（命中施放） | 命中英雄时尝试；法力消耗 `5% MaxMana`；持续 10 年 | 当灵魂编织者命中英雄且法力足够时：为其附加 10 年“牵引标记”。带标记英雄在其后续每次受击时会被强制 `goTo(魔王据点/魔王本体附近 tile)`，并刷新“攻击 +20% / 防御 -20%”（用于表现其被操控冲锋）。 | 在灵魂编织者 `action_attack_target`：`spendMana` 后给目标加 `trait_era_soul_pull`（到期年+次数）。在 `trait_era_soul_pull.action_get_hit`：调用 `goTo(...)` 并刷新 buff/debuff trait（只靠受击推进，不做“按年强制移动”）。 |
| S3 | 灵魂吸取 | 击杀计数触发 | 不耗法力 | 灵魂编织者击杀单位时：恢复 `5% 最大HP`；若击杀英雄则额外恢复 `15%` 并获得 1 层“灵魂护盾”（减伤 10% 持续 30 年）。 | “击杀检测”不做全局监听：在灵魂编织者的 `action_attack_target`（以及必要时 `action_get_hit`）里对比 `Actor.kills` 与 `custom_data_int["era_last_kills"]` 的增量；若有增量则执行 `restoreHealthPercent` 并叠加护盾 trait（写到期年/上限）。英雄击杀用“命中标记 → 目标 `action_on_object_remove` 回写到施法者”口径更稳定。 |
| S4 | 灵魂链接 | 主动（区域） | 法力消耗 `5% MaxMana`；持续 30 年 | 选定区域半径 14：随机 3 名英雄被“链接”。链接期间其中任意一个受到伤害，会有 30% 伤害分摊到其余两人。 | 给被链接者挂链接标记 trait，并用链接标记的 `action_get_hit`（`GetHitAction`）做分摊：对另外两人 `changeHealth(-share)`。 |
| S5 | 意志粉碎 | 主动（命中施放） | 命中英雄时尝试；法力消耗 `5% MaxMana` | 当灵魂编织者命中英雄且法力足够时：目标立刻损失 50% 体力/法力，并短暂硬控（0.3 年）；随后获得“虚弱”（攻击 -15%，移速 -10%）基础 10 年，且契约层数每层额外 +2 年（上限 20 年）。 | 在 `action_attack_target`：`spendMana` 后对目标 `setStamina/setMana` + `addStunnedEffectOnTarget(...)`；虚弱用 `trait_era_weakened` 并写入到期年（不写“计时器移除”，到期清理由三钩子检查）。 |
| S6 | 契约终裁 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana` | 当灵魂编织者命中目标且法力足够时：以自身 `pTile` 半径 35 内所有“已带契约”的英雄：契约层数立即 +2，并触发一次“契约爆发”（大额掉血或失控 10 年，二选一）。 | 只做“局部范围枚举”，不遍历全图：在 `action_attack_target` 中 `spendMana` 后枚举半径 35 单位，筛选 `era_contract_stacks>0` 的英雄并统一处理：层数 +2、扣血 `changeHealth` 或 `makeConfused`，并弹 UI 事件提示。 |

</details>

<a id="demon-9-nature"></a>
##### 魔王9：自然之怒（自然）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 自然亲和 | 被动（地形加成） | 常驻；命中/受击触发刷新 | 当自然之怒**命中或受击**时：检测自身所在 tile。若为森林/草地：自身与附近自然军团攻击/防御 +25%（刷新 5 年）；若为荒地/熔岩：改为 -10%（刷新 5 年）。 | 在自然之怒的 `action_attack_target`/`action_get_hit` 中做 tile type 判定，并给自身/半径内自然军团刷新 buff/debuff trait（写到期年；不做定时器轮询）。 |
| S1 | 植物暴走 | 主动（命中施放/地形） | 命中时尝试；法力消耗 `5% MaxMana` | 当自然之怒命中目标且法力足够时：以目标 `pTile` 半径 18 随机选择 30 个地块转为森林/荆棘顶层，并给半径 18 敌军附加 10 年减速（移速 -15%）。 | 在 `action_attack_target`：`spendMana` 后做 tile 抽样并 `setTileType/setTopTileType`；减速用 trait（写到期年）。 |
| S2 | 巨兽召唤 | 主动（召唤） | 法力消耗 `5% MaxMana` | 召唤 1 只自然巨兽（Boss 单位）+ 6 只护卫兽，目标为最近城市。 | `ActorManager.spawnNewUnit(...)`；用 `goTo` 引导。 |
| S3 | 大地震颤 | 主动（建筑打击） | 法力消耗 `5% MaxMana` | 目标区域半径 16：建筑有 `50%` 概率受到重创（按最大HP比例扣血），并使区域内单位眩晕 1 年。 | 对 `city.buildings` 或区域内 building 列表抽样 `changeHealth(-dmg)`；眩晕用 `ActionLibrary.addStunnedEffectOnTarget`。 |
| S4 | 生命缠绕 | 主动（命中施放/范围控制） | 命中时尝试；法力消耗 `5% MaxMana`；持续 15 年 | 当自然之怒命中目标且法力足够时：以目标 `pTile` 半径 12 的敌军获得 15 年“缠绕”（移速 -40%，攻击 -15%）。 | 在 `action_attack_target`：`spendMana` 后范围枚举并给敌军加 `trait_era_entangle`（写到期年），不维护“区域集合”。 |
| S5 | 风暴召唤 | 主动（命中施放/灾害） | 命中时尝试；法力消耗 `5% MaxMana` | 当自然之怒命中目标且法力足够时：在目标 `pTile` 触发一次风暴表现（龙卷风/落雷/击退任选其一），并对半径 14 敌军施加 5 年减速（移速 -10%）与 0.2 年短眩晕（小概率）。 | 表现优先 `ActionLibrary.castTornado(self, null, tile)`（或雷击/击退等）；核心效果只做一次性范围枚举（减速 trait + 概率短控），不做“风暴区周期伤害”。 |
| S6 | 盖亚之怒 | 终极技（命中施放） | 仅全盛可用；命中时尝试；法力消耗 `5% MaxMana` | 当自然之怒命中目标且法力足够时：以自身 `pTile` 半径 40 触发一次“自然灾变连锁”：执行 S1/S3/S5 的强化版各 1 次（地形改造+震颤+风暴），但每项都有严格“单次影响上限”。 | 只做“局部范围枚举与 tile 抽样”，不选全图热点：在 `action_attack_target` 里 `spendMana` 后复用 S1/S3/S5 的一次性逻辑（范围与数量上调，但有硬上限）。 |

</details>

<a id="demon-10-judge"></a>
##### 魔王10：终焉审判者（终焉）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发/消耗（默认） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 神罚印记 | 被动 | 常驻；命中触发 | 普攻命中英雄：叠 1 层“审判”（最多 3 层）。每层使其受到的伤害 +10%。 | 用终焉审判者的 `ActorTrait.action_attack_target` 命中时：把层数写入英雄 `custom_data_int["era_judgement_stacks"]`（上限 3），并用 trait 表现易伤。 |
| S1 | 天堂审判 | 条件触发 | 审判层数=3 且法力足够时触发；法力消耗 `5% MaxMana` | 审判满 3 层：触发一次“裁决”——目标立刻损失 `60% 当前HP`（若已低于 30% 则处决/击杀），并清空其审判层数。 | 由 MOD 检测层数；伤害 `changeHealth`；处决可用 `ActionLibrary.deathMark(...)`（若适配）或 `setHealth(1)` + 眩晕。 |
| S2 | 末日号角 | 主动（命中施放/范围压制） | 命中时尝试；法力消耗 `5% MaxMana`；持续 30 年 | 当终焉审判者命中目标且法力足够时：以自身 `pTile` 半径 40 的文明单位获得 30 年“末日压迫”（攻击 -10%，防御 -10%，移速 -5%）。 | 在 `action_attack_target`：`spendMana` 后范围枚举并给目标加 `trait_era_doom_pressure`（到期年）。不做“定时全图触发”。 |
| S3 | 圣裁光束 | 主动（点杀） | 法力消耗 `5% MaxMana` | 对目标英雄位置降下“圣裁”：连续 3 次雷击，外加 5 年易伤（受伤 +20%）。 | `ActionLibrary.castLightning(...)` + 易伤 trait。 |
| S4 | 终焉波次 | 主动（命中施放/军团强化） | 命中时尝试；法力消耗 `5% MaxMana`；持续 30 年 | 当终焉审判者命中目标且法力足够时：立刻追加 1 波“终焉军团”（数量=当前波次×系数），并给终焉军团附加 30 年“终焉增幅”：军团单位在其后续每次命中时追加 +15% 伤害。 | 在 `action_attack_target`：`spendMana` 后调用 2.4 生成；增幅用 `trait_era_judge_legion_amp`（逻辑放在军团单位的 `action_attack_target`：追加扣血），并用到期年控制持续。 |
| S5 | 诸神黄昏 | 条件技能（战斗燃命） | HP < 50% 时生效 | HP < 50%：自身进入“黄昏形态”——攻击/防御 +50%，法力恢复 +30%；代价是每次**命中或受击**都会损失 `2% 最大HP`（燃命）。 | 条件检查写在终焉审判者的 `action_attack_target`/`action_get_hit`：满足 HP 条件则刷新 `trait_era_judge_twilight`；燃命=在这两个钩子里 `changeHealth(-maxHP*0.02)`。 |
| S6 | 世界终结（末日计量） | 终极技（累计触发） | 仅全盛可用；累计“末日值”触发 | 终焉审判者在全盛期会累积“末日值”：每次命中/受击/处决都会增加。末日值达到阈值（例如 300）时：触发一次“终焉事件”——在自身附近连续执行 3 个灾害爆发（雷暴/瘟疫/地形崩坏各 1 次）并追加 1 次终极波次。 | 末日值写入 `custom_data_int["era_doom_meter"]` 并只在三钩子里递增；达到阈值时清零并触发：灾害爆发全部限定在半径内（范围枚举 + tile 抽样），终极波次复用 2.4。 |

</details>

---

### 2.3 纪元遗产系统（轮回的"记忆"）
<a id="s23"></a>

#### 2.3.1 核心概念

每次轮回结算后，**文明和魔王都会获得永久性增强**，通过"纪元遗产"实现。这让每一轮回都有意义，下一轮回更有挑战。

#### 2.3.2 遗产分类与机制

| 遗产类型 | 效果示例 | 如何获得 | 叠加规则 |
|---------|---------|--------|--------|
| **军事遗产** | +10% 攻击力、+15% 防御力 | 击杀魔物 100+ | 最多 5 层，递减：1 层 100% → 2 层 80% → ... → 5 层 20% |
| **经济遗产** | +20% 资源产出、+10% 建造速度 | 城市存活完整轮回 | 同上 |
| **知识遗产** | +15% 经验获取、+10% 建造/征兵效率、解锁“封印研究/破魔锻造”等遗产能力（以特性/状态/建筑效果实现） | 发展等级达阈值 | 同上 |
| **传奇遗产** | +5% 英雄诞生率、全属性 +5% | 英雄击杀将领 3+ | 同上 |
| **诅咒遗产**（负面） | -10% 生命、-20% 疾病抗性 | 城市被摧毁 > 50%、英雄阵亡 > 3 | 同上，可通过特殊事件移除 |

#### 2.3.3 发放规则（结算时）

轮回结算时，系统自动发放：

```
基础遗产：
  - 1个军事遗产
  - 1个经济遗产
  - 1个知识遗产

额外遗产（根据胜利程度）：
  - 伤亡低 → +1个传奇遗产
  - 伤亡高 → +1个诅咒遗产（可选择接受/拒绝）
  - 特殊胜利（仪式/击杀等级高） → +1个随机遗产

魔王成长：
  - 基础属性 ×(1 + 轮回倍率 × 轮回数)
  - 默认倍率 25%/轮回，可配置（不限制；内部仅做安全修正）
  - 硬上限：最高 +200%（约 8 轮回达到）
```

#### 2.3.4 技术实现方式（WorldBox traits 系统）

```csharp
// 添加遗产 = 注册 trait（示意）
AssetManager.traits.add(new ActorTrait
{
    id = "legacy_warrior_1",
    name = "战士遗产 Lv1",
    description = "+10% 攻击力",
    // 具体字段以 WorldBox/NeoModLoader 实际 trait 数据结构为准
});

// 应用遗产 = 在单位出生/生成时添加 trait（示意）
// newActor.addTrait("legacy_warrior_1");
```

---

### 2.4 军团波次系统
<a id="s24"></a>

#### 2.4.1 核心概念

魔王降临后，每隔固定时间（通常 10 年）自动生成一波敌军。随着波次增加，单位数量和强度逐步提升，给文明压力梯度。

**生成口径（自定义生成）**：
- 军团单位通过自定义单位模板 ID 直接生成新单位（`ActorManager.spawnNewUnit(assetId, tile, ...)`）
- 不通过“随机挑选现存生物并改造”为军团的方式生成

#### 2.4.2 波次生成规则

| 波次 | 间隔 | 数量 | 单位等级 | 单位类型 | 备注 |
|------|------|------|--------|---------|------|
| 1-3 波 | 10 年/波 | 30 个 | 基础等级 | 先锋型（移速快、防低） | 试探性进攻 |
| 4-6 波 | 10 年/波 | 35 个 | 等级+1 | 混合型（均衡） | 主要压力 |
| 7-9 波 | 10 年/波 | 40 个 | 等级+2 | 精锐型（高伤害） | 有特殊技能 |
| 10+波 | 10 年/波 | 50 个 | 等级+3 | Boss 级（范围伤害） | 终极威胁 |

**强度递增公式**：
```
单波强度 = 基础强度 × (1 + 15% × 波次)
同时存在上限：默认 200（可配置；超过则停止生成，性能保护）
```

#### 2.4.3 军团单位分类

每个魔王 4 种军团单位：

- **先锋** - 虚无/瘟疫等
- **主力** - 存在蚀刻者/瘟疫兽等
- **精锐** - 虚空裂解者/变异领主等
- **Boss** - 反物质泰坦/瘟疫泰坦等（终极）

#### 2.4.4 可配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `军团_生成间隔` | 10 年 | 每波间隔（`<= 0` 自动修正为最小正值） |
| `军团_初始数量` | 30 | 第 1 波数量 |
| `军团_强度递增` | 15%/波 | 每波增长（允许负值；最终强度会做安全修正） |
| `军团_同时上限` | 200 | 同时存活上限（不限制输入，但极端值可能导致严重卡顿） |
| `军团_全盛加速` | ×2 | 全盛期生成加速 |

---

### 2.5 文明与英雄系统
<a id="s25"></a>

#### 2.5.1 抗魔等级（反馈机制）

文明通过与魔王战斗，逐步提升抗魔等级（0-10），形成“越战越强”的正反馈。  
本设计采用**生产级口径**：不修改底层伤害公式，而是通过 **KingdomTrait/ActorTrait 的基础属性**与**魔王阵营生成倍率**共同实现“越打越能打”。

**提升规则（默认）**：
```
每击杀 1000 个“魔王阵营单位”（本 MOD 生成） → 抗魔等级 +1
每成功完成 1 次封印战结算（胜利）        → 抗魔等级 +1.5
```

**效果（默认，示例可配置）**：
- Lv0：无加成
- Lv1：文明全体单位基础属性 +3%（攻击/防御/最大HP）
- Lv3：基础属性 +10%，并降低“魔王阵营单位生成倍率” 5%（仅影响本 MOD 生成的魔王/将领/军团）
- Lv5：解锁“破魔锻造”：对文明新生/新生成的战士与命定英雄额外施加 `trait_era_sealforged`（攻击/防御/最大HP +10%）
- Lv7：基础属性 +25%，并降低“魔王阵营单位生成倍率” 12%
- Lv10：基础属性 +40%，并降低“魔王阵营单位生成倍率” 20%

**实现要点（可直接实现）**：
- 抗魔等级是“文明维度”数据：用 `KingdomTrait` 表现（显示/持久化），并在单位生成/出生时继承其文明 trait 的基础倍率。
- “降低魔王阵营生成倍率”不改底层战斗：只作用于本 MOD 的生成与成长公式（例如军团 HP/攻击的 `spawnMultiplier` / `cycleMultiplier`），保证稳定可控。

#### 2.5.2 命定英雄

**产生规则（晋升制，默认）**：
- 每个文明在每个轮回内最多拥有 `1` 名“命定英雄”（可配置）。
- 当文明满足繁荣/危机阈值（人口、城市、战损等）时，系统从该文明现存单位中挑选 1 名候选并“晋升”为命定英雄：
  - 优先：高等级/高击杀/传奇特性多/存活时间长的单位
  - 若没有合格候选：允许直接生成 1 名“命定英雄模板单位”（可选）

**评分规则（更清楚的版本，可配置）**：
- **候选池先过滤**：同文明、已成年、存活、非魔王/将领/军团、未被标记为英雄。
- **打分思路**：把候选的关键指标都“归一化为 0-1”，再按权重相加，分数最高者晋升（或在前 N 名里随机 1 个，避免总是同一个人）。
- **默认权重（可改）**：
  - 等级 `w_level = 0.20`
  - 击杀 `w_kills = 0.20`
  - 传奇特质数量 `w_legend = 0.20`
  - 寿命 `w_life = 0.10`
  - 最大生命值 `w_hp = 0.10`
  - 攻击能力 `w_atk = 0.10`
  - 指挥/组织/声望（合并项）`w_lead = 0.10`
- **归一化口径（举例）**：
  - `level_score = clamp01(level / 10)`
  - `kills_score = clamp01(kills / 50)`
  - `legend_score = clamp01(legend_traits / 3)`
  - `life_score = clamp01(lifespan / 200)`（只取“寿命/最大寿命”类 stats；**不使用年龄兜底**。若取不到寿命，则忽略该项并重分配权重）
  - `hp_score = clamp01(max_hp / 1000)`（可用 `Actor.getMaxHealth()`）
  - `atk_score = clamp01(attack_power / 100)`（建议从 `BaseStats` 的攻击相关项读取；若缺失则跳过）
  - `lead_score = avg(renown, warfare, stewardship)`  
    - `renown` = 声望（ActorData.renown）  
    - `warfare` = 指挥（BaseStats id: `warfare`）  
    - `stewardship` = 组织（BaseStats id: `stewardship`）  
    - 任一缺失就忽略该项，只对已有项做平均
- **总分**：
  - `score = Σ(w_i * score_i)`（若角色是国王/城主/军团队长，可额外 +0.05）
- **兼容性规则**：
  - 若某项数据取不到（例如声望/指挥），就**忽略该项并把权重平均分给其他项**，避免“没数据就被扣分”。
  - 若出现并列：优先“传奇特质多 / 击杀高”，仍相同则在前 3 名里随机。

**命定英雄自带（默认）**：
- 3 个传奇特性（随机选择）
- `trait_era_destined`：基础属性 +50%（攻击/防御/最大HP/移速）
- “史诗表现”：独立头像/名字前后缀、UI 高亮与日志记录（不依赖行为树接管）
- “战场爆发”：当命定英雄在“魔王影响区”（距离魔王或其据点一定半径）内发生**命中/受击**时，获得 10 年 `trait_era_heroic_surge`（小幅攻防/移速提升，可刷新）

**实现要点（可直接实现）**：
- 晋升 = 直接对目标 `Actor.addTrait(...)`（必要时清理互斥 trait），并写入 MOD 内部“英雄档案”（用于 UI/叙事/继承）。
- “魔王影响区”不做“每年扫描”：在命定英雄自己的 `action_attack_target` / `action_get_hit` 里，用 `pTile` 与“魔王/据点中心 tile”做距离判定；命中/受击且在范围内就添加/刷新 `trait_era_heroic_surge`。

**家族继承**：
- 英雄死亡后，后代有 30% 概率继承部分能力
- 可形成"英雄家族"，延续故事线

#### 2.5.3 世界停战与共同讨伐（生产级核心）

> 目标：魔王存在期间，世界内部战争暂停，所有王国把“魔王阵营”视为共同敌人；封印后恢复各自外交与冲突，保证后续仍有剧情发展。

**触发条件（任一满足）**：
- 魔王进入「降临」阶段；或
- 魔王摧毁世界城市 > 20%；或
- 世界人口跌破阈值且魔王仍存活（避免内耗导致灭绝）

**停战规则（直接可实现）**：
- 触发时立刻执行一次“王国间停战”：遍历 `World.world.wars.getActiveWars()`，对**不包含魔王阵营**的战争调用 `World.world.wars.endWar(war)`。
- 停战持续到本轮回结束（魔王被再封印/世界灭绝/终焉结算）。
- 停战期间，每 `T=1-5` 年重复执行一次“王国间停战”（同上），用于抑制新战争自然生成（这是机制定义的一部分，不是“兜底”）。

**共同讨伐规则（不建立世界联盟）**：
- 所有王国获得 `kingdom_trait_era_truce`（示例：士气/基础攻防小幅提升），但不会合并为同一联盟。
- “魔王阵营”统一使用本 MOD 生成的独立阵营（独立 `Kingdom`），并与所有王国保持战争关系（共同敌人）。
- 讨伐关系的落地方式：
  1) 仅首次触发时创建魔王阵营：用 `World.world.kingdoms.makeNewCivKingdom(founderActor, "era_demon", pLog:false)` 生成 `demonKingdom`（founderActor 可用 MOD 生成的“魔王本体”，或一个隐藏的占位单位）。  
  2) 在进入停战时，遍历当前所有王国：对每个王国调用 `World.world.wars.newWar(kingdom, demonKingdom, WarTypeLibrary.normal)` 建立“对魔王阵营的战争”。  
  3) 为避免重复建战：建战前用 `DiplomacyHelpers.isThereFightBetween(kingdom, demonKingdom)` 判定是否已在交战。
- UI 共享情报：由 MOD UI 直接显示“魔王/将领/据点/波次热点”标记（不依赖原生联盟/情报系统）。

**封印后恢复**：
- 魔王被再封印后，移除 `kingdom_trait_era_truce`；停止周期性停战调用；世界恢复正常外交与战争生成。

---

#### 2.5.4 英雄技能系统（可直接实现）

> 目标：英雄“像英雄”，但不依赖接管行为树。技能由 MOD 在**命中/受击钩子**里判定触发（仅对少量英雄单位），通过现成 API/ActionLibrary 直接施放。  
> 本设计不使用技能冷却，直接使用游戏内置的“法力（Mana）”作为技能资源：施放消耗法力，法力按游戏规则自动恢复。

**技能槽位口径（默认）**：
- 命定英雄：`P0 + S1`（1 被动 + 1 主动）
- 普通英雄（可选）：仅 `P0`（用于轻量差异化）

**触发逻辑（生产级）**：
- 命定英雄在**命中/受击**时做判定：若满足触发条件且魔力足够，则施放技能并扣除魔力。
- 触发条件采用“位置/周围敌军数量/自身 HP 阈值/是否处于魔王影响区”等可观测条件，不使用任务 ID。

**法力规则（默认，可配置）**：
- 读取：`Actor.getMana()` / `Actor.getMaxMana()`。
- 消耗：优先 `Actor.spendMana(cost)`；或 `Actor.setMana(actor.getMana() - cost)`。
- 命定英雄晋升时可直接补满：`Actor.setMaxMana()`。
- 为了让“命定英雄”能稳定施放：通过 `trait_era_destined` 或专用 trait 提升其最大法力与法力恢复（走 `base_stats_meta` 口径）。

**技能库（示例，默认可配置；仅使用已确认存在的 API）**：
- P0「英勇」：常驻基础属性 +10%，并免疫本 MOD 的“牵引/侵蚀”类效果（通过 trait 标记实现）。
- S1「圣愈」：消耗 `5% MaxMana`（默认；可配置）；当 HP < 40% 时触发，恢复 25% 最大HP。实现：`spendMana(cost)` + `Actor.restoreHealthPercent(0.25f)`。
- S1「天罚」：消耗 `5% MaxMana`（默认；可配置）；对附近 1 个魔王阵营目标降下雷击（固定伤害 + 短暂硬控 0.2 年）。实现：`spendMana` + `ActionLibrary.castLightning(self, target, tile)` + `ActionLibrary.addStunnedEffectOnTarget(self, target)`。
- S1「护盾」：消耗 `5% MaxMana`（默认；可配置）；为自身施放护盾。实现：`spendMana` + `ActionLibrary.castShieldOnHimself(self, null, tile)`（或等价现成护盾动作）。
- S1「战吼」：消耗 `5% MaxMana`（默认；可配置）；周围半径 10 友军获得 10 年攻击 +10%。实现：`spendMana` + 范围添加 buff trait。

**技能与遗产联动（可选）**：
- 抗魔等级/知识遗产可解锁新的英雄技能条目（仍遵循 `P0+S1`，避免复杂度爆炸）。

### 2.6 将领系统（详细）
<a id="s26"></a>

> 将领是“魔王侧的英雄单位”，用于把战局从“数量压制”变成“关键目标对抗”。  
> 口径统一：每个将领 **1 个被动（P0）+ 1 个主动（S1）**，并且技能实现**只允许**落在三种钩子里：命中（`action_attack_target`）/受击（`action_get_hit`）/死亡点（`action_on_object_remove`）。

#### 2.6.1 通用规则（必须一致）

- **数量**：每个魔王固定 5 名将领（先锋/坦克/输出/辅助/精英）。
- **激活**：降临阶段开始激活；激活数量与轮回成长参考 `3.2 将领成长`（上限 6 个同时激活）。
- **生成口径**：将领使用独立 `ActorAsset` 模板生成（不改造现存单位）；模板需选用“原生可见法力条”的基底，便于主动技能走法力制。
- **技能口径**：将领技能只做“强干预”，不承诺完全接管底层 AI；需要范围效果时只能在钩子触发时做**半径枚举**，不做“周期刷新/定时器轮询/全图扫描”。
- **持续/限频**：统一写入 `custom_data_*`，并且只在三钩子里检查与更新（例如“每 X 次触发最多 1 次”“最多触发 N 次后失效”）。
- **安全上限**：任何“范围枚举/召唤/地形改造”都必须有**单次触发上限**与**同时存在上限**（避免卡顿）。
- **平衡约束**（将领 < 魔王）：将领不做“全图效果/世界级事件/直接处决/真正免疫/永久控制”；控制类效果默认不超过 1 年（可被净化/驱散）；召唤默认不超过 3 个单位且必须受军团上限约束。

#### 2.6.2 将领定位模板（倍率口径）

| 定位 | HP倍率 | 攻击倍率 | 防御倍率 | 移速倍率 | 主要职责 |
|------|--------|---------|---------|---------|---------|
| 先锋型 | 0.8× | 1.0× | 0.6× | 1.5× | 突进/骚扰/抓单 |
| 坦克型 | 1.5× | 0.7× | 1.5× | 0.7× | 护卫/抗线/区域控制 |
| 输出型 | 0.7× | 1.5× | 0.5× | 1.0× | 点杀/范围伤害 |
| 辅助型 | 1.0× | 0.8× | 1.0× | 1.0× | Buff/召唤/净化 |
| 精英型 | 1.2× | 1.2× | 1.2× | 1.0× | 多面手/关键技 |

#### 2.6.3 将领名册与技能表（P0+S1）

> 表中“实现抓手”是建议路线：优先 `ActionLibrary.*`（眩晕/传送/瘟疫/雷击等），没有现成动作时就用自定义 trait + `custom_data_*` 存状态（层数/持续/限频），并且只在三钩子里推进与清理。

**将领索引**： [虚无](#generals-void) · [瘟疫](#generals-plague) · [机械](#generals-mech) · [时空](#generals-time) · [火焰](#generals-fire) · [深渊](#generals-abyss) · [亡灵](#generals-death) · [契约](#generals-soul) · [自然](#generals-nature) · [终焉](#generals-judge)

<a id="generals-void"></a>
##### 2.6.3.1 虚无之主将领（领域：虚空）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_void_dps` | 虚空剑圣·塞拉斯 | 输出 | **虚空剑意**：普攻命中时附加一次“裂伤”（目标 10 年内受到伤害 +15%）；对英雄额外 +10%。实现：给塞拉斯挂 `ActorTrait`，用 `action_attack_target` 命中时给目标 `addTrait("trait_era_void_bleed")` 并刷新剩余年限。 | **相位穿行**：法力消耗 `5% MaxMana` / 持续 5 年；自身移速 +100%，并在施放时瞬移到最近英雄附近（3 格内）。实现：`ActionLibrary.singularityTeleportation(...)` + 速度 trait。 |
| `general_void_vanguard` | 虚空行者·米拉 | 先锋 | **虚空潜行（相位）**：自身累计命中 `N=5` 次后获得 10 年“相位潜行”（移速 +20%）；相位潜行期间受击时有 10% 概率随机传送脱离（每累计受击 `M=20` 次最多触发 1 次）。实现：命中计数写入 `custom_data_int`；受击用 `action_get_hit` 判定传送，并用 `custom_data_int` 做“受击次数限频”。 | **相位突袭**：法力消耗 `5% MaxMana`；瞬移到最近英雄附近（3 格内），首次命中伤害 ×3，并施加短暂硬控（0.3 年）。实现：`singularityTeleportation(...)` + 命中用 `action_attack_target` + `addStunnedEffectOnTarget(...)`。 |
| `general_void_tank` | 虚空守卫·奥古斯 | 坦克 | **存在锚定**：当奥古斯命中/受击时：以当前 `pTile` 半径 10 刷新友军“锚定”（控制时间 -30%，不受“抹除类效果”影响，持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 buff trait（到期年）。 | **虚空护盾**：法力消耗 `5% MaxMana` / 持续 10 年；获得护盾值 `500 + 50×轮回数`，护盾存在时减伤 30%。实现：自定义护盾数值（MOD 内部）+ 护盾 trait 的 `action_get_hit`（`GetHitAction`）减伤。 |
| `general_void_support` | 虚空祭司·艾莉娅 | 辅助 | **虚空祝福**：当艾莉娅命中/受击时：以当前 `pTile` 半径 12 刷新友军攻击 +15%（持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 buff trait（到期年）。 | **虚空召唤**：法力消耗 `5% MaxMana`；在自身周围召唤 3 个“虚空行者”（军团先锋单位），并命令其向最近城市移动。实现：`spawnNewUnit(...)` + `goTo(...)` 引导（受军团上限约束）。 |
| `general_void_elite` | 虚空领主·塞拉菲姆 | 精英 | **虚空领域（弱化版）**：当塞拉菲姆命中/受击时：以当前 `pTile` 半径 10 对敌军结算一次 `3% 当前HP` 伤害（最低扣到 1HP）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `changeHealth(-dmg)`。 | **相位打击**：法力消耗 `5% MaxMana`；瞬移到目标英雄身边并造成一次“无视护甲”的高伤害（按自身攻击 ×2.0）。实现：传送 + 直接扣血 `changeHealth(-dmg)`。 |

</details>

<a id="generals-plague"></a>
##### 2.6.3.2 瘟疫母神将领（领域：瘟疫）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_plague_vanguard` | 疫病使者·莫格拉 | 先锋 | **疫源携带**：普攻命中给目标叠 1 层感染（最多 5 层）；感染额外伤害统一走“感染者受击触发”（与瘟疫母神同口径）。实现：`action_attack_target` 命中时写 `custom_data_int["era_infection_stacks"]` 并刷新到期年。 | **感染冲锋**：法力消耗 `5% MaxMana`；直线冲锋 20 格，路径上每隔若干格触发一次“毒雾冲击”（半径 4：敌军损失 `6% 当前HP` 并中毒 5 年）。实现：路径取样 + 在命中钩子里做多次“半径枚举爆发”（不做毒雾区列表）。 |
| `general_plague_tank` | 腐烂巨兽·格罗尔 | 坦克 | **毒素护甲**：受击时反施加“中毒”（10 年内攻击 -10%）。实现：在自身 `action_get_hit` 对攻击者加中毒 trait（到期年）。 | **腐烂光环**：法力消耗 `5% MaxMana` / 持续 30 年；持续期内当格罗尔命中/受击时：以当前 `pTile` 半径 12 对敌军结算一次 `10% 当前HP` 伤害（最低扣到 1HP）。实现：施放时给自身加 `trait_era_plague_rot_aura`（写到期年），该 trait 在 `action_attack_target/action_get_hit` 做半径枚举扣血。 |
| `general_plague_dps` | 变异领主·泽克斯 | 输出 | **突变体质**：每次命中有 `5%` 概率获得 20 年增益（攻击/防御/移速 +20%），最多叠 2 层。实现：在 `action_attack_target` 里做概率 + 层数写入 `custom_data_int` + buff trait。 | **毒素爆发**：法力消耗 `5% MaxMana`；以目标 `pTile` 半径 8 立刻造成固定伤害（默认 100）并给敌军叠感染 2 层。实现：范围扣血 + 写入感染层数（不做区域持续）。 |
| `general_plague_support` | 瘟疫祭司·娜塔莎 | 辅助 | **感染仪式**：当娜塔莎命中/受击时：以当前 `pTile` 半径 12，对感染者把 `custom_data_int["era_infection_hits_taken"]` 额外 +5（上限不超过转化阈值），等效“加速转化”。实现：在钩子里半径枚举感染者并写字段（不做每年扫描）。 | **瘟疫治愈**：法力消耗 `5% MaxMana`；治疗周围友军 30% 最大HP，并移除 1 个负面状态。实现：`restoreHealthPercent(...)` + 移除 trait。 |
| `general_plague_elite` | 疫病之母·玛拉凯 | 精英 | **超级感染**：感染上限提高到 6 层；感染层数 ≥5 的目标在其受击时有 `5%` 概率直接转化为仆从。实现：把规则合并到 `trait_era_infection.action_get_hit` 的判定里（仅对“超级感染来源”生效）。 | **疫病领主召唤**：法力消耗 `5% MaxMana`；召唤 2 个“疫病领主”，优先围攻最近大城。实现：批量 `spawnNewUnit(...)` + `goTo(...)`（受军团上限约束）。 |

</details>

<a id="generals-mech"></a>
##### 2.6.3.3 机械暴君将领（领域：机械）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_mech_dps` | 歼灭者·阿尔法 | 输出 | **火控校准**：攻击 +15%，并获得“攻城加压”：当阿尔法普攻命中建筑时，额外造成一次固定伤害（默认 30）。实现：在 `action_attack_target` 里检测 `pTarget` 为建筑并 `changeHealth(-30)`。 | **离子炮**：法力消耗 `5% MaxMana`；对目标点半径 4 造成固定伤害（默认 120）并短暂眩晕（0.3 年）。实现：范围扣血 + `addStunnedEffectOnTarget(...)`。 |
| `general_mech_vanguard` | 侦察者·贝塔 | 先锋 | **扫描协议**：当贝塔命中/受击时：在半径 20 内选择最近英雄作为“扫描目标”（用于 UI），并刷新 10 年“受伤 +10%”标记。实现：在 `action_attack_target/action_get_hit` 里做局部选目标 + 加 debuff trait。 | **扫描锁定**：法力消耗 `5% MaxMana` / 持续 20 年；给目标英雄施加“锁定”（受伤 +30%）。实现：命中时施放并写到期年（到期清理由三钩子检查）。 |
| `general_mech_tank` | 堡垒·伽马 | 坦克 | **反击协议**：受击时有 15% 概率使攻击者短暂眩晕（0.2 年）并减速 5 年（-15%）；为避免刷屏，改为“每累计受击 `H=10` 次最多触发 1 次”。实现：在 `action_get_hit` 里用 `custom_data_int` 做受击计数与触发重置。 | **能量护盾**：法力消耗 `5% MaxMana` / 持续 12 年；获得护盾值 `600 + 60×轮回数`，护盾期间减伤 20%（不做控制免疫）。实现：护盾数值（MOD 内部）+ 护盾 trait 的 `action_get_hit`（`GetHitAction`）减伤。 |
| `general_mech_support` | 工程师·德尔塔 | 辅助 | **维修协议**：当德尔塔命中/受击时：以当前 `pTile` 半径 12 为机械友军恢复 `3% 最大HP`（单次处理上限）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `restoreHealthPercent(...)`。 | **生产线**：法力消耗 `5% MaxMana`；召唤 3 个机械兵（军团先锋/主力按比例），并立即集结到自身。实现：批量 `spawnNewUnit(...)` + `goTo(...)`（受军团上限约束）。 |
| `general_mech_elite` | 终结者·欧米茄二号 | 精英 | **歼灭协议**：对建筑伤害 ×3；对军团单位伤害 +30%。实现：命中加成。 | **同化光束**：法力消耗 `5% MaxMana`；对目标单位施加“同化标记”：目标累计受击 `H=20` 次后转化为机械单位；英雄仅施加虚弱（不强制同化）。实现：命中时加 `trait_era_mech_assimilate_mark`，在其 `action_get_hit` 里计数到阈值后 spawn+remove。 |

</details>

<a id="generals-time"></a>
##### 2.6.3.4 时空扭曲者将领（领域：时空）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_time_vanguard` | 时空游侠·克洛诺 | 先锋 | **时间漂移**：受到致命伤害时有 20% 概率瞬移脱离；为限制频率，改为“每累计受击 `H=80` 次最多触发 1 次”。实现：在自身 `action_get_hit` 里做致命判定 + 受击计数阈值，触发后清零计数并 `teleportRandom(...)`。 | **闪烁**：法力消耗 `5% MaxMana`；瞬移到目标附近随机位置，并获得 5 年移速 +50%。实现：传送 + trait。 |
| `general_time_tank` | 时间守护者·埃恩 | 坦克 | **时间护盾（被动触发）**：HP < 30% 受击时自动获得 5 年减伤 60%；为限制频率，改为“每累计受击 `H=100` 次最多触发 1 次”。实现：在 `action_get_hit` 里用受击计数阈值 + 到期年控制。 | **时间静止**：法力消耗 `5% MaxMana`；半径 10 敌军获得 1 年“静止标记”：带标记敌军在其后续每次受击时触发 0.3 年眩晕（英雄 0.2 年），最多触发 3 次。实现：范围加标记 trait，并在标记的 `action_get_hit` 里短控+次数递减。 |
| `general_time_dps` | 命运编织者·莫伊拉 | 输出 | **命运之箭**：对英雄命中必定暴击（用伤害 ×1.5 近似）。实现：命中加成。 | **因果链**：法力消耗 `5% MaxMana`；对 1 个目标造成伤害后，弹射到最近 3 个敌人（每次伤害衰减 20%）。实现：邻近搜索 + 多段扣血。 |
| `general_time_support` | 时空祭司·泰姆拉 | 辅助 | **时间加速光环**：当泰姆拉命中/受击时：以当前 `pTile` 半径 12 为友军额外恢复法力 `5% MaxMana`（不超过 MaxMana，仅对有法力者生效）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `addMana(...)`。 | **时间回溯**：法力消耗 `5% MaxMana`；在据点召唤 1 名“时间幻影将领”（属性为本体 50%），持续到泰姆拉被移除/死亡或幻影被击杀。实现：`spawnNewUnit(...)`；清理由泰姆拉的 `action_on_object_remove` 批量移除仍存活的幻影。 |
| `general_time_elite` | 永恒者·伊特尼提 | 精英 | **时间循环（阈值触发）**：HP < 20% 时触发一次“回环”：瞬移到安全位置并恢复 20% 最大HP；为限制频率，改为“每累计受击 `H=120` 次最多触发 1 次”。实现：在 `action_get_hit` 里做阈值检测 + 受击计数阈值，触发后清零计数并 `teleportRandom(...)` + `restoreHealthPercent(...)`。 | **命运改写**：法力消耗 `5% MaxMana`；目标英雄 10 年内伤害 -30%，并短暂眩晕（0.3 年）。实现：debuff trait + 短控。 |

</details>

<a id="generals-fire"></a>
##### 2.6.3.5 混沌炎魔将领（领域：火焰）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_fire_vanguard` | 火焰使者·伊格尼斯 | 先锋 | **炽焰印记**：普攻命中时点燃目标 5 年；点燃的额外伤害改为“目标后续每次受击触发”（每次受击额外 -3% 当前HP）。实现：命中加 `trait_era_burn_mark`，在标记的 `action_get_hit` 里扣血。 | **火焰冲锋**：法力消耗 `5% MaxMana`；冲锋到目标并造成一次伤害 ×1.5，点燃目标 10 年。实现：位移引导 + 点燃标记（受击触发扣血）。 |
| `general_fire_tank` | 熔岩巨人·玛格玛 | 坦克 | **熔岩护甲**：近战攻击者受到固定反伤（默认 50）。实现：用护甲 trait 的 `action_get_hit`（`GetHitAction`）对攻击者 `changeHealth(-dmg)`。 | **岩浆喷发**：法力消耗 `5% MaxMana`；目标区域半径 10 转熔岩地形（每次最多改 20 格），并眩晕敌军 1 年。实现：`WorldTile.setTileType(...)` + 范围眩晕。 |
| `general_fire_dps` | 地狱火焰·因菲诺 | 输出 | **焚烧**：普攻命中附加 10 年“焚烧标记”；带标记目标后续每次受击额外损失 `4% 当前HP`。实现：命中加 `trait_era_burn_dot`，额外伤害在其 `action_get_hit` 里结算（不按年 DOT）。 | **烈焰风暴**：法力消耗 `5% MaxMana`；半径 12 造成固定火焰伤害（默认 300）。实现：范围扣血 + 表现用 `ActionLibrary.castFire(...)`。 |
| `general_fire_support` | 火焰祭司·辛德拉 | 辅助 | **火焰祝福**：当辛德拉命中/受击时：以当前 `pTile` 半径 12 刷新友军“火焰附魔”（持续 5 年）：友军命中会给目标附加 5 年燃烧标记（燃烧额外伤害走受击触发）。实现：在钩子里半径枚举给友军加 trait。 | **火焰复苏**：法力消耗 `5% MaxMana` / 持续 20 年；持续期内当友军命中/受击且站在火焰/熔岩地形上时，恢复量 ×1.5，并获得 20 年火抗。实现：给友军刷新“复苏标记 trait”，在其命中/受击钩子里做地形判定与回血倍率。 |
| `general_fire_elite` | 炎魔之子·弗雷格 | 精英 | **火焰抗性**：火焰伤害 -80%（口径：仅对 MOD 的火焰伤害/燃烧触发伤害生效）；并在受击时若站在熔岩上恢复 `5% 最大HP`。实现：在自身 `action_get_hit` 里做地形判定 + `restoreHealthPercent(...)`。 | **末日烈焰（局部）**：法力消耗 `5% MaxMana` / 持续 20 年；以目标 `pTile` 半径 14 触发一次火雨爆发（一次性范围伤害），并给敌军附加 20 年“热浪标记”：其后续每次受击额外掉血（小额）。实现：范围扣血 + 标记的 `action_get_hit` 追加伤害（不做区域列表）。 |

</details>

<a id="generals-abyss"></a>
##### 2.6.3.6 深渊邪神将领（领域：疯狂）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_abyss_vanguard` | 低语者·萨尔 | 先锋 | **精神侵蚀**：命中英雄使其疯狂值 +20（0-100）。实现：用 `ActorTrait.action_attack_target` 命中时把疯狂值写入目标 `custom_data_int["era_madness"]`（0-100）。 | **低语突袭**：法力消耗 `5% MaxMana`；使目标英雄立即 `makeConfused(...)` 5 年，并降低其攻击 10 年。实现：混乱 + trait。 |
| `general_abyss_tank` | 深渊守卫·达贡 | 坦克 | **深渊护甲**：当达贡命中/受击时：以当前 `pTile` 半径 10 对疯狂值 ≥50 的敌军刷新“攻击 -15%”（持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举 + debuff trait。 | **恐惧光环**：法力消耗 `5% MaxMana` / 持续 20 年；持续期内当达贡命中/受击时：以当前 `pTile` 半径 12 刷新敌军攻击 -25%。实现：施放后给自身加光环 trait（到期年），光环 trait 在 `action_attack_target/action_get_hit` 做半径枚举。 |
| `general_abyss_dps` | 疯狂使者·尼亚拉 | 输出 | **疯狂共鸣**：对疯狂值越高的目标伤害越高（满 100 时 +100%）。实现：按疯狂值加成。 | **精神爆破**：法力消耗 `5% MaxMana`；对目标区域半径 8 造成伤害，对疯狂值 ≥50 的单位伤害 ×3。实现：范围扣血 + 条件倍率。 |
| `general_abyss_support` | 邪教主教·海德拉 | 辅助 | **深渊祝福**：当海德拉命中/受击时：以当前 `pTile` 半径 12 刷新友军“疯狂值增长 -50%”标记，并使混乱/恐惧持续时间 -50%（仅对 MOD 精神控制生效）。实现：在钩子里半径枚举给友军加 trait 标记；写入到期年时按标记缩短。 | **邪教仪式**：法力消耗 `5% MaxMana`；在目标 `pTile` 周围召唤邪教徒 3 名，并命令其优先靠近英雄传播疯狂。实现：`spawnNewUnit(...)` + `goTo(...)`（受军团上限约束）。 |
| `general_abyss_elite` | 触手领主·肖格斯 | 精英 | **触手横扫**：普攻命中时对目标周围 2 个敌人造成 50% 溅射伤害（每累计命中 `H=20` 次最多触发 1 次）。实现：在 `action_attack_target` 里用命中计数阈值限频，并邻近搜索 2 个敌人 `changeHealth(-dmg)`。 | **深渊凝视（弱化版）**：法力消耗 `5% MaxMana`；对目标英雄疯狂值 +30，并短暂眩晕（0.5 年）。实现：疯狂值写入 `custom_data_int["era_madness"]` + `addStunnedEffectOnTarget(...)`。 |

</details>

<a id="generals-death"></a>
##### 2.6.3.7 死亡君王将领（领域：亡灵）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_death_vanguard` | 死亡骑士·莫德雷德 | 先锋 | **尸骸践踏**：普攻命中时有 15% 概率在目标附近生成 1 个骷髅兵；为限频，改为“每累计命中 `H=20` 次最多触发 1 次”，且总触发上限 6 次。实现：在 `action_attack_target` 里做计数与上限控制，触发时 `spawnNewUnit("skeleton_thrall", tile, ...)`。 | **亡灵冲锋**：法力消耗 `5% MaxMana`；冲锋到目标并造成伤害 ×1.3，同时给目标附加 3 年“冲锋标记”；目标在标记期内死亡则额外召唤 1 个骷髅。实现：冲锋引导 + 命中标记 trait → 标记 trait `action_on_object_remove` 生成骷髅（受军团上限约束）。 |
| `general_death_tank` | 骨龙·尼德霍格 | 坦克 | **骨甲**：减伤 30%，并使控制持续时间 -30%（仅对 MOD 控制生效）。实现：骨甲 trait 的 `action_get_hit`（`GetHitAction`）减伤；控制缩短按“到期年”规则处理。 | **亡灵光环**：法力消耗 `5% MaxMana` / 持续 20 年；当尼德霍格命中/受击时：以当前 `pTile` 半径 12 为亡灵单位恢复 4% 最大HP（可限频）。实现：光环 trait 在 `action_attack_target/action_get_hit` 里范围恢复。 |
| `general_death_dps` | 死灵法师·凯尔苏斯 | 输出 | **灵魂收割**：击杀单位恢复 50% 最大HP；为限频，改为“每累计击杀 `K=3` 次最多触发 1 次”。实现：用 `Actor.kills` 增量触发（上次 kills 记到 `custom_data_int`）+ 次数上限。 | **死亡射线**：法力消耗 `5% MaxMana`；对目标英雄造成固定伤害（默认 100）且“无视护甲”。实现：直接扣血。 |
| `general_death_support` | 亡灵祭司·莉莉丝 | 辅助 | **死亡祝福**：半径 12 友军死亡后有 20% 概率转化为亡灵仆从。实现：当莉莉丝命中/受击时，以当前 `pTile` 半径 12 为友军刷新标记 `trait_era_death_bless_mark`；标记 trait 的 `action_on_object_remove` 在友军死亡时按概率 `spawnNewUnit("undead_thrall", tile, ...)`。 | **复活仪式**：法力消耗 `5% MaxMana`；在据点召唤 3 个亡灵单位（优先主力/精锐）。实现：批量生成（受军团上限约束）。 |
| `general_death_elite` | 死神使者·桑纳托斯 | 精英 | **死亡审判（弱化版）**：附近死亡事件触发骷髅生成概率 +15%。实现：全局/区域加成。 | **冥界召唤**：法力消耗 `5% MaxMana`；召唤“死神分身”2 个（临时精英单位，持续 50 年）。实现：生成 + 计时清理。 |

</details>

<a id="generals-soul"></a>
##### 2.6.3.8 灵魂编织者将领（领域：契约）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_soul_vanguard` | 诱惑者·莉莉丝 | 先锋 | **魅影**：对英雄命中时有 10% 概率触发“短暂魅惑”（目标停止攻击 1 年）。实现：用 `ActorTrait.action_attack_target` 命中时做概率判定，对目标 `makeWait(...)` 或短眩晕（按“<1 年”规则）。 | **魅惑**：法力消耗 `5% MaxMana`；目标英雄停止攻击 5 年，并获得“易伤”10 年。实现：`makeWait(...)`/短控 + 易伤 trait（到期年写入 `custom_data_int`）。 |
| `general_soul_tank` | 契约守护者·巴弗灭 | 坦克 | **契约壁垒**：当巴弗灭命中/受击时：以当前 `pTile` 半径 8 刷新友军减伤 10% 与“控制持续时间 -20%”（持续 5 年，仅对 MOD 控制生效）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 buff trait。 | **灵魂守护（近似嘲讽）**：法力消耗 `5% MaxMana` / 持续 20 年；指定 1 名友军获得护盾值（默认 300）与减伤 20%。守护期间当巴弗灭命中/受击时：对半径 8 敌军执行一次 `startFightingWith(巴弗灭)`，实现近似嘲讽（带数量上限）。 |
| `general_soul_dps` | 灵魂猎手·阿撒兹勒 | 输出 | **灵魂吸取**：击杀永久 +5% 攻击（上限 10 层）。实现：用 `Actor.kills` 增量触发并把层数写入 `custom_data_int` + trait 表现。 | **灵魂撕裂**：法力消耗 `5% MaxMana`；对“有契约层数”的目标伤害 ×3。实现：读取目标 `custom_data_int["era_contract_stacks"]` + 条件倍率。 |
| `general_soul_support` | 契约祭司·贝利亚尔 | 辅助 | **契约解除**：普攻命中时有概率移除目标 1 个增益状态；为限制频率，改为“每累计命中 `H=20` 次最多触发 1 次”。实现：在 `action_attack_target` 里计数到阈值后对目标移除 1 个增益 trait。 | **批量契约**：法力消耗 `5% MaxMana`；半径 12 内敌军全部获得 1 层契约。实现：范围施加契约层数。 |
| `general_soul_elite` | 傀儡大师·墨菲斯托二世 | 精英 | **契约侵蚀**：当带契约层数 ≥3 的英雄受击时：触发 0.5 年混乱并附加 1 年易伤（受伤 +15%）；为限制频率，改为“每累计受击 `H=10` 次最多触发 1 次”。实现：逻辑写在英雄的 `trait_era_contract.action_get_hit`（读 `era_contract_stacks` + 受击计数阈值）。 | **契约牵引**：法力消耗 `5% MaxMana` / 持续 30 年；给目标英雄施加“牵引标记”：其后续每次受击都会 `goTo(魔王据点/魔王本体附近 tile)` 并短暂混乱 0.5 年（最多触发 10 次）。实现：命中施放标记 trait，在标记的 `action_get_hit` 里牵引+混乱+次数递减。 |

</details>

<a id="generals-nature"></a>
##### 2.6.3.9 自然之怒将领（领域：自然）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_nature_vanguard` | 荆棘使者·索恩 | 先锋 | **荆棘印记**：命中目标附加 10 年减速（-20%），并在其脚下生成 1 个荆棘地块。实现：trait + 小范围地形改造（严格上限）。 | **荆棘冲锋**：法力消耗 `5% MaxMana`；冲锋路径留下荆棘（仅做地形表现，持续 15 年），并对路径附近敌军触发一次“荆棘爆刺”（半径 2：损失 `3% 当前HP`，最低扣到 1HP）。实现：路径取样 + 每个采样点做一次性半径枚举扣血（不做“经过后每年掉血”）。 |
| `general_nature_tank` | 古树守护者·伊格德拉西尔 | 坦克 | **树皮护甲**：减伤 40%。实现：护甲 trait 的 `action_get_hit`（`GetHitAction`）减伤。 | **根系缠绕**：法力消耗 `5% MaxMana`；半径 10 敌军减速 -40% 持续 10 年，并短暂眩晕（0.5 年）。实现：范围 debuff + 短控。 |
| `general_nature_dps` | 野兽之王·芬里尔 | 输出 | **狂暴**：HP < 50% 时攻击 +30%，移速 +15%。实现：阈值 trait。 | **召唤狼群**：法力消耗 `5% MaxMana`；召唤 3 只狼加入战斗并围攻最近城市。实现：生成单位（受军团上限约束）。 |
| `general_nature_support` | 自然祭司·德鲁伊娜 | 辅助 | **自然治愈**：当德鲁伊娜命中/受击时：以当前 `pTile` 半径 12 为友军恢复 `3% 最大HP`（单次处理上限）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `restoreHealthPercent(...)`。 | **植物召唤**：法力消耗 `5% MaxMana`；召唤食人花/植物怪 6 个守卫据点。实现：生成单位。 |
| `general_nature_elite` | 盖亚之子·泰坦 | 精英 | **大地之躯**：控制持续时间 -30%（仅对 MOD 控制生效）；建筑伤害 +30%。实现：trait。 | **地震**：法力消耗 `5% MaxMana`；目标区域半径 16，建筑 40% 概率受重创，单位短暂眩晕（0.5 年）。实现：建筑抽样扣血 + 范围短控。 |

</details>

<a id="generals-judge"></a>
##### 2.6.3.10 终焉审判者将领（领域：审判）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `general_judge_vanguard` | 天使长·米迦勒 | 先锋 | **圣痕**：命中英雄叠加 1 层审判（最多 3 层；每层受伤 +10%）。实现：用 `ActorTrait.action_attack_target` 命中时写入目标 `custom_data_int["era_judgement_stacks"]`（上限 3）并刷新到期年。 | **神圣冲锋**：法力消耗 `5% MaxMana`；无视地形冲锋到目标并造成伤害 ×1.5。实现：路径忽略用传送近似 + 扣血。 |
| `general_judge_tank` | 守护天使·乌列尔 | 坦克 | **神圣护盾**：减伤 25%，并使控制持续时间 -30%（仅对 MOD 施加的控制生效）。实现：护盾 trait 的 `action_get_hit`（`GetHitAction`）减伤；控制缩短按“到期年”规则处理。 | **神罚反击**：法力消耗 `5% MaxMana` / 持续 20 年；受击时给攻击者叠 1 层审判（持续期内最多触发 20 次）。实现：用反击 trait 的 `action_get_hit`（`GetHitAction`）做 `procs_left` 递减与叠层写入 `custom_data_int["era_judgement_stacks"]`。 |
| `general_judge_dps` | 审判天使·加百列 | 输出 | **神圣之剑**：对“被审判目标”伤害 +80%（不做 ×3）。实现：按审判层数/标记加成。 | **审判执行**：法力消耗 `5% MaxMana`；对审判 ≥3 层的英雄造成一次重击（当前HP -35%），并短暂眩晕（0.5 年），随后清空其审判层数。实现：条件扣血 + 控制 + 清层。 |
| `general_judge_support` | 救赎天使·拉斐尔 | 辅助 | **净化**：半径 12 友军负面状态持续时间 -40%（仅对 MOD 负面状态生效）。实现：trait 标记 + 对应负面效果写入到期年（`custom_data_int`）时按该标记缩短。 | **神圣治愈**：法力消耗 `5% MaxMana`；治疗半径 12 友军 60% 最大HP，并移除 1 个负面状态。实现：`restoreHealthPercent(0.6)` + 移除。 |
| `general_judge_elite` | 末日天使·亚兹拉尔 | 精英 | **末日宣告（局部）**：当亚兹拉尔命中/受击时：以当前 `pTile` 半径 14 刷新敌军攻击/防御 -5%（持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 debuff trait。 | **最终审判（局部）**：法力消耗 `5% MaxMana`；目标区域半径 16 敌军当前HP -15%（英雄减半），并施加 10 年移速 -10%。实现：范围扣血 + debuff。 |

</details>

---

### 2.7 军团系统（单位库）
<a id="s27"></a>

> `2.4` 已定义波次与生成规则；本节补齐“每个魔王 4 类军团单位（先锋/主力/精锐/终极）”的可实现口径：单位模板、倍率、以及每单位 **P0+S1**。

#### 2.7.1 军团通用规则（必须一致）

- **单位分层**：先锋（骚扰）/主力（均衡）/精锐（带技能）/终极（Boss）。
- **同时上限**：受 `2.4` 的“军团_同时上限”约束；超过则停止生成。
- **法力口径**：军团单位若配置 S1 主动技能，则其模板需选用“原生可见法力条”的基底；主动技能消耗法力（全局默认 `5% MaxMana`）。
- **技能口径**：军团单位技能尽量短（单次/短持续），避免复杂状态机；所有持续效果用 `Date.getCurrentYear()` + `custom_data_*` 记录到期年，并只在三钩子里检查与清理（不做按年轮询）。
- **死亡替换**：军团单位不做“永久变形”，需要“转化/同化”时用“替换生成（spawn + remove）”实现。
- **平衡约束**（军团 << 魔王）：军团不做“全图效果/复活/召唤自复制/直接处决/长期强控”；控制类效果默认不超过 0.5 年（Boss 可到 1 年）。

#### 2.7.2 军团单位倍率模板（默认）

| 阶层 | HP倍率 | 攻击倍率 | 防御倍率 | 移速倍率 | 技能配置 |
|------|--------|---------|---------|---------|---------|
| 先锋 | 0.7× | 0.8× | 0.6× | 1.3× | P0 + S1（主动耗法力） |
| 主力 | 1.0× | 1.0× | 1.0× | 1.0× | P0 + S1（主动耗法力） |
| 精锐 | 1.6× | 1.4× | 1.2× | 1.0× | P0 + S1（强控/爆发） |
| 终极 | 4.0× | 2.5× | 2.0× | 0.8× | P0 + S1（范围技能） |

#### 2.7.3 各魔王军团单位名册（4 类）

| 魔王领域 | 先锋 | 主力 | 精锐 | 终极 |
|------|------|------|------|------|
| 虚无 | 虚空行者 | 存在蚀刻者 | 虚空裂解者 | 反物质泰坦 |
| 瘟疫 | 感染者 | 瘟疫兽 | 变异领主 | 瘟疫泰坦 |
| 机械 | 机械兵 | 战斗机器人 | 歼灭者 | 机械泰坦 |
| 时空 | 时间碎片 | 时空行者 | 命运编织者 | 永恒泰坦 |
| 炎魔 | 火元素 | 熔岩兽 | 地狱火焰 | 炎魔泰坦 |
| 深渊 | 邪教徒 | 深渊行者 | 触手怪 | 旧日支配者 |
| 死亡 | 骷髅兵 | 亡灵骑士 | 死灵法师 | 骨龙 |
| 灵魂 | 傀儡 | 契约者 | 灵魂猎手 | 傀儡大师 |
| 自然 | 狼 | 熊 | 巨兽 | 世界树守护者 |
| 终焉 | 低阶天使 | 中阶天使 | 高阶天使 | 炽天使 |

#### 2.7.4 军团单位技能表（P0+S1）

> 说明：军团单位的“数值”默认是便于实现与平衡的起点；最终可在配置里按轮回/波次做倍率修正。

**军团索引**： [虚无](#units-void) · [瘟疫](#units-plague) · [机械](#units-mech) · [时空](#units-time) · [火焰](#units-fire) · [深渊](#units-abyss) · [亡灵](#units-death) · [契约](#units-soul) · [自然](#units-nature) · [终焉](#units-judge)

<a id="units-void"></a>
##### 2.7.4.1 虚无军团（虚空）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_void_vanguard` | 虚空行者 | 先锋 | **虚空步**：脱战 3 年移速 +20%，首次命中附加 5 年减速 -20%。实现：脱战计时 + trait。 | **裂隙跳跃**：法力消耗 `5% MaxMana`；瞬移到目标附近并造成伤害 ×1.2。实现：`teleportRandom(...)`/`singularityTeleportation(...)` + 扣血。 |
| `unit_void_main` | 存在蚀刻者 | 主力 | **蚀刻**：命中建筑伤害 +50%。实现：命中加成。 | **虚空撕裂**：法力消耗 `5% MaxMana`；对目标区域半径 6 造成固定伤害（默认 40）。实现：范围扣血。 |
| `unit_void_elite` | 虚空裂解者 | 精锐 | **裂解光环**：命中/受击时：以当前 `pTile` 半径 6 对敌军结算一次 `-2% 当前HP`（最低扣到 1HP）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `changeHealth(-dmg)`。 | **相位斩**：法力消耗 `5% MaxMana`；对英雄造成伤害 ×1.4，并短暂眩晕（0.5 年）。实现：条件扣血 + 短控。 |
| `unit_void_boss` | 反物质泰坦 | 终极 | **巨体**：减伤 25%（不做控制免疫）。实现：trait。 | **反物质震荡**：法力消耗 `5% MaxMana`；半径 14 敌军受到固定伤害（默认 120，英雄减半）并减速 10 年（-15%）；建筑受重创。实现：范围扣血 + debuff + 建筑扣血（分批）。 |

</details>

<a id="units-plague"></a>
##### 2.7.4.2 瘟疫军团（瘟疫）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_plague_vanguard` | 感染者 | 先锋 | **携毒**：命中叠 1 层感染（最多 3 层）。实现：用 `ActorTrait.action_attack_target` 命中时写入目标 `custom_data_int[\"era_infection_stacks\"]`（上限 3）并刷新到期年。 | **毒雾喷吐**：法力消耗 `5% MaxMana`；在目标点触发一次“毒雾冲击”（半径 6：敌军损失 `6% 当前HP`，并中毒 5 年）。实现：范围枚举扣血 + 中毒 trait（不做区域列表）。 |
| `unit_plague_main` | 瘟疫兽 | 主力 | **腐化咬噬**：对英雄伤害 +20%。实现：命中加成。 | **腐烂波**：法力消耗 `5% MaxMana`；半径 8 敌军当前HP -15%。实现：范围扣血。 |
| `unit_plague_elite` | 变异领主 | 精锐 | **突变**：每累计命中 `H=20` 次触发一次强化（攻击/防御 +20%，持续 20 年），触发后清零计数。实现：在 `action_attack_target` 里计数并刷新 buff trait。 | **感染爆发**：法力消耗 `5% MaxMana`；半径 10 敌军感染层数 +2。实现：范围叠层。 |
| `unit_plague_boss` | 瘟疫泰坦 | 终极 | **疫体**：中毒/感染伤害 -80%（口径：仅对 MOD 相关 DOT 生效）；受击时按累计次数触发自愈：每累计受击 `H=20` 次恢复 3% 最大HP。实现：trait + 在 `action_get_hit` 里计数与回血。 | **黑疫扩散**：法力消耗 `5% MaxMana`；以自身为中心半径 12 随机抽取 6 个点各触发一次“毒雾冲击”（半径 6），并给范围内敌军附加 10 年中毒。实现：tile 抽样 + 范围枚举（不做区域列表）。 |

</details>

<a id="units-mech"></a>
##### 2.7.4.3 机械军团（机械）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_mech_vanguard` | 机械兵 | 先锋 | **钢壳**：减伤 10%。实现：trait。 | **电击**：法力消耗 `5% MaxMana`；对目标短暂眩晕（0.2 年）并减速 5 年（-15%）。实现：短控 + debuff trait。 |
| `unit_mech_main` | 战斗机器人 | 主力 | **协同火力**：附近有机械单位时攻击 +10%（半径 8）。实现：邻近计数 + trait。 | **压制射击**：法力消耗 `5% MaxMana` / 持续 10 年；目标减速 -30%。实现：trait。 |
| `unit_mech_elite` | 歼灭者 | 精锐 | **破城**：对建筑伤害 ×2。实现：命中加成。 | **超载齐射**：法力消耗 `5% MaxMana`；半径 8 造成固定伤害（默认 80）。实现：范围扣血。 |
| `unit_mech_boss` | 机械泰坦 | 终极 | **自我修复**：受击时按累计次数触发修复：每累计受击 `H=20` 次恢复 4% 最大HP。实现：在 `action_get_hit` 里计数与回血。 | **天网指令**：法力消耗 `5% MaxMana` / 持续 30 年；周围机械单位攻击 +25%。实现：范围 buff。 |

</details>

<a id="units-time"></a>
##### 2.7.4.4 时空军团（时空）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_time_vanguard` | 时间碎片 | 先锋 | **时移**：受到伤害时有 10% 概率瞬移到附近空地；为限频，改为“每累计受击 `H=20` 次最多触发 1 次”。实现：在自身 `action_get_hit` 里做概率与计数限频，并执行传送。 | **闪回**：法力消耗 `5% MaxMana`；移速 +50% 持续 5 年。实现：trait。 |
| `unit_time_main` | 时空行者 | 主力 | **缓时打击**：命中使目标 5 年减速 -20%。实现：trait。 | **错位传送**：法力消耗 `5% MaxMana`；随机传送 1 名敌人到附近荒地。实现：`teleportRandom(...)`。 |
| `unit_time_elite` | 命运编织者 | 精锐 | **预知**：减伤 12%。实现：trait。 | **命运锁链**：法力消耗 `5% MaxMana`；半径 8 敌军短暂眩晕（0.5 年）。实现：范围短控。 |
| `unit_time_boss` | 永恒泰坦 | 终极 | **回环护盾（阈值触发）**：HP < 25% 时获得 10 年减伤 30%；为限频，改为“每累计受击 `H=150` 次最多触发 1 次”。实现：在护盾 trait 的 `action_get_hit` 里检查阈值/计数并减伤。 | **时间风暴**：法力消耗 `5% MaxMana`；对半径 10 敌军施加 15 年“风暴标记”：减速 -15%，并在其后续受击时有小概率短暂眩晕（0.2 年，单体可限频）。实现：范围加标记 trait + `action_get_hit` 概率短控。 |

</details>

<a id="units-fire"></a>
##### 2.7.4.5 炎魔军团（火焰）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_fire_vanguard` | 火元素 | 先锋 | **点燃**：命中附加 5 年燃烧；燃烧目标在其后续每次受击时额外损失 `2% 当前HP`。实现：命中加燃烧标记，在标记的 `action_get_hit` 里扣血。 | **火焰冲刺**：法力消耗 `5% MaxMana`；冲刺并点燃地块 10 年。实现：点火 + 位移引导。 |
| `unit_fire_main` | 熔岩兽 | 主力 | **耐热**：火焰伤害 -50%。实现：trait。 | **熔岩吐息**：法力消耗 `5% MaxMana`；半径 6 固定伤害（默认 60）并点火。实现：范围扣血 + 点火。 |
| `unit_fire_elite` | 地狱火焰 | 精锐 | **灼热光环**：命中/受击时：以当前 `pTile` 半径 8 对敌军结算一次 `-3% 当前HP`（最低扣到 1HP）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并扣血。 | **烈焰风暴**：法力消耗 `5% MaxMana`；半径 10 固定伤害（默认 120）。实现：范围扣血。 |
| `unit_fire_boss` | 炎魔泰坦 | 终极 | **熔岩之躯**：命中/受击时若站在熔岩上则恢复 4% 最大HP（可限频）。实现：在 `action_attack_target/action_get_hit` 里做地形判定 + `custom_data_int` 限频回血。 | **末日喷发**：法力消耗 `5% MaxMana`；目标区域转熔岩（每次最多改 30 格），并眩晕敌军 1 年。实现：地形改造 + 控制。 |

</details>

<a id="units-abyss"></a>
##### 2.7.4.6 深渊军团（疯狂）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_abyss_vanguard` | 邪教徒 | 先锋 | **低语**：命中英雄疯狂值 +10。实现：用 `ActorTrait.action_attack_target` 命中时写入目标 `custom_data_int[\"era_madness\"]`（0-100）。 | **祈祷**：法力消耗 `5% MaxMana`；半径 8 友军攻击 +10%，并给范围内敌军施加 10 年“低语标记”：标记单位在其后续受击时疯狂值 +5（有上限）。实现：范围加 buff trait + 标记 trait，在标记的 `action_get_hit` 里累加疯狂值。 |
| `unit_abyss_main` | 深渊行者 | 主力 | **恐惧**：命中目标 5 年攻击 -10%。实现：trait。 | **深渊踏击**：法力消耗 `5% MaxMana`；半径 6 短暂眩晕（0.3 年）并减速 5 年（-20%）。实现：短控 + debuff。 |
| `unit_abyss_elite` | 触手怪 | 精锐 | **疯狂共鸣**：对疯狂值 ≥50 的目标伤害 +50%。实现：条件加成。 | **触手缠绕**：法力消耗 `5% MaxMana`；单体“缠绕压制”3 年：标记期间目标每次受击触发 0.3 年眩晕（最多触发 5 次），并附加移速 -30%。实现：压制标记 + `action_get_hit` 限次短控 + debuff trait。 |
| `unit_abyss_boss` | 旧日支配者 | 终极 | **心智撕裂**：命中/受击时：以当前 `pTile` 半径 12 的敌军疯狂值 +15（上限 100）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并写 `custom_data_int`。 | **旧日降临（军团版）**：法力消耗 `5% MaxMana`；召唤触手 4 个守卫据点 50 年。实现：生成 + 清理。 |

</details>

<a id="units-death"></a>
##### 2.7.4.7 死亡军团（亡灵）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_death_vanguard` | 骷髅兵 | 先锋 | **亡灵坚韧**：减伤 10%。实现：trait。 | **骨刃突刺**：法力消耗 `5% MaxMana`；单体伤害 ×1.3。实现：扣血。 |
| `unit_death_main` | 亡灵骑士 | 主力 | **恐惧骑乘**：命中使目标 5 年移速 -15%。实现：trait。 | **亡灵冲锋**：法力消耗 `5% MaxMana`；冲锋并造成范围小伤害。实现：位移引导 + 范围扣血。 |
| `unit_death_elite` | 死灵法师 | 精锐 | **亡灵增幅**：半径 10 亡灵攻击 +15%。实现：范围 buff。 | **死灵召唤**：法力消耗 `5% MaxMana`；召唤骷髅兵 6 名。实现：生成单位。 |
| `unit_death_boss` | 骨龙 | 终极 | **骨甲**：减伤 25%（不做控制免疫）。实现：trait。 | **亡灵吐息**：法力消耗 `5% MaxMana`；半径 12 造成固定伤害（默认 120），并使目标 5 年内生命恢复 -30%。实现：范围扣血 + debuff。 |

</details>

<a id="units-soul"></a>
##### 2.7.4.8 灵魂军团（契约）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_soul_vanguard` | 傀儡 | 先锋 | **牵线**：命中目标 5 年攻击 -10%。实现：trait。 | **绞线**：法力消耗 `5% MaxMana`；单体短暂眩晕（0.3 年）并减速 5 年（-20%）。实现：短控 + debuff。 |
| `unit_soul_main` | 契约者 | 主力 | **契约烙印**：命中叠 1 层契约（最多 3）。实现：用 `ActorTrait.action_attack_target` 命中时写入目标 `custom_data_int["era_contract_stacks"]`（上限 3）并刷新到期年。 | **献祭**：法力消耗 `5% MaxMana`；对自身周围敌军造成伤害并自身掉血（自爆式）。实现：范围扣血 + 自身扣血。 |
| `unit_soul_elite` | 灵魂猎手 | 精锐 | **猎魂**：对契约目标伤害 +50%。实现：条件加成。 | **抽魂**：法力消耗 `5% MaxMana`；对英雄造成伤害并治疗自身 20% 最大HP。实现：扣血 + `restoreHealthPercent(...)`。 |
| `unit_soul_boss` | 傀儡大师 | 终极 | **操线领域**：半径 12 敌军更易混乱（概率 +20%）。实现：区域加成。 | **夺心**：法力消耗 `5% MaxMana`；选择 1 名英雄施加 5 年“夺心压制”（敌我不分的失控近似）。实现：施加“夺心标记”，标记期间目标每次受击触发短暂混乱（`makeConfused`）+ 牵引（`goTo`）（可限频）。 |

</details>

<a id="units-nature"></a>
##### 2.7.4.9 自然军团（自然）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_nature_vanguard` | 狼 | 先锋 | **追猎**：对落单目标伤害 +15%。实现：目标判定。 | **扑击**：法力消耗 `5% MaxMana`；单体短暂眩晕（0.2 年）并造成额外伤害 ×1.2。实现：短控 + 扣血。 |
| `unit_nature_main` | 熊 | 主力 | **厚皮**：减伤 15%。实现：trait。 | **震地**：法力消耗 `5% MaxMana`；半径 6 敌军减速 5 年。实现：trait。 |
| `unit_nature_elite` | 巨兽 | 精锐 | **狂暴**：HP < 50% 时攻击 +25%。实现：阈值 trait。 | **践踏**：法力消耗 `5% MaxMana`；半径 10 敌军短暂眩晕（0.5 年）并造成固定伤害（默认 60）。实现：范围短控 + 扣血。 |
| `unit_nature_boss` | 世界树守护者 | 终极 | **根系庇护**：命中/受击时：以当前 `pTile` 半径 12 的友军恢复 2% 最大HP（可限频）。实现：在 `action_attack_target/action_get_hit` 里范围恢复并用 `custom_data_int` 限频。 | **根系爆发**：法力消耗 `5% MaxMana`；目标区域生成荆棘地形（每次最多改 25 格）并减速敌军 20 年。实现：地形改造 + debuff。 |

</details>

<a id="units-judge"></a>
##### 2.7.4.10 终焉军团（审判）
<details>
<summary>展开单位表（4 类，P0+S1）</summary>

| 单位ID | 名称 | 阶层 | P0 被动（常驻） | S1 主动（法力/持续） |
|---|---|---|---|---|
| `unit_judge_vanguard` | 低阶天使 | 先锋 | **审判印记**：命中英雄叠 1 层审判（最多 2）。实现：用 `ActorTrait.action_attack_target` 命中时写入目标 `custom_data_int[\"era_judgement_stacks\"]`（上限 2）并刷新到期年。 | **雷击**：法力消耗 `5% MaxMana`；对目标造成固定伤害（默认 40）。实现：`castLightning(...)`/扣血。 |
| `unit_judge_main` | 中阶天使 | 主力 | **圣光**：半径 10 友军攻击 +10%。实现：范围 buff。 | **裁决**：法力消耗 `5% MaxMana`；对审判层数 ≥2 的目标造成额外伤害。实现：条件倍率。 |
| `unit_judge_elite` | 高阶天使 | 精锐 | **圣裁**：减伤 15%（不做“免疫控制”）。实现：trait。 | **圣裁光束**：法力消耗 `5% MaxMana`；半径 8 固定伤害（默认 90），并减速 5 年（-15%）。实现：范围扣血 + debuff。 |
| `unit_judge_boss` | 炽天使 | 终极 | **神性**：命中/受击时恢复 2% 最大HP（可限频）；审判层数上限 +1。实现：在 `action_attack_target/action_get_hit` 里回血并计数限频。 | **天罚（局部）**：法力消耗 `5% MaxMana`；施放时在自身周围半径 14 随机落雷 4 次（范围内敌军受伤），并给范围内敌军附加 15 年“天罚标记”：标记单位后续受击时有小概率再次落雷（可限频）。实现：tile 抽样 + 标记 trait 的 `action_get_hit` 里触发 `castLightning(...)`。 |

</details>

<a id="p3"></a>
## 第三部分：游戏数值与平衡
[↩ 目录](#toc) · [↑ 顶部](#top)

### 3.1 魔王成长公式

#### 3.1.1 基础强度增长

```
属性值 = 基础值 × (1 + 轮回倍率 × ln(轮回数 + 1))
```

**特点**：
- 对数增长，避免指数爆炸
- 前期快速强化，后期增速放缓
- 自然平衡感

**示例**（默认 25%/轮回）：
```
轮回 1: ×1.00
轮回 2: ×1.17
轮回 3: ×1.28
轮回 5: ×1.40
轮回 8: ×1.55
轮回 14: ×1.70 (硬上限触发)
```

#### 3.1.2 自适应难度（CSI 系统）

**文明强度指数 (CSI)** 由五维指标合成：

```
CSI = (人口权重 + 城市权重 + 发展权重 + 英雄权重 + 抗魔权重) / 5

各维详解：
- 人口权重 = (当前人口 / 最大历史人口) × 25%
- 城市权重 = (当前城市 / 历史最多城市) × 25%
- 发展权重 = (发展等级 / 10) × 25%（发展等级为 MOD 内部指数，综合人口/城市/文化繁荣/英雄等）
- 英雄权重 = (传奇英雄数 / 世界英雄总数) × 25%
- 抗魔权重 = (抗魔等级 / 10) × 25%

最终 CSI 范围：0-100
```

**魔王强度调整**：
```
最终强度 = 基础强度 × 自适应倍率

自适应倍率计算：
- CSI < 30: 倍率 = 自适应下限（默认 0.85）
- CSI 30-70: 倍率 = 线性插值(中段起点, 中段终点)（默认 0.90→1.10）
- CSI > 70: 倍率 = 自适应上限（默认 1.10）

变化阻尼：
- 每次更新 ΔRate = min(目标倍率 - 当前倍率, ±0.05)
- 避免忽强忽弱的体验问题
```

#### 3.1.3 难度边界保护

| 场景 | 触发条件 | 反应 |
|------|--------|------|
| **过于简单** | 连续 5 轮未死英雄、城市 0 损毁 | 下轮魔王强度 +10% |
| **过于困难** | 人口 < 500 或文明 < 2 | 自动进入"保底模式"，倍率固定 0.85 |
| **无解死档** | 存活文明 ≤ 1 | 开启"失败不死档"，可重启轮回保留进度 |

---

### 3.2 将领成长

| 属性 | 1 轮回 | 5 轮回 | 10 轮回 | 说明 |
|------|-------|--------|---------|------|
| 激活数量 | 2 个 | 6 个 | 6 个（上限） | 每轮回 +1，最多 6 个 |
| 平均等级 | 10 | 22 | 34 | 每轮回 +3 级，上限 40 级 |
| 平均属性 | 基础值 | ×1.35 | ×1.70 | 跟随魔王×0.7 系数 |

---

### 3.3 数值检查清单

开发时必须验证：

- [ ] 轮回 1 vs 轮回 5：难度有明显提升但仍可解
- [ ] 轮回 10 vs 轮回 1：难度提升倍数不超过 5 倍（过高导致无解）
- [ ] CSI 自适应倍率变化幅度每次 ≤ 0.05（平滑）
- [ ] 高轮回（5+）存在至少 3 种不同的克制策略
- [ ] 人口 < 100 时强制保底，不可继续提升难度

---

<a id="p4"></a>
## 第四部分：用户交互设计
[↩ 目录](#toc) · [↑ 顶部](#top)

### 4.1 设计目标（推倒重来版）

- **一眼看懂**：新手打开面板就能知道“现在发生什么、我该做什么”。  
- **两层 UI**：常用信息常驻、深度操作进大面板，避免屏幕拥挤。  
- **技能可见可控**：技能清单可阅读、可搜索、可开关。  
- **不做上帝模式**：UI 是“调参与查看”，不承诺全控游戏底层 AI。  
- **性能友好**：不依赖频繁 UI 刷新，不做高频全量列表重绘。  

### 4.2 双层结构（HUD + 控制面板）

**A. 轻量 HUD（常驻小面板）**  
只显示最关键的 6 件事：轮回/阶段、封印强度、活跃魔王、文明概况、危险提示、快捷按钮。  

```
┌──────── 纪元之轮 HUD ────────┐
│ 轮回: 3  阶段: 降临  封印: 18% │
│ 活跃魔王: 虚无之主 (HP 80%)   │
│ 文明: 7  人口: 12.5k  英雄: 8 │
│ ⚠️ 警报: 北境城沦陷            │
│ [打开面板] [暂停] [下一阶段]  │
└──────────────────────────────┘
```

**B. 控制面板（完整交互）**  
通过按钮或快捷键打开，支持分类浏览、技能开关、事件与配置管理。  

### 4.3 面板信息架构（新的栏目划分）

左侧为主栏目，右侧为“详情抽屉”（点选任意单位/技能后展开）。  

| 栏目 | 作用 | 典型操作 |
|---|---|---|
| **总览** | 全局状态与关键提示 | 只读为主、少量快捷按钮 |
| **战局** | 魔王/军团/英雄/文明的运行态 | 观察、筛选、定位、查看详情 |
| **技能库** | 所有技能说明 + 开关 | 搜索、开关技能、查看规则 |
| **封印与遗产** | 封印建筑、结算遗产 | 查看进度、规则提示、允许的干预 |
| **事件与日志** | 事件时间线、过滤 | 查看、导出、低风险手动触发 |
| **设置** | 参数/预设/导入导出/AI | 调参、保存方案、导入导出 |
| **调试**（隐藏） | 内部变量、开发工具 | 仅开发模式显示 |

### 4.4 总览页（面向新手）

**结构：四块卡片 + 一条时间线**  

```
┌─ 轮回与封印 ──────────┐  ┌─ 魔王态势 ──────────┐
│ 轮回: 3  阶段: 降临    │  │ 活跃: 虚无之主      │
│ 封印强度: 18% 下降中   │  │ HP: 80%  波次: 8/15 │
│ 距封印战: 约 40 年     │  │ 预警: 瘟疫母神 22%  │
└──────────────────────┘  └──────────────────────┘

┌─ 文明概览 ────────────┐  ┌─ 当前规则摘要 ──────┐
│ 文明: 7  人口: 12.5k  │  │ 世界停战: 生效      │
│ 城市: 45 英雄: 8(2传奇)│  │ 难度: 自适应 (高)   │
│ CSI: 72 (强势)         │  │ 关键规则: 3钩子技能 │
└──────────────────────┘  └──────────────────────┘

┌─ 时间线 ─────────────────────────────────────────┐
│ 🔴 战斗: 北境城沦陷   🟡 英雄: 莱昂升传奇         │
│ 🟢 文明: 人类王国壮大  ⚪ 系统: 停战令已生效       │
│ [更多] [过滤] [导出]                                 │
└───────────────────────────────────────────────────┘
```

### 4.5 战局页（统一看“谁在场上”）

**顶部筛选**：阵营/类型/危险等级/当前阶段  
**主体列表**：魔王、将领、军团、英雄、文明（可折叠）  
**右侧详情抽屉**：点谁看谁，内容统一为：

- 基础信息（名称/阵营/阶段/状态）
- 关键属性（HP/攻击/防御/法力）
- **技能卡片（只读）**：展示该对象的技能、触发方式、效果摘要
- 行为提示（比如“优先攻击城镇/正在撤退”）
- 快捷操作（仅安全类：定位、关注、标记）

### 4.6 技能库页（玩家最关心的地方）

目标：让玩家能**读懂技能**、**打开/关闭技能**、**搜索**。  

**技能列表结构（统一字段）**：
- 名称 / 类型（被动/主动）  
- 触发方式（命中/受击/死亡点）  
- 消耗（法力/无）  
- 效果一句话  
- 开关（启用/禁用）  

**开关规则（简单易懂）**：
- **全局开关**：对所有同类单位生效（默认）。  
- **分组开关**：按“魔王/将领/军团/英雄”类别分组。  
- **生效方式**：  
  - “立即生效”：下次触发时生效（推荐）  
  - “下轮回生效”：避免现有单位状态混乱（可选）  

**技能库示例布局**：

```
[搜索] 关键字...   [筛选] 魔王/军团/英雄
---------------------------------------------------
☑️ 虚空领域  被动  触发: 命中/受击  消耗: 无
   效果: 半径内敌军掉血 + 缓慢（简述）
☐ 黑疫降临  主动  触发: 命中  消耗: 5% MaxMana
   效果: 大范围虚弱 + 毒雾爆发（简述）
---------------------------------------------------
```

### 4.7 封印与遗产页（封印建筑可视化）

**封印建筑逻辑可视化**：
- 当前封印阶段  
- 封印遗迹坐标/区域  
- 进度条（推进/回退/暂停）  
- 触发条件提示（例如“区域内需 X 名英雄”）  

**遗产区**：  
- 已获得遗产列表  
- 本轮可选遗产预览  
- 遗产对文明的影响摘要  

### 4.8 事件与日志页

- **时间线**：按年份排序，支持关键词/类别过滤  
- **安全触发**：只允许触发“事件池”里标记为“安全”的事件  
- **导出**：导出为文本或 JSON（用于复盘/分享）  

### 4.9 设置与预设页

**分类**：轮回 / 难度 / 魔王 / 军团 / 封印 / 文明 / 英雄 / 叙事 / 性能 / UI  
**核心操作**：  
- 一键恢复默认  
- 导入/导出配置  
- 保存与加载预设  

### 4.10 UI 视觉规范（新的风格）

**风格关键词**：史诗、羊皮纸、金属、像“史书记载”而非系统后台。  

**颜色建议（避免紫色）**：
- 主色：深棕 #3A2F26  
- 次色：古金 #B28B4C  
- 背景：羊皮纸 #E6D7B8  
- 强调：暗红 #8E2E2E  
- 危险：深红 #B63A3A  
- 成功：墨绿 #2F6B4E  

**字体建议**：  
- 标题：像素或古典感字体（例：Press Start 2P）  
- 正文：易读中文字体（例：思源黑体/Noto Sans SC）  

**交互规范**：
- 主要按钮：古金底 + 深色文字  
- 次按钮：羊皮纸底 + 深棕描边  
- 危险按钮：深红底 + 白字 + 二次确认  
- 重要开关：需要“确认提示”或“提示生效方式”  

**提示与通知**：
- 弹窗尽量少，优先用顶部提示条  
- 连续触发的事件合并显示  
- 重要错误给出“可恢复按钮”  


---

<a id="p5"></a>
## 第五部分：技术可行性与实现方案
[↩ 目录](#toc) · [↑ 顶部](#top)

### 5.1 核心 API 确认

本 MOD 依赖的所有 API 已通过 WikiBox 文档或现有 MOD 实现验证：

| API / 功能 | 用途 | 确认状态 | 证据 |
|-----------|------|--------|------|
| `AssetManager.actor_library` | 注册/读取自定义单位模板 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `AssetManager.buildings` | 注册/读取自定义建筑模板 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `AssetManager.traits` | 添加特性 | ✅ 确认 | WikiBox 文档 |
| `ToolBox.LoadSprite()` | 加载自定义图片 | ✅ 确认 | WikiBox 文档 |
| `ActorManager.spawnNewUnit(...)` | 生成魔王/将领/军团（自定义生成） | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `BuildingManager.addBuilding(...)` | 运行时放置自定义建筑 | ✅ 可行（反射调用 internal） | `docs/api/Assembly-CSharp.md`（该重载为 `internal`） |
| 数据读取 API | 读取游戏统计 | ✅ 可行 | CollectionMod 实现 |
| 定时器系统 | MOD 轮询 | ✅ 确认 | ModLoader 框架 |
| JSON 文件 IO | 存档、配置 | ✅ 确认 | C# 原生 + WikiBox |
| `WorldTile.setTileType(...)` | 小范围地形改造/污染表现 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |

### 5.2 技术框架

```
┌─ MOD 核心 ──────────────────────┐
│ ERaWheelMain.cs                 │
│ ├─ 初始化、注册、更新循环       │
│ └─ UI 管理器                    │
└─────────────────────────────────┘
         ↓
┌─ 轮回系统 ──────────────────────┐
│ CycleManager.cs                 │
│ ├─ 状态机管理                   │
│ ├─ 轮回触发与结算               │
│ ├─ 遗产发放                     │
│ └─ 历史记录                     │
└─────────────────────────────────┘
┌─ 魔王系统 ──────────────────────┐
│ DemonLordManager.cs             │
│ ├─ 魔王初始化                   │
│ ├─ 状态转换                     │
│ ├─ 技能执行                     │
│ └─ 据点管理                     │
└─────────────────────────────────┘
┌─ 将领军团系统 ──────────────────┐
│ GeneralManager.cs               │
│ UnitManager.cs                  │
│ ├─ 将领生成/激活                │
│ ├─ 军团波次生成                 │
│ ├─ 契约侵蚀/牵引机制            │
│ └─ 战场热点/停战讨伐调度        │
└─────────────────────────────────┘
┌─ 文明英雄系统 ──────────────────┐
│ CivilizationManager.cs          │
│ HeroManager.cs                  │
│ ├─ CSI 计算                     │
│ ├─ 遗产应用                     │
│ ├─ 英雄晋升                     │
│ └─ 停战/讨伐管理                │
└─────────────────────────────────┘
┌─ 叙事系统 ──────────────────────┐
│ EventManager.cs                 │
│ AIIntegration.cs                │
│ ├─ 事件池管理                   │
│ ├─ LLM 调用与权限                │
│ ├─ 后备事件池                   │
│ └─ 操作日志                     │
└─────────────────────────────────┘
┌─ UI 系统 ────────────────────────┐
│ PanelController.cs              │
│ ├─ HUD 轻量面板                 │
│ ├─ 总览面板                     │
│ ├─ 战局面板                     │
│ ├─ 技能库面板                   │
│ ├─ 封印与遗产面板               │
│ ├─ 事件与日志面板               │
│ ├─ 设置/预设面板                │
│ ├─ 调试面板（可选）             │
│ └─ 动画和过渡效果               │
└─────────────────────────────────┘
┌─ 配置与存档 ──────────────────────┐
│ ConfigManager.cs                │
│ SaveSystem.cs                   │
│ ├─ JSON 读写                    │
│ ├─ 参数校验                     │
│ ├─ 版本迁移                     │
│ └─ 备份 MOD 数据/配置            │
└─────────────────────────────────┘
```

### 5.3 资产注册与自动生成（实现口径）

本 MOD 采用“自定义生成”为默认策略：魔王/将领/军团/关键建筑均由 MOD 自动生成，避免依赖随机抽取现存单位导致的行为不可控与兼容性问题。

**资产注册（启动时）**：
- 单位：注册魔王/将领/军团等自定义单位模板（`ActorAsset`）到 `AssetManager.actor_library`
- 特性：注册遗产、感染、标记、护盾等自定义 `ActorTrait/KingdomTrait` 到 `AssetManager.traits`（用于 `action_attack_target / action_on_object_remove` 等钩子）
- 建筑：注册封印遗迹/魔王据点等自定义建筑模板（`BuildingAsset`）到 `AssetManager.buildings`

**实体生成（运行时）**：
- 生成单位：通过 `ActorManager.spawnNewUnit(assetId, tile, ...)` 在目标地块直接生成新的 `Actor`
- 生成“封印遗迹/据点建筑”：通过反射调用 `World.world.buildings` 的 `internal addBuilding(...)` 直接放置；并用 `WorldTile.setTileType/setTopTileType/removeTrees/removeGrass` 做小范围视觉表现
- 自动选点：根据魔王类型偏好地形与“可行走/空间足够”等条件选取落点；若不满足则在有限半径内进行小范围地形修正后生成遗迹/据点建筑（严格限制频率与次数）

**封印遗迹（仪式封印）**：
- 封印战阶段自动生成“封印遗迹建筑（`Building`）”与仪式区域
- 封印遗迹是本 MOD 新注册的 `BuildingAsset`（可从中立世界建筑克隆再改），不是“选一个现有建筑变成遗迹”
- 建筑生成与清理：强盛期/封印战期随机点位生成；魔王被再封印后统一移除并清空记录；下一轮回重新随机生成
- “占领/进度条/打断/回退/完成触发封印”为 MOD 逻辑（范围检测 + 计时累积），不依赖玩家手动放置或游戏内置占领系统

### 5.4 性能优化策略

```
更新频率分层:
├─ 每 5 帧: 军团行为 AI
├─ 每 10 帧: 英雄追踪
├─ 每 30 帧: 文明数据统计
├─ 每 300 帧: AI 叙事检查
└─ 定时 (10 年内): 轮回阶段转换

对象池管理:
└─ 英雄 AI 节点缓存

内存优化:
├─ 事件池使用 ScriptableObject 引用
├─ 轮回历史每 5 轮自动清理旧数据
└─ 日志文件写入磁盘而非内存
```

---

<a id="p6"></a>
## 第六部分：版本规划与交付
[↩ 目录](#toc) · [↑ 顶部](#top)

本节将“最终目标设计”拆成可交付、可验收、可回退的阶段，避免实现时反复跳转与口径不一致。

### 6.1 版本目标与范围（口径）

**本文档定位**：目标态设计（最终希望达到的系统集合），实现允许分阶段落地，但每个阶段必须形成可运行的闭环。

**范围内（计划实现）**：
- 轮回系统与阶段推进（含结算与历史）
- 魔王/将领/军团的自定义生成与波次
- 自适应难度（CSI）与边界保护
- 纪元遗产（跨轮回持久化）
- 文明/英雄/联盟相关机制（最小可用 → 逐步完善）
- 事件池（无 LLM 可运行；有 LLM 为增强）
- 控制面板与配置导入/导出
- 存档/配置的版本迁移与回退

**范围外（暂不做承诺）**：
- 对游戏底层战斗 AI 的“全量可控/可脚本化”
- 大规模、持续性、无上限地形改造（受性能与兼容性约束）

### 6.2 里程碑建议（交付闭环）

| 里程碑 | 目标 | 核心交付 | 最小验收标准 |
|---|---|---|---|
| M0 原型（Prototype） | 跑起来、能观察 | 资产注册 + 生成 1 个魔王 + 基础 UI 状态展示 | 新存档可启动；无报错；可看到魔王与阶段状态 |
| M1 闭环（Core Loop） | 完成 1 次轮回 | 2.1 轮回推进 + 2.4 波次生成 + 封印战胜利结算 + 2.3 遗产发放 | 同一存档完成至少 1 次完整轮回；轮回计数正确；配置错误可回退 |
| M2 内容（Content） | 玩法可重复 | 10 魔王框架落地（技能可先占位）+ 将领/军团名册结构化 + 事件池扩充 | 同一存档完成至少 3 次轮回；不同魔王差异可感知 |
| M3 稳定（Stabilization） | 可长期运行 | 性能分层更新 + 存档/配置迁移 + UI 可用性打磨 | 同一存档完成至少 5 次轮回；无明显性能崩溃/卡死 |
| M4 发布（1.0） | 面向玩家发布 | 平衡调整 + 文档补全 + 默认参数定型 | 关键指标达标（见 1.4）；常见错误有提示与自愈 |

### 6.3 验收与回归清单（摘要）

- 轮回推进：阶段顺序正确；阈值/倒计时/强制推进可用且不会跳错状态
- 存档一致性：读档后轮回计数、阶段、魔王/将领/军团状态保持一致
- 配置鲁棒性：非法值（空/NaN/负间隔/超大值）被安全修正并提示，不崩溃
- 性能底线：高单位量场景下更新频率分层生效，避免每帧全量扫描

---

<a id="p7"></a>
## 第七部分：附录与快速参考
[↩ 目录](#toc) · [↑ 顶部](#top)

### 7.1 轮回阶段快速对照

| 阶段 | 触发 | 持续 | 主要事件 | 结束条件 |
|------|------|------|--------|--------|
| 预发展 | 游戏开始 | 0-200年 | 文明自然演化 | 繁荣度达成 |
| 预兆 | 繁荣度达成 | 30-50年 | 诡异事件、英雄觉醒 | 封印强度 ≤ 20% |
| 苏醒 | 封印强度 ≤ 20% | 30-100年 | 魔王现身、据点形成 | 封印强度 ≤ 10% |
| 降临 | 封印强度 ≤ 10% | 100-200年 | 大战争、军团波次 | 魔王 HP < 30% |
| 衰弱 | 魔王 HP < 30% | 20-50年 | 逃离、恢复、撤退/失控 | 自动触发 |
| 封印战 | 衰弱后自动 | 50-100年 | 决战窗口、三路胜利条件 | 胜利/失败 |
| 重建 | 胜利后 | 1 年 | 轮回结算、遗产发放、轮回数+1 | 进入预发展 |

### 7.2 十大魔王核心能力对比

| # | 魔王 | 核心机制 | 主要威胁 | 克制思路 |
|---|------|---------|--------|--------|
| 1 | 虚无之主 | 世界收缩 | 边缘地块“虚空化”（危险地形扩张） | 集中输出 |
| 2 | 瘟疫母神 | 感染转化 | 单位变节 | 隔离净化 |
| 3 | 机械暴君 | 反魔法 | 施法受抑制 | 物理英雄 |
| 4 | 时空扭曲者 | 因果锚点 | 致死回避 + 强控 | 多路同时 |
| 5 | 炎魔 | 地狱化 | 地形恶化 | 焦土战术 |
| 6 | 深渊邪神 | 精神污染 | 友军造反 | 快速击杀 |
| 7 | 死亡君王 | 亡灵复活 | 死亡扩军 | 净化亡灵、快速封印 |
| 8 | 灵魂编织者 | 契约烙印 | 英雄侵蚀/失控（混乱、易伤、牵引） | 群体作战 |
| 9 | 自然之怒 | 植物暴走 | 地形阻碍 | 高机动性 |
| 10 | 终焉审判者 | 模块化末日 | 末日倒计时 + 全局压制 | 超级英雄+多文明 |

<a id="s73"></a>
### 7.3 关键参数默认值速查

| 参数 | 值 | 备注 |
|------|-----|------|
| 轮回成长倍率 | 25%/轮回 | 可配置（不限制；内部仅做安全修正） |
| 初始封印强度 | 100% | 可配置（可超过 100%） |
| 封印衰减速率 | 5%/10年 | 可配置（可超过 100%） |
| 魔王生成间隔 | 10 年 | `<= 0` 自动修正为最小正值 |
| 军团初始数量 | 30 个 | 可配置 |
| 军团强度递增 | 15%/波 | 可配置（允许负值） |
| 自适应倍率 | 0.85-1.10 | 作为默认曲线；可改为更宽/更激进（注意平滑与稳定性） |
| 命定英雄每文明上限 | 1 名 | 可配置（晋升制，不走“出生随机”） |
| 命定英雄晋升冷却 | 30 年 | 英雄死亡后，文明再次晋升的冷却期 |
| 主动技能法力消耗比例（全局） | 5% MaxMana | 可配置；用于魔王/将领/军团/英雄的“主动类技能” |
| 命定英雄法力上限加成 | +170 | 可配置；实现建议：写入英雄 `custom_data_int` 并在 `Actor.getMaxMana()` 后置叠加 bonus |
| 命定英雄战斗回蓝加成 | +1/触发 | 可配置；实现建议：命定英雄命中/受击时按计数限频 `addMana(bonus)`（不超过 MaxMana） |
| 魔王阵营法力上限加成 | +0 | 可配置；作用于魔王/将领/军团单位（同上，用 `getMaxMana()` 后置叠加） |
| 魔王阵营战斗回蓝加成 | +0/触发 | 可配置；作用于魔王/将领/军团单位（命中/受击按计数限频 `addMana`） |
| 英雄家族继承率 | 30% | 概率按 `0%-100%` 归一 |
