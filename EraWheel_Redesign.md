# 纪元之轮：魔王轮回 MOD - 全新设计文档
<a id="top"></a>

## 文档信息

| 字段 | 值 |
|---|---|
| 项目编号 | ERA-WHEEL-DEMON-MOD |
| 版本 | 2.0（完全重新设计） |
| 最后更新 | 2026-02-01 |
| 目标平台 | WorldBox 0.51.2+ (NeoModLoader) |

---

<a id="doc-rules"></a>
## 阅读与口径约定（建议先看）

**文档定位**
- 面向开发：给出可落地的规则、实现口径与参数入口。
- 同时包含玩家 UI 说明，用于对齐“玩家看到什么”。

**推荐阅读顺序（开发优先）**
1. 先看「[1.2 玩法总览](#s12)」→「[2.1 轮回系统](#s21)」→「[2.4 敌方军势](#s24)」→「[2.5 王国与英雄](#s25)」，把玩法闭环打通。
2. 再看「[附录A：统一口径](#conventions)」与「[第五部分：技术可行性与实现方案](#p5)」，确认实现边界与口径。
3. 最后按需查「[第四部分：用户交互设计](#p4)」与「[第七部分：关键参数默认值速查](#p7)」及各附录大表。

**只查信息的快速入口**
- 默认值/参数：跳到「[7.1 关键参数默认值速查](#s71)」（第七部分）
- 技能表与名册：跳到「[附录E/F](#appendix-demon-skills)」
- 可用属性字段：跳到「[附录G：可用属性字段速查](#appendix-base-stats)」

**统一口径（必须遵守）**
- 默认值与参数：以「[第七部分](#p7)」为唯一权威来源；正文如有示例或范围描述，以第七部分为准。
- 实现口径与容错：统一见「[附录A](#conventions)」，正文只引用不重复。
- 技能/名册/大表：集中在「[附录E/F](#appendix-demon-skills)」，正文只讲规则。

**统一口径速览（只给入口）**
- 触发/存储/范围/生成/技能资源：见「[附录A](#conventions)」
- 数值口径与字段含义：见「[附录A](#conventions)」与「[附录G](#appendix-base-stats)」
- 默认消耗与频率：见「[7.1.7](#s71)」
- 其他默认参数：见「[第七部分](#p7)」

**检索建议**
- 关键词：轮回 / 阶段 / 魔王 / 将领 / 军团 / 遗产 / 封印 / 技能 / 波次

<a id="terms"></a>
## 术语速查（必读）

| 术语 | 含义 |
|---|---|
| 轮回（Cycle） | 从“魔王被再封印”到“下一次魔王被再封印”的完整周期 |
| 阶段（Stage） | 轮回内的进程阶段（预发展/预兆/苏醒/降临/衰弱/封印战/重建） |
| 战斗子状态 | 仅在“降临”阶段生效，用于描述魔王强弱变化（全盛/常态），不改变轮回阶段 |
| 王国（Kingdom） | 游戏中的“王国/国家”实体（`Kingdom`）：拥有城市、单位、国王与外交。本文提到的“王国数量/王国城市/王国人口”，默认都按 `Kingdom` 口径统计。 |
| 封印强度 | 封印剩余强度（影响阶段推进与魔王出现节奏） |
| 魔王/将领/军团 | 三层敌对单位结构：魔王（核心）→ 将领（精英）→ 军团（波次单位） |
| 多魔王互动模式 | 同时存在多个魔王时的关系规则（各自征战/联盟/内战/议会） |
| 机械单位 | 机械暴君阵营单位统称（含魔王/将领/军团，以及被“同化/转化”的单位），默认归魔王阵营 |
| 纪元遗产（Legacy） | 轮回结算后给予王国/英雄/魔王的永久增益（跨轮回继承） |
| P0 / S1-S6 | 技能标记口径：P0=被动；S1-S6=主动技能（按序号区分） |
| LLM | 大语言模型；用于叙事增强 |
| NeoModLoader | WorldBox 的 MOD 加载框架（本文目标平台） |

<a id="toc"></a>
## 目录（可点击）

**前置阅读**
- [阅读与口径约定](#doc-rules)
- [术语速查](#terms)

**正文结构（开发为主）**
1. [第一部分：项目概述](#p1)
2. [第二部分：核心系统设计](#p2)
   1. [2.1 轮回系统](#s21)
   2. [2.2 魔王状态机系统](#s22)
   3. [2.3 纪元遗产系统](#s23)
   4. [2.4 敌方军势（波次/将领/军团）](#s24)
      - [2.4.1 军团波次系统](#s241)
      - [2.4.2 将领系统（精英单位）](#s242)
      - [2.4.3 军团系统（单位库）](#s243)
   5. [2.5 王国与英雄系统](#s25)
3. [第三部分：游戏数值与平衡](#p3)
4. [第四部分：用户交互设计](#p4)
5. [第五部分：技术可行性与实现方案](#p5)
6. [第六部分：版本规划与交付](#p6)
7. [第七部分：关键参数默认值速查](#p7)

**附录（大表与口径）**
- [附录A：统一口径与实现约定](#conventions)
- [附录E：魔王技能表](#appendix-demon-skills)
- [附录F：将领名册与技能表](#appendix-general-skills)
- [附录G：可用属性字段速查](#appendix-base-stats)

---

<a id="p1"></a>
## 第一部分：项目概述

### 1.1 核心理念

**一句话概括**：将 WorldBox 变成一个"自我讲述的史诗叙事引擎"，通过 AI 驱动的动态轮回系统，让每场游戏都是独一无二的王国兴衰史。

### 1.2 玩法总览（游戏循环 + 系统关系）
<a id="s12"></a>

#### 1.2.1 游戏循环流程（玩家视角）

```
玩家启动游戏
   ↓
[预发展阶段] 观看王国自然演化
   ↓ (达到阈值)
[预兆阶段] 诡异事件、英雄觉醒
   ↓ 
[苏醒阶段] 魔王现身、据点形成
   ↓ 
[降临阶段（正式入侵）] 大规模战争、军团波次
   ↓ 
[衰弱阶段] 魔王逐渐虚弱、英雄突起
   ↓ 
[封印战阶段] 关键决战窗口
   ↓ (胜利 / 失败/灭绝)
[战后重建] 轮回结算 → 获得遗产 → 回到预发展
   ↓ (轮回计数 +1)
重复...（可无限循环或至终焉）
```

#### 1.2.2 系统关系总览（开发视角）
<a id="s20"></a>

**系统关系（从驱动到结果）**
- 轮回系统驱动阶段推进 → 魔王状态机切换 → 军团波次与将领激活
- 王国与英雄形成抗魔等级与遗产 → 提升对抗能力
- 轮回结算 → 纪元遗产 → 影响下一轮回

| 系统 | 主要作用 | 关键输入 | 关键输出 | 入口 |
|---|---|---|---|---|
| 轮回系统 | 驱动阶段与结算 | 繁荣度/封印强度/轮回数 | 当前阶段/结算结果 | [2.1](#s21) |
| 魔王状态机 | 决定魔王行为与技能 | 当前阶段/HP/魔王类型 | 技能激活/强度与行为 | [2.2](#s22) |
| 纪元遗产 | 轮回的“记忆” | 轮回结算数据 | 王国/魔王遗产 | [2.3](#s23) |
| 军团波次 | 敌军生成节奏 | 阶段/波次配置/军团库 | 军团生成与规模 | [2.4.1](#s241) |
| 王国与英雄 | 反哺与对抗系统 | 王国/英雄运行态 | 抗魔等级/英雄晋升 | [2.5](#s25) |
| 将领系统 | 精英单位与指挥层 | 魔王类型/轮回数 | 将领激活与技能 | [2.4.2](#s242) |
| 军团系统 | 单位库与规格 | 魔王类型/军团配置 | 军团单位与生成规则 | [2.4.3](#s243) |

想看细节实现：从「[2.1 轮回系统](#s21)」开始按系统阅读。

### 1.3 核心特色

| 特色 | 说明 |
|------|------|
| **轮回系统** | 每次"被再封印"为一个完整轮回；双方都变强；故事有记忆 |
| **十大魔王** | 虚无/瘟疫/机械/时空/炎魔/深渊/死亡/灵魂/自然/终焉，机制完全不同 |
| **多魔王互动** | 当 2+ 魔王同时苏醒，可切换内战/联盟/议会等模式，世界局势明显变化 |
| **纪元遗产** | 轮回结算后王国+英雄+魔王获得遗产：王国侧以“工艺/研究”解锁装备与能力，魔王侧以“征服/魔能”强化军势（均可配置） |
| **英雄系统** | 命定英雄由系统“晋升产生”（每王国/每轮回有上限）；以属性/特性/事件塑造故事主角，不依赖接管底层 AI 行为树 |
| **后备事件池** | 200+事件，无LLM也能运行；有LLM则故事更丰富 |
| **完整控制面板** | 玩家对 MOD 关键参数与运行状态有控制权；参数可改、可导出、可调试（对游戏底层状态不做“全量可控”承诺） |

### 1.4 成功指标

**说明**：本节是高层目标摘要；详细验收与回归清单见 [第六部分-6.3](#p6)。

| 指标 | 目标 |
|------|------|
| **完整轮回** | 同一存档完成至少 5 次完整轮回，计数正确，无崩溃 |
| **故事感** | 即使不配 LLM，也有 200+ 事件产生丰富叙事 |
| **可玩性** | 高轮回（5+）仍有解法，不会无解死档 |
| **易用性** | 玩家可通过 UI 调整关键参数；设置分新手/高级 |
| **鲁棒性** | 配置错误自动回退默认，游戏不崩溃，有错误提示 |

---

<a id="p2"></a>
## 第二部分：核心系统设计

本部分按系统优先级讲解。每个系统尽量按 **关键概念 → 游戏流程 → 参数配置 → 保护机制 → 实现口径** 的顺序组织。  
阅读提示：带“实现/口径/示例代码/【需反射】”的段落偏开发；玩家可跳过。系统关系总览见「[1.2.2](#s20)」。

### 2.1 轮回系统（核心驱动）
<a id="s21"></a>

#### 2.1.1 核心概念

**轮回** = 从"魔王被再封印"到"下一次魔王被再封印"的完整周期。

- **轮回计数** 从 0 开始，每次被再封印时 +1
- **轮回数据** 写入游戏存档（建议存到 `World.world.map_stats.custom_data`，【需反射】；不反射则改用 MOD 自有 JSON/存档文件），所以玩家只需用游戏自带的存档/读档，不需要额外按钮
- **轮回历史** 可在 UI 查看时间线和详细对比

#### 2.1.2 轮回阶段详解

##### [1] 预发展阶段

**触发条件**：
- 游戏开始时自动进入
- 第 1 轮回时，需要世界达到"繁荣度"阈值：
  - **世界现存人口阈值**，**或**
  - **世界现存城市阈值**
- 第 2+ 轮回时，自动进入（无额外触发条件）

**持续规则**：
- MOD 监控王国发展，每 10 年检查阈值一次
- 玩家也可通过控制面板"强制进入预兆阶段"

**状态表现**：
- 魔王处于封印状态，封印强度 100%
- 周围逐渐生成魔气节点（可视化）

**结束条件**：
- 条件满足 → 进入预兆阶段

##### [2] 预兆阶段

**触发条件**：
- 世界达到繁荣度阈值 → 自动进入

**持续规则**：
- 系统随机从事件池选择"预兆事件"触发（每 3-5 年一次）
- 预兆事件示例：先知梦境、邪异天象、英雄觉醒、野兽袭击等

**状态表现**：
- 封印强度开始衰减（默认 1%/年，可配置，见 [7.1.1](#s71)）
- 出现封印衰减视觉效果（魔气节点增多、地形扭曲）

**结束条件**：
- 封印强度衰至 20% 以下 → 自动进入苏醒阶段
- 或预兆持续年数到期（默认 30 年，可配置）→ 自动进入苏醒阶段
- 或玩家手动"强制苏醒"

##### [3] 苏醒阶段

**触发条件**：
- 封印强度 ≤ 20%

**持续规则**：
- 魔王实体在地图上生成，强度为魔王最大强度的 30%，使用自定义单位模板 ID 生成新的魔王单位，不通过“随机挑选/改造现存单位”的方式产生魔王（生成口径见 5.3）
- 魔王实体位置优先选择符合特性的地形；必要时在有限半径内做小范围调整（实现口径见 5.3，需限频）
- 生成点与主要王国/王城保持安全距离，避免“出生即撞城”或卡位
- 据点逐渐扩大，污染向外扩散
- 第一批将领开始激活（通常 1-2 个）
- 尚不生成军团（或生成极少）

**状态表现**：
- 魔王头像/标记出现在 UI
- 根据魔王类型改变地形视觉（虚无→灰白、瘟疫→绿毒、炎魔→熔岩等）
- 世界出现"异象"事件提示

**结束条件**：
- 封印强度衰至 10% 以下 → 进入降临阶段
- 或苏醒持续年数到期（默认 50 年，可配置）→ 进入降临阶段

##### [4] 降临阶段（正式入侵）

**触发条件**：
- 封印强度 ≤ 10%

**持续规则**：
- 魔王实体强度达到 100%（全盛）
- 军团开始按波次生成（默认每 10 年一波，可配置；`<=0` 的间隔会被修正为最小正值）
- 将领逐个激活（按轮回数量）
- 魔王主动发起进攻，攻击附近所有非魔王单位（城市/英雄/中立生物/野兽等）
- 事件频繁触发（战役、城市陷落、英雄阵亡等）

**状态表现**：
- 魔王强度条显示 100%
- 军团波次进度条显示当前波数
- 地图上出现大量敌军
- 事件日志频繁更新

**结束条件**：
- 入侵持续 100-200 年，**或**
- 魔王生命值 < 30% → 进入衰弱阶段

##### [5] 衰弱阶段

**触发条件**：
- 魔王 HP < 30% 或入侵持续超 200 年

**持续规则**：
- 魔王优先逃离战场，修复/恢复
- 军团生成暂停
- 部分将领可能撤退/失控（离场或脱离据点防线）
- 英雄乘机追击
- 系统按配置检查“衰弱持续年数/封印战触发条件”

**状态表现**：
- 魔王标记显示"虚弱"状态
- 进度条提示"离封印战还有多久"

**结束条件**：
- 衰弱持续年数到期（默认 20 年，可配置）→ 进入封印战阶段
- 或按配置/事件提前触发封印战（可选）

##### [6] 封印战阶段（决战窗口）

**触发条件**：
- 魔王进入衰弱 → 自动触发，持续年数可配置（默认 50 年）

**持续规则**：
- 系统自动生成封印遗迹（自定义建筑）与“仪式区域”（范围检测 + 进度条逻辑）；遗迹数量可配置（见 [7.1.11](#s71)）
  - 资产注册与运行时放置口径见 5.3（不改造现有建筑，从“中立世界建筑”克隆一份再改 ，不是把现有建筑“选中后改成遗迹”；封印后统一清理）
- 胜利条件（两选一，可配置）：
  1. **击杀封印**：直接击杀魔王
  2. **仪式封印**：友方单位在遗迹范围内完成“占据”（先清理完遗迹范围内的魔王势力，且人数达到阈值）即可**触发占领状态**；触发后不要求持续保持人数，除非触发中断条件。  
     当**同时处于“已占领”状态的遗迹数量达到阈值**时，仪式进度开始推进并累积至 100%。  
    （占领人数/清场要求/占领遗迹数量/进度速度/中断条件可配置，见 [7.1.11](#s71)）
- **遗迹争夺驱动（让双方会去打）**：
  - **战场热点**：系统定期从“魔王阵营”和“附近王国”各抽取一小队，给一次性 `goTo(遗迹附近)` 指令，把遗迹变成真实战场热点（不接管 AI 行为树）。  
  - **占领收益**：占领遗迹后，遗迹范围内友军获得小幅增益，促使双方主动争夺。  
  - **叠加规则**：同阵营多个遗迹收益不叠加（或设置上限），避免数值爆炸。  
  - 参数统一见 [7.1.11](#s71)。
- 如果魔王恢复到「封印战_回退_HP阈值」以上，则回到降临阶段继续战争（默认 70%，可配置，见 [7.1.1](#s71)）

**状态表现**：
- 封印遗迹标记在地图上
- 两种胜利条件进度条并行显示
- 倒计时提示

**结束条件**：
- 满足任意胜利条件 → 魔王被再封印（胜利条件可配置，见 [7.1.11](#s71)）
- 或魔王恢复→回到降临阶段
- 玩家也可主动放弃（导致世界灭绝或王国全灭）

##### [7] 战后重建阶段

**触发条件**：
- 魔王被再封印

**持续规则**：
- **轮回结算**：计算本轮数据、统计伤害、评估胜利程度
- **遗产发放**：王国获得纪元遗产（见 2.3 节）
- **魔王处理**：
  - 重置位置到世界某处
  - 强度根据轮回倍率提升
  - 据点清除或净化
- **转移状态**：进入预发展阶段
- **轮回计数** +1

**结果呈现**：
- UI 弹窗显示"轮回总结"
  - 本轮轮回次数、耗时、伤亡数据
  - 获得的遗产列表
  - 下轮对手强度预估

#### 2.1.3 轮回触发参数（可配置）

**默认值统一入口**：见 [7.1.1 轮回与阶段](#s71)

**参数口径与容错**：统一放在「[附录A：配置与容错](#conventions)」，本节不重复。

#### 2.1.4 保护机制

- **封印胜利保底**：可配置是否启用“击杀封印”保底规则（见 [7.1.11](#s71)）

#### 2.1.5 魔王池与苏醒选择（UI 规则）

> 目标：让玩家在 UI 里**勾选想要苏醒的魔王**，并决定“指定/随机”方式。

**基础规则**：
- UI 提供 10 个魔王的勾选列表，默认全部启用。
- 只有“已启用”的魔王才会进入本轮苏醒候选池。

**苏醒模式**：
- **指定模式**：玩家勾选哪几个，本轮就只苏醒这些魔王（可多选）。
- **随机模式**：从已启用池中随机抽取；可设置「苏醒数量」（1-10）。

**生效时机**：
- 默认在“下一次苏醒判定”生效；若要立即生效需 UI 确认提示。

**保护机制**：
- 若所有魔王都被禁用：轮回不触发魔王事件，UI 给出明确提示。

---

### 2.2 魔王状态机系统
<a id="s22"></a>

#### 2.2.1 核心概念

为避免“阶段”和“战斗态”混淆，魔王状态机分两层：
- **阶段状态（与 2.1 轮回阶段一一对应）**：封印 → 预兆 → 苏醒 → 降临 → 衰弱 → 封印战 → 再封印  
- **战斗子状态（仅在“降临”阶段生效）**：全盛 / 常态  

阶段状态决定是否有实体、军团与封印战；战斗子状态只影响强度、技能频率与波次速度。

#### 2.2.2 状态转换图（对齐 2.1）

```
[封印] 
  ↓ (世界繁荣度达成)
[预兆]
  ↓ (封印强度 ≤ 苏醒阈值 或 预兆持续年数到期)
[苏醒] → 实体生成，初始强度 = 设定比例
  ↓ (封印强度 ≤ 降临阈值 或 苏醒持续年数到期)
[降临] → 正式入侵
   └─ 战斗子状态：全盛（HP > 70%）/ 常态
  ↓ (HP < 衰弱阈值 或 降临持续年数到期)
[衰弱]
  ↓ (衰弱持续年数到期)
[封印战]
  ↓ (击杀封印 / 仪式封印)
[再封印] → 轮回计数 +1 → 战后重建

(回退：封印战中若 HP ≥ 回退阈值 → 回到 [降临])
```

#### 2.2.3 状态行为对照表（主状态）

| 主状态（阶段） | 技能激活 | 军团生成 | 将领数量 | 说明 |
|------|---------|--------|--------|--------|
| 封印 | 被动仅 | ❌ | 0 | 无实体，仅封印强度变化 |
| 预兆 | 被动仅 | ❌ | 0 | 触发预兆事件、封印开始衰减 |
| 苏醒 | 被动+1个主动 | ❌/极少 | 1-2 | 实体出现、据点形成 |
| 降临 | 主动技能可用 | ✅ | 按轮回 | 正式入侵，大规模战争 |
| 衰弱 | 被动为主 | ❌ | ×0.5（可配，见 7.1.2） | 进入封印战前的疲态期 |
| 封印战 | 被动为主 | ❌ | 由配置决定 | 围绕遗迹与仪式推进 |
| 再封印 | ❌ | ❌ | 0 | 轮回结算与重置 |

**战斗子状态（仅在“降临”阶段）**：
- **全盛**：HP > 70%，法力恢复 +50%，军团波次加速 ×2（默认值见 [7.1.2](#s71) / [7.1.3](#s71)）

#### 2.2.4 魔王技能库（移至附录）

> 为减少主文档冗长与跳转负担，完整技能表统一放在「附录E」。
> 入口：见 [附录E：魔王技能表](#appendix-demon-skills)。

### 2.3 纪元遗产系统（轮回的"记忆"）
<a id="s23"></a>

#### 2.3.1 核心概念

每次轮回结算后，**王国、英雄、魔王都会获得各自的“纪元遗产”**。  
王国遗产影响王国与英雄；魔王遗产影响魔王阵营。  
魔王的“轮回成长公式”仍保留（见 2.3.3），遗产用于“机制解锁/方向强化”，成长用于“基础数值抬升”。
遗产统一为 **10 个档位**，每完成 1 次轮回自动解锁下 1 档（上限 10），不再做条件判定。

#### 2.3.2 遗产分类与机制

| 遗产类型 | 效果示例 | 档位规则 |
|---------|---------|--------|
| **工艺遗产（王国）** | 解锁更高阶装备/材料池；按原版装备制作/领取逻辑**概率获得**（全王国可用，英雄/精英权重更高，非全体必得） | 档位 1-10：随档位扩展装备池 |
| **研究遗产（王国）** | 解锁“研究技能池”能力；按原版“特质/成长”逻辑**概率获得**（全王国可用，英雄/精英权重更高，非全体必得） | 档位 1-10：随档位扩展技能池 |
| **军事遗产（王国）** | 对魔王阵营单位攻击 +10%，防御 +10%（基础档位） | 档位 1-10：加成幅度递减（档位越高增量越大） |
| **传承遗产（英雄）** | 命定英雄晋升评分 +10%，英雄基础属性 +10%（基础档位） | 档位 1-10：加成幅度递减（档位越高增量越大） |

**魔王遗产（魔王阵营）**：

| 遗产类型 | 效果示例 | 档位规则 |
|---------|---------|--------|
| **征服遗产（魔王）** | 魔王阵营基础伤害/防御 +10%（基础档位） | 档位 1-10：加成幅度递减（档位越高增量越大） |
| **军备遗产（魔王）** | 解锁魔王专属装备池；生成/补给时**按概率配装**（魔王/将领权重更高，非全体必得） | 档位 1-10：随档位扩展装备池 |
| **魔能遗产（魔王）** | 魔王/将领法力上限与回复 +10%（基础档位） | 档位 1-10：加成幅度递减（档位越高增量越大） |
| **领域遗产（魔王）** | 领域类技能半径/次数 +10%（只影响 MOD 技能）（基础档位） | 档位 1-10：加成幅度递减（档位越高增量越大） |

**口径说明**：战斗数值强化主要归魔王遗产；王国侧以“工艺解锁/装备强化/英雄传承”为主，避免变成“王国也直接数值碾压”的不合理体验。  
**档位规则说明**：
- 统一 10 档，每轮回结算自动 +1 档，上限 10。
- **解锁类（工艺/研究/军备）**：档位只扩展可用清单，不直接叠加数值。
- **数值类（军事/传承/征服/魔能/领域）**：收益递减（档位越高增量越小，可配置）。

#### 2.3.2.1 遗产生效与出现方式（玩家视角）

- **总原则**：遗产不是“全员立刻强化”，而是**解锁池 + 被动 trait**，并沿用原版装备/特质逻辑，因此是“概率出现”。  
- **工艺遗产（王国装备）**：  
  - 解锁装备池，不直接全员配装。  
  - 走原版流程：`ItemCrafting.tryToCraftRandomWeapon/Equipment` 负责制作 → 城市库存 → `City.giveItem` 领取/替换。  
  - 是否拿到取决于资源、城市生产、AI 行为触发（`BehMakeItem` / `BehActorTryToTakeItemFromCity`），因此天然是概率获得。  
  - 可配置：英雄/精英单位权重更高，普通单位低概率。  
- **研究遗产（王国技能/特质）**：  
  - 解锁技能/能力池，不直接全员授予。  
  - 走原版“随机特质”逻辑：出生用 `Actor.checkTraitMutationOnBirth()` 从 `AssetManager.traits.pot_traits_birth` 抽取；成长用 `Actor.checkTraitMutationGrowUp()` 从 `pot_traits_growup` 抽取；抽取次数由 `Subspecies.getAmountOfRandomMutationsActorTraits()` 与 `Randy.randomInt(0,3)` 决定。  
  - 研究遗产以 trait 形式加入候选池（按档位解锁），因此是概率获得；英雄/精英可提高权重/多一次抽取。  
- **军事遗产**：**王国维度被动**。通过 `KingdomTrait` 影响本王国单位的战斗表现（对魔王阵营伤害/减伤），通常可立即生效。  
- **传承遗产**：**英雄维度加成**。主要影响“命定英雄晋升评分/基础属性”，在晋升或生成时写入英雄 trait；是否对已存在英雄**补发加成**可配置（默认不补发，避免突变）。  
- **魔王遗产（补充）**：  
  - **军备遗产**：解锁装备池，魔王/将领/军团生成或补给时**按概率配装**（非全体必得）。  
  - **征服/魔能**：数值类被动，全阵营直接生效。  
  - **领域**：当前为数值类被动；若后续扩展“领域技能池”，也按生成概率携带。

> 一句话总结：遗产不是“突然冒出来一个新生物”，而是**解锁池 + trait 加成**，在“制作/领取/出生/成长/战斗”等节点逐步体现出来。

#### 2.3.2.2 解锁档位清单（10 档，默认可配置）

**工艺遗产（王国）档位**（解锁装备池，全部为 MOD 自定义装备，带自定义外观/特效）：
- 1 层：基础工坊  
  - 风刃短剑（武器）：`damage +5%`，`speed +5%`；命中魔王阵营有 20% 概率追加 `2% 当前生命` 伤害（限频：每累计命中 `H=10` 次触发 1 次）  
  - 护城皮甲（护甲）：`armor +5%`，`health +5%`；受击时每累计 `H=12` 次触发一次护盾（默认动作）  
  - 灵木护符（饰品）：每累计命中 `H=8` 次恢复 `5% MaxMana`
- 2 层：精良工坊  
  - 破魔长枪（武器）：`damage +10%`；命中魔王阵营附加 5 年“破魔标记”（受伤 +10%）  
  - 城墙链甲（护甲）：`armor +8%`；对魔王阵营伤害减免 +8%  
  - 勇气戒（饰品）：`critical_chance +5%`，`critical_damage_multiplier +10%`
- 3 层：稀有工坊  
  - 陨铁巨刃（武器）：`damage +15%`；命中有 10% 概率附加 5 年易伤（受伤 +10%）  
  - 秘银护甲（护甲）：`armor +12%`；本 MOD 控制持续时间 -20%  
  - 战歌饰品（饰品）：命中时以自身 `pTile` 半径 6 友军攻击 +5%（持续 5 年，限频）
- 4 层：史诗工坊  
  - 封印裁决刃（武器）：`damage +20%`；命中魔王阵营有 15% 概率短暂眩晕（默认时长）  
  - 封印重铠（护甲）：`armor +15%`；HP < 40% 受击时触发护盾（限频）  
  - 圣印项链（饰品）：击杀时恢复 `5% 最大生命`
- 5 层：传奇工坊  
  - 破魔圣器（武器）：`damage +25%`；命中有 10% 概率落雷（单体）  
  - 王者战甲（护甲）：`armor +20%`；受击减伤 15%  
  - 神赐戒指（饰品）：HP < 30% 时触发 5 年全属性 +10%（限频）
- 6 层：神话工坊  
  - 星陨巨刃（武器）：`damage +30%`；命中魔王阵营有 25% 概率追加 `3% 当前生命` 伤害（限频：每累计命中 `H=8` 次触发 1 次）  
  - 神话重甲（护甲）：`armor +22%`，`health +10%`；受击减伤 10%  
  - 星纹护符（饰品）：`critical_chance +8%`，`critical_damage_multiplier +15%`
- 7 层：圣辉工坊  
  - 圣辉长枪（武器）：`damage +35%`；命中魔王阵营附加 8 年“破魔标记”（受伤 +12%）  
  - 圣辉板甲（护甲）：`armor +25%`；控制持续时间 -30%（仅对 MOD 控制生效）  
  - 圣辉戒指（饰品）：`critical_chance +12%`，`speed +10%`
- 8 层：远古工坊  
  - 远古裁决刃（武器）：`damage +40%`；命中有 15% 概率落雷（单体）  
  - 远古壁垒甲（护甲）：`armor +28%`；受击减伤 18%  
  - 远古圣印（饰品）：命中时以自身 `pTile` 半径 6 友军攻击 +8%（持续 5 年，限频）
- 9 层：至高工坊  
  - 至高破魔斧（武器）：`damage +45%`；命中魔王阵营追加 `4% 当前生命` 伤害（限频：每累计命中 `H=6` 次触发 1 次）  
  - 至高王铠（护甲）：`armor +30%`，`health +20%`；HP < 40% 受击时触发护盾（限频）  
  - 至高神赐戒（饰品）：HP < 35% 时触发 5 年全属性 +15%（限频）
- 10 层：终焉工坊  
  - 终焉圣裁器（武器）：`damage +50%`；命中魔王阵营有 20% 概率短暂眩晕（默认时长）  
  - 终焉守护铠（护甲）：`armor +35%`；受击减伤 25%  
  - 终焉灵戒（饰品）：命中/受击时恢复 `2% 最大生命（health_max）`（限频）

**研究遗产（王国）档位**（解锁技能池/能力，全部为 MOD 自定义技能）：
- 1 层：基础研究  
  - 净化：移除 1 个负面状态（无负面则小幅回血 5%）  
  - 破魔冲击：雷击 1 名魔王阵营目标并削减其 `10% MaxMana`
- 2 层：进阶研究  
  - 护封：施放护盾 + 5 年抗控（仅对 MOD 控制生效）  
  - 破魔标记：为目标附加 10 年“破魔标记”（受伤 +10%）
- 3 层：高阶研究  
  - 禁制雷击：雷击 + 短暂眩晕（默认时长）
- 4 层：史诗研究  
  - 群体圣愈：半径 8 友军恢复 `20% 最大生命` 并净化 1 个负面
- 5 层：传奇研究  
  - 天罚连击：对同一目标连续 3 次落雷
- 6 层：神话研究  
  - 圣域护壁：半径 6 友军获得 8 年减伤 15%  
  - 破魔连锁：对 2 名魔王阵营目标落雷（小范围）
- 7 层：圣域研究  
  - 群体净化：半径 10 友军移除 2 个负面状态  
  - 破界震荡：半径 6 敌军损失 `10% 当前生命（health_current）`（英雄减半）
- 8 层：远古研究  
  - 时停封印：目标短暂眩晕（默认时长）并减速 10 年  
  - 圣光回响：半径 8 友军恢复 `15% 最大生命` 并获得 5 年攻防 +5%
- 9 层：至高研究  
  - 天罚暴雨：目标区域随机落雷 5 次  
  - 破魔禁域：半径 8 魔王阵营单位法力回复 -50%（持续 10 年）
- 10 层：终焉研究  
  - 终焉封印：对魔王阵营单体造成 `40% 当前生命（health_current）` 伤害（英雄减半）并短暂眩晕  
  - 神佑结界：半径 8 友军获得 10 年减伤 20%（限频）

**军备遗产（魔王）档位**（解锁装备池，全部为 MOD 自定义装备，带自定义外观/特效）：
- 1 层：魔钢制式  
  - 魔钢巨刃（武器）：`damage +5%`；命中英雄有 15% 概率追加 `2% 当前生命` 伤害  
  - 魔钢胸甲（护甲）：`armor +5%`，`health +5%`
- 2 层：邪铁重装  
  - 邪铁重甲（护甲）：`health +10%`；受击有 15% 概率使攻击者减速 5 年（-10%）  
  - 邪铁钉锤（武器）：`damage +10%`；命中有 15% 概率短暂眩晕（默认时长）
- 3 层：虚空利刃  
  - 虚空刃（武器）：`damage +15%`；命中有 10% 概率瞬移到目标附近  
  - 虚空披风（护甲）：`speed +10%`；受击有 10% 概率随机传送脱离（限频）
- 4 层：深渊符印  
  - 深渊符刃（武器）：`critical_chance +10%`，`critical_damage_multiplier +15%`  
  - 深渊印记（饰品）：命中附加 5 年易伤（受伤 +10%）
- 5 层：天灾遗物  
  - 天灾巨镰（武器）：命中触发半径 6 敌军 `2% 当前生命` 伤害（限频）  
  - 天灾甲胄（护甲）：`armor +20%`；受击减伤 15%
- 6 层：灾星锻造  
  - 灾星战刃（武器）：`damage +25%`；命中英雄有 20% 概率追加 `3% 当前生命` 伤害（限频）  
  - 灾星重甲（护甲）：`armor +22%`，`health +10%`
- 7 层：噬界锻造  
  - 噬界大镰（武器）：`damage +30%`；命中英雄附加 8 年易伤（受伤 +12%）  
  - 噬界战甲（护甲）：`armor +25%`；受击有 15% 概率使攻击者减速 5 年（-15%）
- 8 层：深渊神铸  
  - 深渊神刃（武器）：`critical_chance +12%`，`critical_damage_multiplier +20%`  
  - 深渊神甲（护甲）：`speed +10%`；受击减伤 15%
- 9 层：虚无圣铸  
  - 虚无圣镰（武器）：`damage +40%`；命中触发半径 6 敌军 `3% 当前生命` 伤害（限频）  
  - 虚无圣铠（护甲）：`armor +30%`；HP < 40% 受击时触发护盾（限频）
- 10 层：终焉遗铸  
  - 终焉裁决刃（武器）：`damage +45%`；命中英雄有 20% 概率短暂眩晕（默认时长）  
  - 终焉魔甲（护甲）：`armor +35%`；受击减伤 25%

**实现要点（统一）**
- 工艺遗产：每档为一组 **MOD 自定义** `ItemAsset` 列表，只进入原版“制作/领取”候选池（`ItemCrafting` → 城市库存 → `City.giveItem`），不直接全员配装；特效由“装备绑定 trait”实现（命中/受击钩子）。
- 研究遗产：以 **trait 形式**挂载，按档位加入 `pot_traits_birth` / `pot_traits_growup`；英雄仍遵循 `P0+S1`，与“英雄随机技能池”分开管理；默认法力消耗：普通 5 点 / 终极 10 点（可配）。
- 军备遗产：每档为魔王专属 **MOD 自定义** 装备 `ItemAsset` 列表；魔王/将领/军团生成或补给时按概率配装，不做战斗中动态换装。
- 装备溯源：装备生成时写入“制造王国/制作者”标记，用于叙事与追溯。

#### 2.3.2.3 王国掌握度与继承规则（混合方案，可实现）

**核心思路**：  
- 世界有“档位”（1-10）。  
- **存活王国**：在轮回结算后直接拿“世界档位”（视为满掌握度）。  
- **新建王国**：用“掌握度”来折算（0-100%）。  
- 新建王国生效档位 = `floor(世界档位 × 王国掌握度)`（最低档位可配置，见 [7.1.10](#s71)）。  

**默认规则用到的信息（来自现有 API）**：  
- 王国：`cities / capital / culture / religion / language`  
- 王国统计：`countCities() / countWarriors() / getPopulationTotal()`  
- 城市：`countBooks() / countBuildings()`  
- 文化/宗教/语言：`books`（书籍列表，或 `getBooks()`）  
- 书籍：`author_kingdom_id / author_city_id / culture_id / religion_id / language_id`（仅用于可选增强）

**掌握度计算规则（仅用于新建王国，默认公式）**：  
1) 先把上面的信息变成“分数”（全部 0-1）：  
- `文化书籍分` = `clamp01(文化书籍数 / 10)`  
- `宗教书籍分` = `clamp01(宗教书籍数 / 10)`  
- `语言书籍分` = `clamp01(语言书籍数 / 10)`  
- `首都书籍分` = `clamp01(首都书籍数 / 5)`  
- `全城书籍分` = `clamp01(所有城市书籍总数 / 30)`  
- `城市规模分` = `clamp01(城市数 / 5)`  
- `人口规模分` = `clamp01(总人口 / 688)`  
- `军力规模分` = `clamp01(战士数 / 200)`  
- `建筑规模分` = `clamp01(所有城市建筑总数 / 6)`  

2) 再把分数加权求和（默认权重，合计 1.00）：  
- 基础掌握度：`0.20`  
- 文化书籍：`+ 0.10 × 文化书籍分`  
- 宗教书籍：`+ 0.10 × 宗教书籍分`  
- 语言书籍：`+ 0.05 × 语言书籍分`  
- 首都书籍：`+ 0.10 × 首都书籍分`  
- 全城书籍：`+ 0.05 × 全城书籍分`  
- 城市规模：`+ 0.15 × 城市规模分`  
- 人口规模：`+ 0.10 × 人口规模分`  
- 军力规模：`+ 0.10 × 军力规模分`  
- 建筑规模：`+ 0.05 × 建筑规模分`  

3) 最终掌握度：`clamp01(基础 + 各项加成)`  

> 说明：阈值与权重只是默认值，可在配置中调整（见 [7.1.10](#s71)）。  
> 取数口径：文化/宗教/语言缺失时书籍数按 0 计；没有首都时首都书籍数按 0 计；  
> “全城书籍总数/建筑总数”= 遍历 `cities` 累加 `countBooks()/countBuildings()`。

**使用时机（避免误解）**：  
- 新王国建立：按公式算“初始掌握度”。  
- 轮回结算后：**存活王国直接拿世界档位**（不走掌握度计算）。  
- 运行中：可选每 `掌握度刷新间隔` 年更新新王国的掌握度（默认 10 年，见 [7.1.10](#s71)；不建议每年重算）。  
> 结果：存活王国能到 10 档；新王国按掌握度起步。  

**按遗产类型微调（可选）**：  
- 研究遗产：提高“书籍相关”权重，降低“军力/工艺”权重。  
- 军事遗产：提高“城市/军力”权重，降低“书籍/工艺”权重。  
- 工艺遗产：提高“建筑规模（建筑总数）”权重，略降“军力”权重。

**覆灭/复国表现**：  
- 王国覆灭时，王国掌握度归零；世界档位不变。  
- 新王国若文化/宗教/书籍延续，则掌握度自然恢复；若书籍被毁/文化断层，则掌握度偏低。  

#### 2.3.3 发放规则（结算时）

轮回结算时，系统只做“档位推进”：
- 所有遗产档位 +1（上限默认 10），不做条件判定（上限/步进可配置，见 [7.1.10](#s71)）。
- 第 1 次完成轮回后解锁第 1 档；第 N 次轮回对应第 N 档（封顶值可配置）。

魔王成长（保留）：
- 基础属性 ×(1 + 轮回倍率 × 轮回数)
- 默认倍率 25%/轮回，可配置（不限制；内部仅做安全修正）
- 硬上限：最高 +200%（约 8 轮回达到）

#### 2.3.4 技术实现方式（WorldBox traits 系统）

**实现要点**
- 遗产以 `ActorTrait/KingdomTrait` 表达：王国遗产挂 `KingdomTrait`；魔王/将领/军团遗产在生成或补给时挂 `ActorTrait`。
- 工艺/军备遗产只解锁装备池：走原版“制作/领取”流程或生成时配装，不做全员强行配装。
- 研究遗产以 trait 形式加入出生/成长随机池，沿用原版概率逻辑。
- 军事遗产在命中/受击钩子里判定，对魔王阵营单位生效（阵营统一 trait 标记）。
- 相关 API 与资产注册口径见「[第五部分：技术可行性与实现方案](#p5)」。

---

### 2.4 敌方军势（波次 / 将领 / 军团）
<a id="s24"></a>

**本节结构**
- 2.4.1 军团波次系统
- 2.4.2 将领系统（精英单位）
- 2.4.3 军团系统（单位库）

#### 2.4.1 军团波次系统
<a id="s241"></a>

##### 2.4.1.1 核心概念

魔王降临后，每隔固定时间（通常 10 年）自动生成一波敌军。随着波次增加，单位数量和强度逐步提升，给王国压力梯度。

**生成口径（自定义生成）**：
- 军团单位使用自定义模板直接生成（实现口径见 5.3）
- 不通过“随机挑选现存生物并改造”为军团的方式生成
- 生成地点：默认在魔王据点附近成团生成并集结；若据点不存在则在魔王附近或世界边缘可行走地块生成，再向最近王国推进
- 若开启“遗迹战场热点”，波次会优先将集结目标指向“未占领/被敌占领”的遗迹，保证战斗集中（见 [7.1.11](#s71)）

##### 2.4.1.2 波次生成规则

| 波次 | 间隔 | 数量 | 单位等级 | 单位类型 | 备注 |
|------|------|------|--------|---------|------|
| 1-3 波 | 10 年/波 | 30 个 | 基础等级 | 先锋型（移速快、防低） | 试探性进攻 |
| 4-6 波 | 10 年/波 | 35 个 | 等级+1 | 混合型（均衡） | 主要压力 |
| 7-9 波 | 10 年/波 | 40 个 | 等级+2 | 精锐型（高伤害） | 精锐属性 |
| 10+波 | 10 年/波 | 50 个 | 等级+3 | Boss 级（高伤害） | 终极威胁 |

**强度递增公式**：
```
单波强度 = 基础强度 × (1 + 15% × 波次)
同时存在上限：默认 200（可配置；超过则停止生成，性能保护）
```

**波次构成比例（可配置）**：
- 每波内部由“先锋/主力/精锐/终极”按比例抽取（默认比例见 [7.1.3](#s71)）
- 可按“波次区间”覆盖比例（例如 1-3 波不出终极），以保证节奏递进

##### 2.4.1.3 军团单位分类

每个魔王 4 种军团单位：

- **先锋** - 虚无/瘟疫等
- **主力** - 存在蚀刻者/瘟疫兽等
- **精锐** - 虚空裂解者/变异领主等
- **Boss** - 反物质泰坦/瘟疫泰坦等（终极）

##### 2.4.1.4 可配置参数

**默认值统一入口**：见 [7.1.3 军团与波次](#s71)

**口径补充**：
- `军团_强度递增` 允许负值，但最终强度会做安全修正
- `军团_同时上限` 不限制输入，但极端值会明显影响性能
- 通用校验与容错规则见「[附录A](#conventions)」

---

#### 2.4.2 将领系统（详细）
<a id="s242"></a>

> 将领是“魔王侧的英雄单位”，用于把战局从“数量压制”变成“关键目标对抗”。  
> 统一槽位：每个将领 **P0 + S1**；技能触发与性能约束遵循「[统一口径](#conventions)」。

##### 2.4.2.1 通用规则（必须一致）

- **数量**：每个魔王固定 5 名将领（先锋/坦克/输出/辅助/精英）。
- **激活**：降临阶段开始激活；将领使用独立 `ActorAsset` 模板生成（不改造现存单位）；激活数量与轮回成长参考 `3.2 将领成长`（上限 5 个同时激活）。
- **生成地点**：优先在魔王据点或魔王附近的可行走地块生成；不足时在据点周边有限半径内补点；避开水域/山脉与封印遗迹范围。
- **模板/法力/频率/触发/持续/限频口径**：统一见「[统一口径](#conventions)」与「[7.1.7](#s71)」，本节不再重复。
- **安全上限**：任何“范围枚举/召唤/地形改造”都必须有**单次触发上限**与**同时存在上限**（避免卡顿）。
- **平衡约束**（将领 < 魔王）：将领不做“全图效果/世界级事件/直接处决/真正免疫/永久控制”；控制类效果默认不超过 1 年（可被净化/驱散）

##### 2.4.2.2 将领名册与技能表（移至附录）

> 完整将领表已统一整理到「附录F」。
> 入口：见 [附录F：将领名册与技能表](#appendix-general-skills)。

#### 2.4.3 军团系统（单位库）
<a id="s243"></a>

> `2.4` 已定义波次与生成规则；本节补齐“每个魔王 4 类军团单位（先锋/主力/精锐/终极）”的可实现口径：单位模板与分层规则。  
> 军团单位不配置技能，保持“自动生成小兵”的直观体验与性能稳定。

##### 2.4.3.1 军团通用规则（必须一致）

- **单位分层**：先锋（骚扰）/主力（均衡）/精锐（高属性）/终极（Boss）。
- **技能规则**：军团单位无技能（不配置 P0/S1），仅靠基础属性与模板区分。
- **同时上限**：受 `2.4` 的“军团_同时上限”约束；超过则停止生成。
- **模板口径**：见「[统一口径](#conventions)」。
- **死亡替换**：军团单位不做“永久变形”，需要“转化/同化”时用“替换生成（spawn + remove）”实现。
- **平衡约束**（军团 << 魔王）：军团不做“全图效果/复活/召唤自复制/直接处决/长期强控”。

##### 2.4.3.2 各魔王军团单位名册（4 类）

| 魔王领域 | 先锋 | 主力 | 精锐 | 终极 |
|------|------|------|------|------|
| 虚无 | 虚空行者 | 存在蚀刻者 | 虚空裂解者 | 反物质泰坦 |
| 瘟疫 | 感染者 | 瘟疫兽 | 变异领主 | 瘟疫泰坦 |
| 机械 | 机械兵 | 战斗机器人 | 歼灭者 | 机械泰坦 |
| 时空 | 时间碎片 | 时空行者 | 命运编织者 | 永恒泰坦 |
| 炎魔 | 火元素 | 熔岩兽 | 地狱火焰 | 炎魔泰坦 |
| 深渊 | 邪教徒 | 深渊行者 | 触手怪 | 旧日支配者 |
| 死亡 | 骷髅兵 | 亡灵骑士 | 死灵法师 | 骨龙 |
| 灵魂 | 傀儡 | 契约者 | 灵魂猎手 | 傀儡大师 |
| 自然 | 狼 | 熊 | 巨兽 | 世界树守护者 |
| 终焉 | 低阶天使 | 中阶天使 | 高阶天使 | 炽天使 |

### 2.5 王国与英雄系统
<a id="s25"></a>

本节所有阈值/权重/倍率默认值统一见 [7.1.4](#s71)。

#### 2.5.1 抗魔等级（反馈机制）

王国通过与魔王战斗，逐步提升抗魔等级（上限可配置），形成“越战越强”的正反馈。  
本设计采用**生产级口径**：不修改底层伤害公式，而是通过 **KingdomTrait/ActorTrait 的特点属性加成**实现“越打越能打”。

**提升规则（默认）**：
```
累计击杀一定数量“魔王阵营单位”（本 MOD 生成） → 抗魔等级 +1
完成 1 次封印战结算（胜利）              → 抗魔等级获得额外加成
```

**效果（关键档位）**：
- Lv0：无加成
- Lv1：命中 +5%，攻速 +5%（更容易打中、更快出手）
- Lv2：伤害 +5%，生命 +5%（更能打也更能扛）
- Lv3：体力上限 +10%，法力上限 +10%（更能持久作战/放技能）
- Lv4：防御 +8%，移速 +5%（更稳、更灵活）
- Lv5：暴击率 +5%，暴击伤害 +15%（更容易打出爆发）
- Lv6：伤害 +8%，防御 +6%（正面硬碰硬更占优）
- Lv7：攻击范围 +10%，投掷/远程射程 +10%（远程单位收益更明显）
- Lv8：生命 +10%，移速 +5%（生存与机动再提高）
- Lv9：攻速 +8%，暴击率 +3%（出手更快、爆发更频繁）
- Lv10：战斗熟练度 +20%，法术熟练度 +20%，状态触发概率 +10%（控制与特效更常触发）

**实现要点（可直接实现）**：
- 抗魔等级是“王国维度”数据：用 `KingdomTrait` 表现（显示/持久化），并在单位生成/出生时继承其王国 trait 的特点属性加成。
- 加成只作用于王国侧单位的“特点属性”，不改敌方生成与底层伤害公式，保持稳定可控。
- 默认只用“单位/英雄可用”字段（见附录G），隐藏字段不做玩家可调项。

#### 2.5.2 命定英雄

本节只讲规则与口径。

**产生规则（晋升制，默认）**：
- 每个王国在每个轮回内有“命定英雄上限”。
- **触发方式**：事件触发为主 + **每 5 年兜底检测**（仅做条件检查，不强制晋升）。
- 当王国满足繁荣/危机阈值（人口、城市、战损等，**二选一/任一满足即可**；**统计范围=该王国自身**）时，系统从该王国现存单位中挑选 1 名候选并“晋升”为命定英雄：
  - 繁荣/危机阈值默认见 [7.1.4](#s71)（与 2.1.2 的繁荣度阈值**可独立调整**，需要时可手动对齐）
  - 优先：高等级/高击杀/寿命时间长/基础属性高的单位
  - 若没有合格候选：允许直接生成 1 名“命定英雄模板单位”（可选）

**评分规则（可配置）**：
- **候选池过滤**：同王国、已成年、存活、非魔王/将领/军团、未被标记为英雄。
- **评分指标**：等级、击杀、寿命（或最大寿命）、最大生命、攻击、领导力（声望/指挥/组织）。
- **归一化**：将各指标归一化到 0-1。
- **加权求和**：按权重得到综合分。
- **晋升选择**：默认从前 N 名中随机 1 名，避免总是同一个人。
- **并列处理**：优先“击杀高 / 等级高”，仍相同则在前 N 名中随机。
- **兼容性**：若某项数据缺失，则忽略该项并把权重平均分给其他项，避免“没数据就被扣分”。

**简单算例（默认阈值）**：
- 公式：`综合分 = Σ( min(指标/阈值, 1) × 权重 )`
- 例：某单位等级8/击杀30/寿命120/生命800/攻击70/领导力60  
  - 归一化：0.8/0.6/0.6/0.8/0.7/0.6  
  - 加权后综合分约 **0.68**（用于排序）

**命定英雄自带**：
- `trait_era_destined`：基础属性加成
- “史诗表现”：独立头像/名字前后缀、UI 高亮与日志记录（不依赖行为树接管）
- “战场爆发”：当命定英雄在“魔王影响区”（距离魔王或其据点一定半径）内发生**命中/受击**时，获得 10 年 `trait_era_heroic_surge`（小幅攻防/移速提升，可刷新）
- 额外特质/技能按配置抽取

**命定英雄强化参数（可配置）**：
- 基础倍率：`damage/armor/health/speed/attack_speed/critical_chance/critical_damage_multiplier`
- 额外加成：`max_mana_bonus`
- 触发时机：仅在“晋升为命定英雄”时一次性写入（不做周期刷新）

**随机特质池（示例，可配置）**：
- 抽取数量：`hero_random_trait_count`
- 规则：英雄专用池是额外加成；互斥冲突则跳过并补抽；原版出生/成长随机特质照常保留（不冲突）
- 示例池（trait ID → 效果口径）：
  - `trait_era_hero_vigor`：最大生命（health_max） +20%
  - `trait_era_hero_guardian`：防御（armor） +20%
  - `trait_era_hero_slayer`：对魔王阵营伤害（damage） +15%
  - `trait_era_hero_swift`：移速（speed） +15%
  - `trait_era_hero_sharp`：暴击率（critical_chance） +10%
  - `trait_era_hero_manaheart`：最大法力 +20%

**随机技能池（示例，可配置）**：
- 抽取数量：`hero_random_skill_count`
- 规则：只从“英雄技能池”抽取；技能以 trait 方式挂载；技能 trait 不进入随机特质池
- 示例池（skill trait ID → 简述）：
  - `trait_era_hero_skill_smite`：命中时小概率追加一次雷击
  - `trait_era_hero_skill_guard`：受击时小概率获得短暂减伤
  - `trait_era_hero_skill_charge`：命中时小概率短距冲锋并追加伤害
  - `trait_era_hero_skill_heal`：命中/受击时小概率恢复少量生命
  - `trait_era_hero_skill_blink`：受击时小概率瞬移脱离

**家族继承**：
- 英雄死亡后，后代有一定概率继承部分能力
- 可形成"英雄家族"，延续故事线

**实现要点（可直接实现）**：
- 晋升 = 直接对目标 `Actor.addTrait(...)`（必要时清理互斥 trait），并写入 MOD 内部“英雄档案”（用于 UI/叙事/继承）。
- “魔王影响区”不做“每年扫描”：在命定英雄自己的 `action_attack_target` / `action_get_hit` 里，用 `pTile` 与“魔王/据点中心 tile”做距离判定；命中/受击且在范围内就添加/刷新 `trait_era_heroic_surge`。

#### 2.5.3 世界停战与共同讨伐（生产级核心）

> 目标：魔王存在期间，世界内部战争暂停，所有王国把“魔王阵营”视为共同敌人；封印后恢复各自外交与冲突，保证后续仍有剧情发展。

**触发条件（魔王存活且任一满足）**：
- 魔王进入「降临」阶段（可开关，见 [7.1.5](#s71)）；或
- 魔王摧毁世界城市比例超过阈值（默认值见 [7.1.5](#s71)）；或
- 世界人口跌破阈值且魔王仍存活（阈值见 [7.1.5](#s71)，避免内耗导致灭绝）

**停战规则（直接可实现）**：
- 触发时立刻执行一次“王国间停战”：遍历 `World.world.wars.getActiveWars()`，对**不包含魔王阵营**的战争调用 `World.world.wars.endWar(war)`。
- 停战持续到本轮回结束（魔王被再封印/世界灭绝/终焉结算）。
- 停战期间按固定间隔重复执行一次“王国间停战”（默认值见 [7.1.5](#s71)），用于抑制新战争自然生成（这是机制定义的一部分，不是“兜底”）。

**共同讨伐规则（不建立世界联盟）**：
- 所有王国获得 `kingdom_trait_era_truce`（示例：士气/基础攻防小幅提升），但不会合并为同一联盟。
- “魔王阵营”统一使用本 MOD 生成的独立阵营（独立 `Kingdom`），仅用于管理战争关系；**不建城、不发展、不参与外交**。
- 并与所有王国保持战争关系（共同敌人）。
- **敌对范围（明确）**：除魔王阵营之外，**所有单位与生物都视为敌人**（王国单位/英雄/中立生物/野兽等一律可攻击），符合“毁灭世界”的设定。
- 讨伐关系的落地方式：
  1) 仅首次触发时创建魔王阵营：用 `World.world.kingdoms.makeNewCivKingdom(founderActor, "era_demon", pLog:false)` 生成 `demonKingdom`（founderActor 可用 MOD 生成的“魔王本体”，或一个隐藏的占位单位）。  
  2) 在进入停战时，遍历当前所有王国：对每个王国调用 `World.world.wars.newWar(kingdom, demonKingdom, WarTypeLibrary.normal)` 建立“对魔王阵营的战争”。  
  3) 为避免重复建战：建战前用 `DiplomacyHelpers.isThereFightBetween(kingdom, demonKingdom)` 判定是否已在交战。
  4) **目标选择**：魔王阵营单位在选敌时，以“非魔王阵营单位”为目标（包含中立/野兽/王国/英雄）。
- UI 共享情报：由 MOD UI 直接显示“魔王/将领/据点/波次热点”标记（不依赖原生联盟/情报系统）。

**封印后恢复**：
- 魔王被再封印后，移除 `kingdom_trait_era_truce`；停止周期性停战调用；世界恢复正常外交与战争生成。

#### 2.5.4 多魔王互动模式（内战/联盟/议会）

> 目标：当同一轮回里出现 2 个及以上魔王时，明确“魔王之间”的关系规则，让玩家看到“内战”或“联合对抗世界”的大事件。

**触发条件（任一满足）**：
- 本轮苏醒数量 ≥ 2
- 当前存活魔王数量 ≥ 2

**模式（可切换/可配置）**：
- **魔王联盟**（`alliance`）：强制结盟、共享敌对关系、相互支援；统一把所有非魔王阵营当敌人。**为防难度爆炸，联盟模式下单体强度会下调**。
- **魔王内战**（`civilwar`）：魔王互相宣战，同时也攻击所有非魔王阵营（三方混战）。
- **随机判断**（`random`）：按“宿敌/相性表”或权重随机决定关系。

**魔王联盟规则**：
- 魔王之间的战争必须被立即终止，并阻止再次开战。
- 每个魔王阵营都与所有王国阵营保持战争关系。
- 为避免难度爆炸，**联盟模式下单个魔王强度小幅下调**（对应 [7.1.2](#s71) 的联盟强度系数）。

**魔王内战规则**：
- 魔王阵营两两宣战，同时对王国宣战。
- 参与魔王上限见 [7.1.2](#s71)，超过则随机抽取对战组合。
- 可选“主导权”机制：击败其他魔王可获得短期加成与称号。

**实现要点（可直接实现）**：
- 关键前提：每个魔王必须有独立 `Kingdom`（与 2.5.3 一致）。
- 宣战/停战：`World.world.wars.newWar(...)` / `World.world.wars.endWar(...)`。
- 防重复：`DiplomacyHelpers.isThereFightBetween(k1, k2)`。
- 联盟关系：`World.world.alliances.forceAlliance(k1, k2)` 或 `World.world.alliances.newAlliance(...)`。
- 关系维护：按固定间隔做“关系校验”（默认值见 [7.1.2](#s71)），按模式修正关系，避免 AI 自发改变。

**可配置参数**：
- 默认值见「[7.1.2 魔王与多魔王](#s71)」。
- 字段清单：`multi_demon_mode`、`multi_demon_relation_check_years`、`demon_alliance_strength_mult`、`demon_civilwar_strength_mult`、`demon_civilwar_max_demons`。

**保护机制**：
- 魔王数量 < 2 时，本模式不生效。
- 任一魔王被再封印/死亡后，立即更新关系与联盟。

---

#### 2.5.5 英雄技能系统（可直接实现）

> 目标：英雄“像英雄”，但不依赖接管行为树。技能由 MOD 在**命中/受击钩子**里判定触发（仅对少量英雄单位），通过现成 API/ActionLibrary 直接施放。  
> 触发/资源/频率等统一口径见「[附录A：统一口径与实现约定](#conventions)」，本节仅补充英雄特有规则。

**技能槽位口径（默认）**：
- 命定英雄：`P0 + S1`（P0 固定；S1 从“英雄技能池”随机抽取，数量可配置）
- 普通英雄（可选）：仅 `P0`（可配置为“随机 1 个轻量被动”）
- 随机技能只在“晋升瞬间”确定，不做周期重抽

**触发逻辑（生产级）**：
- 命定英雄在**命中/受击**时做判定：若满足触发条件且魔力足够，则施放技能并扣除魔力。
- 触发条件采用“位置/周围敌军数量/自身 HP 阈值/是否处于魔王影响区”等可观测条件，不使用任务 ID。

**法力规则（默认，可配置）**：
- 读取：`Actor.getMana()` / `Actor.getMaxMana()`。
- 消耗：优先 `Actor.spendMana(cost)`；或 `Actor.setMana(actor.getMana() - cost)`。
- 命定英雄晋升时可直接补满：`Actor.setMaxMana()`。
- 为了让“命定英雄”能稳定施放：通过 `trait_era_destined` 或专用 trait 提升其最大法力与法力恢复（走 `base_stats_meta` 口径）。

**频率规则**：
- 默认值见 [7.1.7](#s71)。
- 实现建议：在技能 trait 内记录 `last_cast_year` 与 `casts_in_year`，仅在三钩子里判定与更新。

**技能库（示例，默认可配置；仅使用已确认存在的 API）**：
- P0「英勇」：常驻基础属性 +10%，并免疫本 MOD 的“牵引/侵蚀”类效果（通过 trait 标记实现）。
- S1「圣愈」：触发：HP < 40%；效果：恢复 25% 最大生命（health_max）。实现：`spendMana(cost)` + `Actor.restoreHealthPercent(0.25f)`。
- S1「天罚」：触发：附近 1 名魔王阵营目标；效果：雷击 + 短暂硬控（默认时长）。实现：`spendMana` + `ActionLibrary.castLightning(self, target, tile)` + `ActionLibrary.addStunnedEffectOnTarget(self, target)`。
- S1「护盾」：触发：主动施放；效果：为自身施放护盾。实现：`spendMana` + `ActionLibrary.castShieldOnHimself(self, null, tile)`（或等价现成护盾动作）。
- S1「战吼」：触发：主动施放；效果：半径 10 友军获得 10 年攻击 +10%。实现：`spendMana` + 范围添加 buff trait。

**技能与遗产联动（可选）**：
- 抗魔等级/研究遗产可解锁新的英雄技能条目（仍遵循 `P0+S1`，避免复杂度爆炸）。
- 普通单位的研究遗产获取仍走“随机特质池”的概率逻辑，不进入英雄技能池。

## 第三部分：游戏数值与平衡

### 3.1 魔王成长公式

#### 3.1.1 基础强度增长

```
属性值 = 基础值 × (1 + 轮回倍率 × 轮回数)
```

**特点**：
- 线性增长，规则直观
- 每轮回固定比例提升
- 便于玩家与策划预估强度

**默认值**：轮回倍率与硬上限见 [7.1.2](#s71)。

### 3.2 将领成长

**成长口径**：
- 激活数量：起始值 + 每轮回增长，至上限  
- 平均等级：起始值 + 每轮回增长，至上限  
- 平均属性：跟随魔王成长，并叠加生成随机倍率（见 [7.1.9](#s71)）

---

### 3.3 数值检查清单

开发时必须验证：

- [ ] 轮回 1 vs 轮回 5：难度有明显提升但仍可解
- [ ] 轮回 10 vs 轮回 1：难度提升倍数不超过 5 倍（过高导致无解）
- [ ] 高轮回（5+）存在至少 3 种不同的克制策略

<a id="p4"></a>
## 第四部分：用户交互设计（给玩家看的版本）

> 本节是**面向普通玩家的 UI 说明书**：用最少的话告诉玩家“这是什么、怎么玩、能看到什么”。不需要懂代码也能读懂。

### 4.1 设计目标（玩家 3 秒能懂）

- **一眼抓重点**：轮回 / 阶段 / 封印 / 魔王危险度始终可见。
- **先结论后原因**：先告诉“发生了什么”，再解释“为什么”。
- **术语翻译**：每个术语都有一句人话解释（鼠标悬停提示）。
- **少打扰**：只有高危事件弹窗，其余进日志。
- **可搜索**：技能、遗产、魔王、英雄都能直接搜。

### 4.2 结构总览（三层 + 搜索 + 模式）

- **HUD 轻量条**（常驻）：最关键的 5~6 条信息。
- **主面板**（按栏目）：左侧导航 + 中央内容 + 右侧详情。
- **详情抽屉**（点对象即出）：简要 → 详细 → 规则说明。
- **全局搜索**：任何页按 `Ctrl/Cmd + K` 打开搜索。
- **模式切换**：玩家模式 / 高级模式 / 开发模式（开发模式才显示调试与“绝对控制”选项）。

```
┌──── 纪元之轮 HUD ────┐
│ 轮回 3 · 阶段：降临 · 封印 18% ↓ │
│ 魔王：虚无之主（HP 80%）         │
│ 王国：7 · 人口：12.5k · 英雄：8   │
│ 警报：北境城沦陷                 │
│ [打开面板] [暂停] [帮助]          │
└────────────────────────┘

┌─ 导航 ─┐┌────────── 内容区 ──────────┐┌─ 详情 ─┐
│ 总览   ││ 卡片 / 列表 / 时间线 / 说明 ││ 对象卡 │
│ 战局   ││                             ││ 规则提示│
│ 魔王图鉴││                             ││ 快捷操作│
│ 技能库 ││                             ││         │
│ 封印与遗产││                           ││         │
│ 事件与日志││                           ││         │
│ 玩法百科││                             ││         │
│ 设置   │└───────────────────────────┘└─────────┘
```

### 4.3 栏目与导航（玩家能找到“想看的”）

| 栏目 | 玩家能看到什么 | 玩家能做什么 |
|---|---|---|
| **总览** | 轮回、阶段、封印、魔王与王国概况 | 只读为主、少量快捷按钮 |
| **战局** | 魔王/将领/军团/英雄/王国列表与定位 | 筛选、定位、关注 |
| **魔王图鉴** | 10 个魔王的主题与技能摘要 | 查看克制提示 |
| **技能库** | 全部技能的“人话说明” | 搜索、过滤、开关 |
| **封印与遗产** | 封印进度与遗产清单 | 查看规则、选择路线 |
| **事件与日志** | 事件时间线与故事回放 | 过滤、导出 |
| **玩法百科** | 新手指南与核心玩法解释 | 只读学习 |
| **设置** | 参数与预设（分新手/高级） | 调参、导入/导出 |
| **调试**（开发模式） | 运行状态与变量监视 | 仅开发使用 |

> 高级/开发模式可额外显示：**魔王管理 / 王国状态 / AI 控制 / 事件管理 / 轮回历史**（与开发文档一致）。

### 4.4 总览页（第一屏就能明白怎么玩）

**布局：四张卡片 + 玩法核心 + 近期事件**
- **轮回与封印卡**：轮回数、阶段、封印强度、趋势、预计封印战时间。
- **魔王态势卡**：活跃魔王、HP、波次进度、下一次高危时间。
- **王国概览卡**：王国数量、人口、英雄数。
- **遗产进度卡**：本轮遗产档位、下轮继承提示。
- **玩法核心卡（人话）**：一句话告诉玩家“这个 MOD 怎么玩”。
- **近期事件**：最近 6 条大事件（可展开）。

**玩法核心卡示例**
> “这是一个**轮回制史诗玩法**：魔王会多次苏醒，你需要培养王国和英雄，在封印战中击败魔王；每轮结束都会获得永久遗产，让下一轮更强。”

**轮回流程条（总览页下方）**
```
预发展 → 预兆 → 苏醒 → 降临 → 衰弱 → 封印战 → 重建
```

### 4.5 战局页（谁在场上、在哪里、在干嘛）

- **顶部筛选**：阵营 / 类型 / 危险等级 / 当前阶段 / 关键词。
- **主体列表**（可折叠）：魔王 → 将领 → 军团；英雄；王国/城市。
- **封印遗迹面板（战局页内置）**：每个遗迹一行展示状态（未清场/可占领/已占领/被打断）、人数达标提示、遗迹占领条。
- **地图标记**：遗迹有清晰光圈与小进度圈，颜色区分占领状态。
- **视觉变化**：待机微光 → 可占领脉冲 → 已占领光柱 → 被打断红色波纹。
- **争夺提示**：当遗迹被敌方占领/夺回时弹出简短提示，并高亮遗迹 3 秒，提醒玩家战场焦点转移。
- **右侧详情卡（对玩家友好）**：
  - 名称、阵营、当前阶段、位置
  - 关键属性：HP / 攻击 / 防御 / 法力
  - **技能摘要**：触发方式 + 一句话效果
  - **当前状态**：正在围攻 / 撤退 / 休整
  - 快捷操作：定位、关注、标记

### 4.6 魔王图鉴（10 个核心玩法一览）

**图鉴卡内容（每个魔王都统一）**
- 名称 + 主题标签
- 被动 + 6 技能摘要
- 危险点提醒（人话）
- 推荐应对（简短提示）

**十魔王列表**
- 虚无之主（虚空）
- 瘟疫母神（瘟疫）
- 机械暴君（机械）
- 时空扭曲者（时空）
- 混沌炎魔（火焰）
- 深渊邪神（疯狂）
- 死亡君王（亡灵）
- 灵魂编织者（契约）
- 自然之怒（自然）
- 终焉审判者（终焉）

**图鉴示例（玩家版）**
```
虚无之主（虚空）
被动：虚空领域 —— 命中/受击时，附近敌军持续掉血并减速
代表技能：反物质爆发 —— 全盛期大范围爆发
危险提醒：血量越高越危险，避免扎堆
建议应对：分散站位 + 高机动英雄
```

### 4.7 技能库（技能说明要完整、可读）

**技能卡统一字段**
- **名称 + 类型**：被动 / 主动
- **来源**：魔王 / 将领 / 英雄
- **触发方式**：命中 / 受击 / 死亡点
- **消耗与频率**：法力消耗、触发上限
- **范围与持续**：影响范围、持续时间
- **效果描述（人话）**：清楚说明“对谁、做什么、持续多久”
- **限制条件**：阶段限制、目标限制、叠加上限
- **开关说明**：立即生效 / 下轮回生效

**技能详情示例（玩家能看懂）**
```
虚空领域（被动）
来源：魔王
触发：命中 / 受击
范围：半径 12
持续：2 年减速
效果：范围内敌军损失当前生命 5%，并降低移速
限制：无
开关：启用（立即生效）
```

**筛选建议**
- 来源：魔王 / 将领 / 军团 / 英雄
- 类型：被动 / 主动
- 标签：控制 / 输出 / 召唤 / 生存

### 4.8 封印与遗产（长期收益看得清）

**封印区（上半区）**
- 当前封印阶段、封印强度、趋势
- 封印地点与条件（例如“区域内需 X 名英雄”）
- 遗迹总数 / 已占领 / 需占领阈值（例：10 / 6 / 8）
- 总体仪式进度条（只有达成占领阈值才推进）
- 胜利方式：击杀封印 / 仪式封印 / 时间窗口

**遗产区（下半区）**
- 分为 **王国遗产** 与 **魔王遗产**
- 采用“列表 + 详情卡”展示，点开可看完整说明

**遗产一览（玩家版）**
- **王国遗产**：工艺 / 研究 / 军事 / 传承  
  - 例：工艺 = 解锁装备池；研究 = 解锁技能；军事/传承 = 增益英雄与王国
- **魔王遗产**：征服 / 军备 / 魔能 / 领域  
  - 例：征服 = 伤害/防御增强；军备 = 解锁魔王装备；魔能 = 法力增强；领域 = 技能范围增强

**展示方式**
- 每类 10 档位，随轮回提升
- 显示“本轮已解锁 / 下轮将解锁 / 继承规则”

### 4.9 轮回历史与故事（时间线可回放）

- **时间线**：按年份排序，支持关键词/类别过滤
- **叙事模式**：把事件整理成章节（可展开查看原始事件）
- **对比视图**：不同轮回的“关键节点”并排对比
- **导出**：可导出为文本，方便分享故事

### 4.10 玩法百科（新手 3 分钟上手）

**一句话理解**
> 魔王会一轮又一轮地苏醒，你需要培养王国和英雄，在封印战中击败魔王；每轮结束都会获得永久遗产，让下一轮更有优势。

**三分钟上手**
1. 看 HUD：轮回 / 阶段 / 封印是否安全。
2. 打开“总览”：确认王国与英雄状态。
3. 进“魔王图鉴”：了解本轮魔王危险点。
4. 进“技能库”：查核心技能与触发方式。
5. 看“封印与遗产”：规划下轮奖励。

**常见问题**
- Q：为什么魔王突然变强？  
  A：轮回次数提升，会让魔王更强。
- Q：遗产会丢吗？  
  A：不会，遗产会跨轮回继承。

### 4.11 设置与视觉规范（清爽、易读）

**设置（新手/高级双模式）**
- **新手模式**：只显示“安全、常用”的设置
- **高级模式**：显示全部参数与调试项（需确认）

**常用入口**
- 魔王池勾选、苏醒模式、苏醒数量
- 多魔王互动模式（联盟/内战）
- 难度相关参数（轮回倍率等）
- 参数文件与预设（见 4.12）

**视觉规范**
- **风格**：文档式 + 轻史诗感，信息清晰优先。
- **色彩**：青蓝为主、绿色为强调、红色为警报；浅色背景护眼。
- **字体**：标题用中文黑体，正文用易读无衬线（如思源黑体 / HarmonyOS Sans）。
- **版式**：网格对齐；一屏不超过 3 层信息。
- **图标**：统一线性图标，不用表情符号。
- **提示**：重要提示用顶部条；错误提示写清原因与解决办法。
- **交互**：按钮有明确悬停反馈；所有可点击区域边界清晰。

### 4.12 参数文件与预设（导出/导入）

> 目标：让玩家**换电脑/分享配置**时不用重调参数，同时保证导入安全、可回退。

**放置位置（设置页）**
- 在“设置 → 参数与预设”内新增“参数文件”卡片
- 按钮：导出参数 / 导入参数 / 保存为预设 / 恢复默认

**导出（分享/换电脑）**
- 导出当前生效的 UI 参数，生成 JSON 文件
- 文件名建议：`EraWheel_Params_YYYYMMDD_HHMM.json`（自动生成）
- 导出内容不包含敏感信息（如 LLM API Key）

**导入（上传参数文件）**
- 流程：选择文件 → 预览变更 → 应用
- 缺字段用默认；多字段忽略；非法值按附录A修正并提示
- 版本不兼容：尝试迁移；迁移失败则不应用并提示原因

**预设**
- 预设 = 命名的一组参数（常用组合一键切换）
- 支持：保存当前为预设 / 应用预设 / 删除预设
- 预设可一键导出分享（本质仍是参数文件）

**生效时机**
- 导入后立即刷新 UI
- 实际生效遵循每个参数的“立即生效/下轮回生效”规则

---
<a id="p5"></a>
## 第五部分：技术可行性与实现方案

### 5.1 核心 API 确认

本 MOD 依赖的所有 API 已通过 WikiBox 文档或现有 MOD 实现验证：

| API / 功能 | 用途 | 确认状态 | 证据 |
|-----------|------|--------|------|
| `AssetManager.actor_library` | 注册/读取自定义单位模板 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `AssetManager.buildings` | 注册/读取自定义建筑模板 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `AssetManager.traits` | 添加特性 | ✅ 确认 | WikiBox 文档 |
| `ToolBox.LoadSprite()` | 加载自定义图片 | ✅ 确认 | WikiBox 文档 |
| `ActorManager.spawnNewUnit(...)` | 生成魔王/将领/军团（自定义生成） | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `BuildingManager.addBuilding(...)` | 运行时放置自定义建筑 | ✅ 可行（【需反射】internal） | `docs/api/Assembly-CSharp.md`（该重载为 `internal`） |
| `World.world.units/kingdoms/cities/wars` | 获取单位/王国/城市/战争列表（运行态数据入口） | ✅ 确认 | `docs/api/Assembly-CSharp.md`（`MapBox.units/kingdoms/cities/wars` 为 public） |
| `World.world.wars.newWar(...)` / `World.world.wars.endWar(...)` | 创建/结束战争（魔王内战/联盟/讨伐） | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `DiplomacyHelpers.isThereFightBetween(...)` | 防止重复宣战/重复建战 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `World.world.alliances` / `AllianceManager.forceAlliance(...)` / `AllianceManager.newAlliance(...)` | 强制结盟与创建联盟（魔王联盟） | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `Actor.age/level/renown` | 单位年龄/等级/声望等基础信息 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `City.status.population` | 城市人口/兵力等统计 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `Kingdom.countCities/countUnits/countWarriors` | 王国规模/兵力统计 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |
| `MapBox.map_stats` / `SavedMap.mapStats` | 世界统计总览 | ⚠️ 可行（【需反射】/读存档） | `docs/api/Assembly-CSharp.md`（`map_stats` 为 `internal`） |
| `MoveCamera.focusOn(...)` | 镜头定位/跟随 | ⚠️ 可行（【需反射】internal） | `docs/api/Assembly-CSharp.md` |
| 定时器系统 | MOD 轮询 | ✅ 确认 | ModLoader 框架 |
| JSON 文件 IO | 存档、配置 | ✅ 确认 | C# 原生 + WikiBox |
| `WorldTile.setTileType(...)` | 小范围地形改造/污染表现 | ✅ 确认 | `docs/api/Assembly-CSharp.md` |

### 5.2 技术框架

```
┌─ MOD 核心 ──────────────────────┐
│ EraWheelMain.cs                 │
│ ├─ 初始化、注册、更新循环       │
│ └─ UI 管理器                    │
└─────────────────────────────────┘
         ↓
┌─ 轮回系统 ──────────────────────┐
│ CycleManager.cs                 │
│ ├─ 状态机管理                   │
│ ├─ 轮回触发与结算               │
│ ├─ 遗产发放                     │
│ └─ 历史记录                     │
└─────────────────────────────────┘
┌─ 魔王系统 ──────────────────────┐
│ DemonLordManager.cs             │
│ ├─ 魔王初始化                   │
│ ├─ 状态转换                     │
│ ├─ 技能执行                     │
│ ├─ 多魔王关系/联盟/内战调度     │
│ └─ 据点管理                     │
└─────────────────────────────────┘
┌─ 将领军团系统 ──────────────────┐
│ GeneralManager.cs               │
│ UnitManager.cs                  │
│ ├─ 将领生成/激活                │
│ ├─ 军团波次生成                 │
│ ├─ 契约侵蚀/牵引机制            │
│ └─ 战场热点/停战讨伐调度        │
└─────────────────────────────────┘
┌─ 王国英雄系统 ──────────────────┐
│ CivilizationManager.cs          │
│ HeroManager.cs                  │
│ ├─ 遗产应用                     │
│ ├─ 英雄晋升                     │
│ └─ 停战/讨伐管理                │
└─────────────────────────────────┘
┌─ 叙事系统 ──────────────────────┐
│ EventManager.cs                 │
│ AIIntegration.cs                │
│ ├─ 事件池管理                   │
│ ├─ LLM 调用与权限                │
│ ├─ 后备事件池                   │
│ └─ 操作日志                     │
└─────────────────────────────────┘
┌─ UI 系统 ────────────────────────┐
│ PanelController.cs              │
│ ├─ HUD 轻量面板                 │
│ ├─ 总览面板                     │
│ ├─ 战局面板                     │
│ ├─ 技能库面板                   │
│ ├─ 封印与遗产面板               │
│ ├─ 事件与日志面板               │
│ ├─ 设置/预设面板                │
│ ├─ 调试面板（可选）             │
│ └─ 动画和过渡效果               │
└─────────────────────────────────┘
┌─ 配置与存档 ──────────────────────┐
│ ConfigManager.cs                │
│ SaveSystem.cs                   │
│ ├─ JSON 读写                    │
│ ├─ 参数校验                     │
│ ├─ 版本迁移                     │
│ └─ 备份 MOD 数据/配置            │
└─────────────────────────────────┘
```

### 5.3 资产注册与自动生成（实现口径）

本 MOD 采用“自定义生成”为默认策略：魔王/将领/军团/关键建筑均由 MOD 自动生成，避免依赖随机抽取现存单位导致的行为不可控与兼容性问题。

**资产注册（启动时）**：
- 单位：注册魔王/将领/军团等自定义单位模板（`ActorAsset`）到 `AssetManager.actor_library`
- 特性：注册遗产、感染、标记、护盾等自定义 `ActorTrait/KingdomTrait` 到 `AssetManager.traits`（用于 `action_attack_target / action_on_object_remove` 等钩子）
- 建筑：注册封印遗迹/魔王据点等自定义建筑模板（`BuildingAsset`）到 `AssetManager.buildings`

**实体生成（运行时）**：
- 生成单位：通过 `ActorManager.spawnNewUnit(assetId, tile, ...)` 在目标地块直接生成新的 `Actor`
- 生成“封印遗迹/据点建筑”：【需反射】通过反射调用 `World.world.buildings` 的 `internal addBuilding(...)` 直接放置；并用 `WorldTile.setTileType/setTopTileType/removeTrees/removeGrass` 做小范围视觉表现
- 自动选点：根据魔王类型偏好地形与“可行走/空间足够”等条件选取落点；若不满足则在有限半径内进行小范围地形修正后生成遗迹/据点建筑（严格限制频率与次数）

**封印遗迹（仪式封印）**：
- 封印战阶段自动生成“封印遗迹建筑（`Building`）”与仪式区域
- 封印遗迹是本 MOD 新注册的 `BuildingAsset`（可从中立世界建筑克隆再改），不是“选一个现有建筑变成遗迹”
- 建筑生成与清理：封印战期随机点位生成；魔王被再封印后统一移除并清空记录；下一轮回重新生成
- 生成点规则（默认）：优先选择可行走地块，避开深水/拥挤区域；遗迹之间保持最小间距；尽量远离核心城市与王城；若点位不足则在有限半径内做小范围地形修正后补点
- “占领/进度条/打断/回退/完成触发封印”为 MOD 逻辑（范围检测 + 计时累积），不依赖玩家手动放置或游戏内置占领系统

### 5.4 性能优化策略

```
更新频率分层:
├─ 每 5 帧: 军团行为 AI
├─ 每 10 帧: 英雄追踪
├─ 每 30 帧: 王国数据统计
├─ 每 300 帧: AI 叙事检查
└─ 定时 (10 年内): 轮回阶段转换

对象池管理:
└─ 英雄 AI 节点缓存

内存优化:
├─ 事件池使用 ScriptableObject 引用
├─ 轮回历史每 5 轮自动清理旧数据
└─ 日志文件写入磁盘而非内存
```

### 5.5 叙事系统（可实现、可校验）

> 目标：把**真实发生的事件**按时间顺序拼成“像小说一样可读”的叙事；不编造、不乱改、不自相矛盾。  
> 规则：所有叙事内容**必须能追溯到事件来源**；如果无法验证，就不写。

#### 5.5.1 数据来源（直接可读）

- **世界历史/历史日志**（对应“世界历史”面板）：  
  - 读取 `db.DBGetter.getWorldLogMessages()`（历史日志列表）  
  - 关键字段：`WorldLogMessage.timestamp / asset_id / kingdom_id / unit_id / x,y / special1-3`  
  - 可用 `WorldLogMessageExtensions.getFormatedText(...)` 得到游戏内原文（保证事实一致）
- **计划与阴谋**（对应“智慧与生命 → 计划与阴谋”）：  
  - 读取 `World.world.plots`（`PlotManager`）  
  - 关键字段：`Plot.getAsset()/getState()/getProgress()`，`PlotData` 的 `id_*` 目标与发起者
- **战争**（对应“战争”面板）：  
  - 读取 `MapBox.wars` 或 `WarManager.getActiveWars()`  
  - 关键字段：`War.getAllAttackers()/getAllDefenders()`、`WarTypeAsset`
- **MOD 自定义事件**（本 MOD 生成的关键剧情）：  
  - 魔王降临/封印/波次/英雄晋升/命定英雄阵亡等  
  - 统一写入 MOD 自有 `era_narrative_log`（JSON 或内存列表，字段对齐下文事件结构）

#### 5.5.2 统一事件结构（NarrativeEvent）

```
id: 全局唯一
year: 年份（与 Date.getCurrentYear()/WorldLogMessage.timestamp 对齐）
source: world_log / plot / war / mod
type: city_new / king_dead / war_started / plot_started / demon_awake ...
entities: kingdom_id / city_id / unit_id / plot_id / war_id（可为空）
location: x,y（可为空）
raw_text: 游戏内原文或短句摘要（用于无 LLM 模式）
importance: 重要度分（用于章节选材）
```

#### 5.5.3 生成流程（保证逻辑一致）

1) **增量采集**：记录 `last_year/last_timestamp`，只拉取新增事件，避免重复。  
2) **标准化**：把世界日志/计划/战争/MOD 事件统一成 `NarrativeEvent`。  
3) **实体时间线校验**（防逻辑错误）：  
   - 若事件明确“死亡/覆灭/解散”，记入 `dead_at`  
   - 后续事件若引用已死亡对象 → 直接丢弃或标记为“无效事件”  
   - 不知道凶手就不写“谁杀了谁”，只写“死亡/覆灭”  
4) **排序分章**：  
   - 严格按 `year` 排序  
   - 章节可按“阶段（预兆/降临/封印战）”或“固定年份跨度（如 20 年）”分组  
5) **生成叙事**：  
   - 无 LLM：模板拼接（保证不加戏）  
   - 有 LLM：只允许“润色和串联”，不允许改事实（见 5.5.5）

#### 5.5.4 无 LLM 模式（稳定、必能跑）

规则：**只用真实事件原文 + 轻量连接词**。

示例模板（按事件类型）：
- 城市建立：`{year} 年，{city_name} 建立。`  
- 王国建立：`{year} 年，{kingdom_name} 建国。`  
- 国王死亡：`{year} 年，{king_name} 逝世。`  
- 战争开始：`{year} 年，{attacker} 与 {defender} 开战。`  
- 魔王降临：`{year} 年，{demon_name} 降临，{region_name} 陷入动荡。`（MOD 事件）

拼接方式：
- 同年事件用“同年/与此同时/不久之后”  
- 不同年事件用“次年/若干年后/到了 {year} 年”

#### 5.5.5 LLM 模式（更像小说，但受强约束）

**输入（结构化事实）**：
```
events: [
  { id, year, type, raw_text, entities, location }
]
rules: [
  "不得新增人物/地点/时间",
  "不得改变因果与先后",
  "每句必须引用 event_ids"
]
```

**输出（可校验）**：
```
paragraphs: [
  { text: "...", event_ids: [1,2,3] }
]
```

**校验（不通过就回退无 LLM）**：
- 事件 ID 必须存在  
- 叙事中出现的名字必须来自 `events`  
- 顺序必须与 `year` 一致  
- 任何“猜测/幻想”直接剔除

#### 5.5.6 叙事一致性与可读性（强制规则）

- **真实优先**：每句都能回溯到事件；无法验证就不写。  
- **时间有序**：严格按年份排列，不允许“死后又参战”。  
- **角色不乱写**：只有在 `WorldLogMessage` 明确提供凶手/参与者时，才描述“谁杀了谁”。  
- **可追溯**：每章保留“事件来源列表”，点击可跳到原始日志。  
- **稳定退路**：LLM 失败 → 立刻回退到“模板叙事”，保证功能可用。  

### 5.6 可自定义外观/特效清单（已确认）

> 目标：明确“哪些外观/特效可以做成**王国自研/魔王自研**”，不留模糊说法。

**确认可做（全部为 MOD 自定义，不是原版改名）**：
- **单位外观与动画**（魔王/将领/军团/英雄）：自定义 `ActorAsset` + 自定义贴图（`ToolBox.LoadSprite()`）；动画基于**精灵帧**（sprite sheet），可替换为自定义帧序列。
- **装备外观与能力**（王国工艺/魔王军备）：自定义 `ItemAsset` + 自定义贴图 + 自定义数值与特效（通过装备绑定 trait 实现命中/受击效果）。
- **建筑/遗迹外观**：自定义 `BuildingAsset` + 自定义贴图与说明；配合小范围地形变化做表现。封印遗迹支持状态特效（待机/可占领/已占领/被打断），用 `EffectAsset` + `EffectsLibrary.spawn(...)` 实现。
- **技能特效**：可用 `EffectAsset` + `EffectsLibrary.spawn(...)` 生成自定义特效（自带 sprite 与播放参数），并由 trait/ActionLibrary 触发。
- **命名与叙事标记**：装备/技能可写入“制造王国/制作者/研发年份”，用于叙事可追溯。

**明确不做（删除承诺）**：
- **自定义音效**：不做新的音效资源加载，仅复用游戏内已有 `sound_*` 标识。  
- **骨骼/模型级动画替换**：游戏为 2D 精灵帧体系，不引入骨骼/3D 动画替换。

**实现原则（统一口径）**：
- “自研”= 使用 MOD 自定义资产（`ActorAsset/ItemAsset/BuildingAsset/EffectAsset`），不是改原版名字。  
- “特效”= 绑定 trait 与现有动作组合实现，确保可落地。  

---

### 5.7 Trait 编辑器扩展（NPC 参与）

> 目标：让**所有单位**都能像原版一样自动获得特质（出生/成长），编辑器只是额外手段。  
> 口径：遵循 `ActorTrait` 与 `TraitType`（Positive/Negative/Other），百分比与数值分开写。

**实现口径（对齐游戏 trait 结构）**：
- 使用 `ActorTrait`，注册到 `AssetManager.traits`（参考 5.3）。
- 固定值写 `base_stats`，百分比写 `base_stats_meta`（在 UI 中显示为 %）。
- **所有单位都可在特质编辑器手动添加**（含魔王/将领/军团/命定英雄）；自定义单位需确保 `ActorAsset.can_edit_traits=true` 且 `can_receive_traits=true`。
- 随机池**默认对所有单位开启**（含魔王/将领/军团/命定英雄，像原版）；可在配置中对某类单位单独关闭。
- **自动化口径**：表中标记“可随机”的特质，需设 `spawn_random_trait_allowed=true`，并设置 `spawn_random_rate` 与 `rate_birth/rate_acquire_grow_up/rate_inherit`（>0）；这样出生与成长会自动抽取，不需要手动点。
- 若需关闭随机：设 `spawn_random_trait_allowed=false` 且 `rate_birth/rate_acquire_grow_up/rate_inherit=0`。
- 触发/频率/范围/存储口径统一见「[附录A](#conventions)」，本节不重复。

#### 5.7.1 数值/百分比混合特质（20）

<details>
<summary>展开特质表（数值+百分比混合）</summary>

| trait ID | 名称 | 类型 | 效果（数值 + 百分比） | 随机池建议 |
|---|---|---|---|---|
| `trait_era_mix_commander` | 统御号令 | 正 | 指挥 +3；伤害 +30%；生命 +100000 | 可随机（极强） |
| `trait_era_mix_ironhide` | 铁肤 | 正 | 防御 +40%；生命 +8000；移速 -10% | 可随机 |
| `trait_era_mix_berserker` | 狂战血脉 | 其他 | 伤害 +45%；攻速 +20%；防御 -15% | 可随机 |
| `trait_era_mix_swiftblade` | 疾刃 | 其他 | 移速 +35%；攻速 +20%；伤害 +15%；生命 -2000 | 可随机 |
| `trait_era_mix_sniper` | 鹰眼狙击 | 正 | 暴击率 +15%；暴伤 +30%；伤害 +10% | 可随机 |
| `trait_era_mix_bulwark` | 壁垒躯壳 | 正 | 生命 +15000；防御 +30%；移速 -15% | 可随机 |
| `trait_era_mix_stoneheart` | 岩心 | 其他 | 生命 +8000；防御 +20%；暴击率 -5% | 可随机 |
| `trait_era_mix_windrunner` | 风行者 | 正 | 移速 +40%；攻速 +10%；生命 +2000 | 可随机 |
| `trait_era_mix_glasscannon` | 玻璃大炮 | 负 | 伤害 +60%；暴击率 +10%；生命 -4000；防御 -20% | 可随机（极端） |
| `trait_era_mix_brutal` | 蛮力 | 正 | 伤害 +30%；生命 +6000；移速 -10% | 可随机 |
| `trait_era_mix_precise` | 精准 | 正 | 暴击率 +20%；伤害 +10%；攻速 +10% | 可随机 |
| `trait_era_mix_shieldbearer` | 盾持 | 正 | 防御 +50%；生命 +3000；攻速 -10% | 可随机 |
| `trait_era_mix_sprinter` | 短跑健将 | 其他 | 移速 +50%；伤害 +5%；生命 -1500 | 可随机 |
| `trait_era_mix_enduring` | 耐久体质 | 正 | 生命 +9000；防御 +15%；寿命 +40 年 | 可随机 |
| `trait_era_mix_keeneye` | 锐视 | 正 | 暴击率 +12%；暴伤 +35%；移速 +5% | 可随机 |
| `trait_era_mix_mystic` | 奥术亲和 | 其他 | 法力上限 +80；伤害 +10% | 可随机 |
| `trait_era_mix_guardian` | 守护者 | 其他 | 防御 +25%；生命 +5000；伤害 -10% | 可随机 |
| `trait_era_mix_predator` | 猎食者 | 其他 | 伤害 +25%；移速 +20%；暴击率 +5%；防御 -10% | 可随机 |
| `trait_era_mix_bloodcore` | 血核 | 正 | 生命 +12000；伤害 +15%；攻速 -5% | 可随机 |
| `trait_era_mix_warmaster` | 战帅 | 正 | 指挥 +2；伤害 +20%；防御 +10%；生命 +3000 | 可随机 |

</details>

#### 5.7.2 特别技能特质（20）

<details>
<summary>展开特质表（技能/特效）</summary>

| trait ID | 名称 | 类型 | 触发 | 效果（含小特效） | 备注 |
|---|---|---|---|---|---|
| `trait_era_skill_thunder_mark` | 雷痕 | 正 | 命中时 15% | 目标落雷 + 短暂眩晕 | `castLightning` |
| `trait_era_skill_ember_touch` | 余烬之触 | 正 | 命中时 20% | 5 年“余烬标记”，标记目标受击额外 -1% 当前生命（最多 4 次） | 火花特效 |
| `trait_era_skill_frostbite` | 霜咬 | 正 | 命中时 20% | 目标减速 -25% 持续 5 年 | 冰霜特效 |
| `trait_era_skill_shockwave` | 震波 | 其他 | 受击时 15% | 半径 3 敌军短暂眩晕 | 冲击波特效 |
| `trait_era_skill_blink_step` | 闪步 | 正 | 受击且 HP < 40% 时 20% | 随机瞬移 6 格内 + 3 年移速 +40% | `teleportRandom` |
| `trait_era_skill_blood_surge` | 血涌 | 正 | 击杀时 | 恢复 6% 当前生命 | 血雾特效 |
| `trait_era_skill_arcane_bolt` | 奥术弹 | 正 | 命中时 10% | 追加一次伤害 | 奥术光效 |
| `trait_era_skill_smokescreen` | 烟幕 | 其他 | 受击时 10% | 攻击者短暂混乱 + 自身 3 年移速 +20% | 烟雾特效 |
| `trait_era_skill_poison_edge` | 毒刃 | 正 | 命中时 20% | 目标中毒 5 年 | 绿色特效 |
| `trait_era_skill_heal_wave` | 疗愈波 | 正 | 受击时 10% | 半径 5 友军恢复 3% 当前生命 | 治疗光效 |
| `trait_era_skill_chain_spark` | 连锁电花 | 正 | 命中时 10% | 额外电击附近 2 名敌军（小伤害） | 细电弧 |
| `trait_era_skill_rally_shout` | 战吼 | 正 | 命中时 15% | 半径 6 友军攻击 +10% 持续 5 年 | 声波特效 |
| `trait_era_skill_blood_curse` | 血咒 | 其他 | 命中时 15% | 8 年“血咒”：受击 25% 概率额外 -1% 当前生命（最多 4 次） | 红纹特效 |
| `trait_era_skill_stone_skin` | 石肤 | 正 | 受击时 10% | 自身获得 3 年减伤 20% 护盾 | 石屑特效 |
| `trait_era_skill_earth_spike` | 地刺 | 其他 | 命中时 12% | 目标脚下刺击：小范围固定伤害 + 3 年减速 -15% | 土刺特效 |
| `trait_era_skill_wind_cut` | 风切 | 其他 | 命中时 20% | 小击退 + 5 年减速 -20% | 风刃特效 |
| `trait_era_skill_soul_echo` | 魂爆 | 其他 | 死亡时 | 半径 6 敌军损失 8% 当前生命 | 灵魂特效 |
| `trait_era_skill_meteor_spark` | 陨火 | 其他 | 命中时 5% | 目标点落火球（小范围固定伤害） | 火球特效 |
| `trait_era_skill_fear_gaze` | 惊惧凝视 | 其他 | 命中英雄时 10% | 英雄短暂恐惧/混乱 | 视线特效 |
| `trait_era_skill_last_stand` | 背水一战 | 正 | 受击且 HP < 25% | 5 年攻防 +30%、移速 +20%（每 10 年最多 1 次） | 怒气特效 |

</details>

#### 5.7.3 其他方面特质（20）

<details>
<summary>展开特质表（生态/抗性/行为）</summary>

| trait ID | 名称 | 类型 | 主要影响 | 备注 |
|---|---|---|---|---|
| `trait_era_misc_longlife` | 长寿 | 正 | 寿命 +80 年 | 被动 |
| `trait_era_misc_shortlife` | 短命 | 负 | 寿命 -40 年 | 被动 |
| `trait_era_misc_nightwalker` | 夜行者 | 其他 | 夜晚移速 +25% | `era_active_night` |
| `trait_era_misc_moonhunter` | 月影猎人 | 其他 | 夜晚暴击率 +10% | `era_active_moon` |
| `trait_era_misc_swimmer` | 逐水者 | 正 | 水域移速 +30% | 仅水地形 |
| `trait_era_misc_climber` | 山地适应 | 正 | 山地/丘陵移速 +20% | 仅高地 |
| `trait_era_misc_pack_bond` | 群居纽带 | 正 | 周围友军 ≥3 时防御 +15% | 半径 6 |
| `trait_era_misc_loner` | 独行胆气 | 其他 | 周围无友军时伤害 +20%，防御 -10% | 半径 6 |
| `trait_era_misc_fearless` | 无畏 | 正 | 混乱/恐惧持续 -50% | 仅对 MOD 控制 |
| `trait_era_misc_timid` | 胆怯 | 负 | HP < 40% 时 30% 概率逃离并移速 +30%（3 年） | 受击触发 |
| `trait_era_misc_plague_resist` | 抗疫体质 | 正 | 中毒/感染持续 -50% | 对 MOD 中毒/感染 |
| `trait_era_misc_fire_resist` | 耐火 | 正 | 火焰伤害 -50% | 对 MOD 火焰伤害 |
| `trait_era_misc_storm_resist` | 防雷 | 正 | 雷击伤害 -40% | 对 MOD 雷击 |
| `trait_era_misc_cold_resist` | 耐寒 | 正 | 减速/冰冻持续 -40% | 对 MOD 控制 |
| `trait_era_misc_toxic_blood` | 毒血 | 其他 | 近战攻击者中毒 3 年 | 受击触发 |
| `trait_era_misc_guard_aura` | 守护气场 | 正 | 受击时半径 6 友军获得 3 年减伤 10% | 受击触发 |
| `trait_era_misc_scavenger` | 食腐者 | 其他 | 命中目标加“短标记”，目标死亡时自身恢复 2% 当前生命 | 死亡点触发 |
| `trait_era_misc_wanderer` | 流浪本能 | 其他 | 受击后 3 年内移速 +20% | 受击触发 |
| `trait_era_misc_morale` | 士气鼓舞 | 正 | 命中/受击时半径 6 友军移速 +10%（3 年） | 小范围刷新 |
| `trait_era_misc_fragile` | 脆弱 | 负 | 受到伤害 +20% | 被动 |

</details>

---

<a id="p6"></a>
## 第六部分：版本规划与交付

本节将“最终目标设计”拆成可交付、可验收、可回退的阶段，避免实现时反复跳转与口径不一致。

### 6.1 版本目标与范围（口径）

**本文档定位**：目标态设计（最终希望达到的系统集合），实现允许分阶段落地，但每个阶段必须形成可运行的闭环。

**范围内（计划实现）**：
- 轮回系统与阶段推进（含结算与历史）
- 魔王/将领/军团的自定义生成与波次
- 难度与边界保护
- 纪元遗产（跨轮回持久化）
- 王国/英雄/联盟相关机制（最小可用 → 逐步完善）
- 事件池（无 LLM 可运行；有 LLM 为增强）
- 控制面板与配置导入/导出
- 存档/配置的版本迁移与回退

**范围外（暂不做承诺）**：
- 对游戏底层战斗 AI 的“全量可控/可脚本化”
- 大规模、持续性、无上限地形改造（受性能与兼容性约束）

### 6.2 里程碑建议（交付闭环）

| 里程碑 | 目标 | 核心交付 | 最小验收标准 |
|---|---|---|---|
| M0 原型（Prototype） | 跑起来、能观察 | 资产注册 + 生成 1 个魔王 + 基础 UI 状态展示 | 新存档可启动；无报错；可看到魔王与阶段状态 |
| M1 闭环（Core Loop） | 完成 1 次轮回 | 2.1 轮回推进 + 2.4 波次生成 + 封印战胜利结算 + 2.3 遗产发放 | 同一存档完成至少 1 次完整轮回；轮回计数正确；配置错误可回退 |
| M2 内容（Content） | 玩法可重复 | 10 魔王框架落地（技能可先占位）+ 将领/军团名册结构化 + 事件池扩充 | 同一存档完成至少 3 次轮回；不同魔王差异可感知 |
| M3 稳定（Stabilization） | 可长期运行 | 性能分层更新 + 存档/配置迁移 + UI 可用性打磨 | 同一存档完成至少 5 次轮回；无明显性能崩溃/卡死 |
| M4 发布（1.0） | 面向玩家发布 | 平衡调整 + 文档补全 + 默认参数定型 | 关键指标达标（见 1.4）；常见错误有提示与自愈 |

### 6.3 验收与回归清单（摘要）

- 轮回推进：阶段顺序正确；阈值/倒计时/强制推进可用且不会跳错状态
- 存档一致性：读档后轮回计数、阶段、魔王/将领/军团状态保持一致
- 配置鲁棒性：见「[附录A](#conventions)」的“配置与容错”
- 性能底线：高单位量场景下更新频率分层生效，避免每帧全量扫描

---

<a id="p7"></a>
## 第七部分：关键参数默认值速查

<a id="s71"></a>
### 7.1 关键参数默认值速查（UI 可调）

说明：本节汇总“可在 UI 直接改”的参数。技能/遗产/装备等大表的数值也可改，默认值以对应表为准。  
参数校验/容错规则统一见「[附录A](#conventions)」，本节不重复。

#### 7.1.1 轮回与阶段

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| `世界现存人口阈值` | 5000 | 世界人口达到这个值就能进入“预兆”（第 1 轮回用，人口/城市满足一个即可） |
| `世界现存城市阈值` | 20 | 世界城市数达到这个值也能进入“预兆”（与人口阈值二选一） |
| `预发展_检查间隔` | 10 年 | 多久检查一次人口/城市阈值，越短越快进入预兆 |
| `预兆_持续年数` | 30 | 预兆阶段持续时间，越长魔王越晚苏醒 |
| `预兆_事件间隔` | 3-5 年 | 预兆事件出现的间隔范围（在范围内随机） |
| `苏醒_持续年数` | 50 | 苏醒阶段持续时间（魔王已现身但未全面进攻） |
| `苏醒_初始强度比例` | 30% | 苏醒阶段初始强度 = 满强度 × 该比例 |
| `降临_持续年数_最短` | 100 | 降临阶段至少持续这么久 |
| `降临_持续年数_最长` | 200 | 降临阶段最多持续这么久，到期转入衰弱 |
| `衰弱_持续年数` | 20 | 衰弱阶段持续时间，越短越快进入封印战 |
| `封印战_持续年数` | 50 | 封印战可进行的时间窗口 |
| `封印战_回退_HP阈值` | 70% | 魔王回血到这个比例就退出封印战，回到降临 |
| `封印_初始强度` | 100% | 封印起始强度，影响推进节奏 |
| `封印_衰减速率` | 1%/年 | 封印每年降低多少 |
| `封印强度_苏醒阈值` | 20% | 封印强度低于此值进入“苏醒” |
| `封印强度_降临阈值` | 10% | 封印强度低于此值进入“降临” |

#### 7.1.2 魔王与多魔王

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 魔王启用默认 | 全部启用 | 默认勾选哪些魔王；未勾选的不会出现 |
| 魔王苏醒模式 | random（随机） | 指定=只出现勾选的；随机=从启用池随机抽 |
| 苏醒数量 | 1 | 仅随机模式生效：本轮抽出几个魔王 |
| 全盛期 HP 阈值 | 70% | 血量高于此值进入全盛 |
| 衰弱期 HP 阈值 | 30% | 血量低于此值进入衰弱 |
| 全盛期法力恢复倍率 | +50% | 全盛期回蓝加成，越高技能越频繁 |
| 衰弱期将领数量倍率 | 0.5 | 衰弱期将领数量 × 该倍数 |
| 轮回成长倍率 | 25%/轮回 | 每完成一轮回，魔王基础属性增加的比例 |
| 魔王成长硬上限 | +200% | 魔王成长的封顶比例（相对基础值） |
| 魔王阵营寿命（lifespan） | 99999 年 | 魔王阵营单位寿命，数值越大越不容易老死 |
| 多魔王互动模式 | random（随机） | 多魔王之间的关系模式：联盟/内战/随机 |
| 多魔王关系校验间隔 | 3 年 | 多久检查一次魔王关系并修正 |
| 魔王联盟强度系数 | 0.85 | 联盟模式下单个魔王最终强度乘数（越小越弱） |
| 魔王内战强度系数 | 1.00 | 内战模式下单个魔王最终强度乘数 |
| 内战最大魔王数 | 5 | 内战参与的魔王数量上限，超出会随机挑 |

#### 7.1.3 军团与波次

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| `军团_生成间隔` | 10 年 | 军团出波间隔 |
| `军团_初始数量` | 30 | 第 1 波军团数量 |
| `军团_强度递增` | 15%/波 | 每波强度递增比例（可为负=越打越弱） |
| `军团_同时上限` | 200 | 场上最多军团数量，超出就停止生成 |
| `军团_全盛加速` | ×2 | 全盛期波次速度倍率（×2=间隔减半） |
| 波次数量模板 | 1-3 波=30 / 4-6 波=35 / 7-9 波=40 / 10+ 波=50 | 不同波段的固定出兵数量 |
| 波次等级增量 | 0 / +1 / +2 / +3 | 不同波段给单位等级的加成 |

**波次构成比例模板（默认，可改）**

| 波次区间 | 先锋 | 主力 | 精锐 | 终极 |
|------|------|------|------|------|
| 1-3 波 | 70% | 30% | 0% | 0% |
| 4-6 波 | 45% | 40% | 15% | 0% |
| 7-9 波 | 30% | 40% | 30% | 0% |
| 10+ 波 | 20% | 40% | 30% | 10% |

#### 7.1.4 王国与英雄

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 抗魔等级范围 | 0-10 | 抗魔等级的最高等级 |
| 抗魔等级_击杀阈值 | 1000 | 击杀魔王阵营单位达到这个数就升 1 级 |
| 抗魔等级_封印加成 | +1.5/次 | 封印胜利额外增加的等级（可累计小数） |
| 抗魔等级关键档位效果 | Lv1 命中+5%、攻速+5%；Lv2 伤害+5%、生命+5%；Lv3 体力+10%、法力+10%；Lv4 防御+8%、移速+5%；Lv5 暴击率+5%、暴伤+15%；Lv6 伤害+8%、防御+6%；Lv7 射程+10%、投掷射程+10%；Lv8 生命+10%、移速+5%；Lv9 攻速+8%、暴击率+3%；Lv10 战斗熟练+20%、法术熟练+20%、状态触发+10% | 各等级加成一览（可改数值） |
| 命定英雄每王国上限 | 1 名 | 每个王国同时最多有几个命定英雄 |
| 命定英雄晋升冷却 | 30 年 | 命定英雄死亡后要等多久才能再次晋升 |
| 命定英雄兜底检测间隔 | 5 年 | 系统多久检查一次晋升条件（不满足也不强制） |
| 命定英雄晋升_繁荣_人口阈值 | 3000 | 繁荣条件之一：人口达到这个值即可（与城市阈值二选一） |
| 命定英雄晋升_繁荣_城市阈值 | 15 | 繁荣条件之一：城市数达到这个值即可（与人口阈值二选一） |
| 命定英雄晋升_危机统计窗口 | 10 年 | 危机判断看最近多少年 |
| 命定英雄晋升_危机_人口跌幅 | 20% | 危机条件之一：人口在窗口内下降比例达到此值（与另外两项任一满足即可） |
| 命定英雄晋升_危机_城市损失数 | 2 | 危机条件之一：窗口内损失城市达到此数 |
| 命定英雄晋升_危机_战损比例 | 30% | 危机条件之一：窗口内战死人数占总人口比例达到此值 |
| 英雄候选最低等级 | 3 | 低于这个等级不会进候选池 |
| 英雄候选最低击杀 | 5 | 击杀少于这个数不会进候选池 |
| 英雄候选最低年龄 | 18 年 | 年龄小于这个值不会被选 |
| 英雄评分权重 | 等级 0.25 / 击杀 0.25 / 寿命 0.15 / 生命上限 0.10 / 攻击 0.10 / 领导力 0.15 | 各评分项的权重，数值越大越看重 |
| 英雄评分归一化阈值 | 等级/10、击杀/50、寿命/200、生命上限/1000、攻击/100、领导力/100 | 达到这个值记满分（超过也按满分） |
| 英雄评分取前 N 随机 | 3 | 从评分前 N 名里随机选 1 名 |
| 命定英雄基础倍率（damage/armor/health/speed） | +35% | 命定英雄基础属性加成（伤害/防御/生命/移速） |
| 命定英雄基础倍率（attack_speed/critical_*） | +20% | 命定英雄攻速/暴击加成 |
| 命定英雄随机特质数量 | 2 个 | 晋升时额外随机获得的特质数量（来自 MOD 自定义特质池，不是原版自带） |
| 命定英雄随机技能数量 | 1 个 | 晋升时额外随机获得的技能数量（来自 MOD 自定义技能池，不是原版自带） |
| 命定英雄法力上限加成 | +170 | 命定英雄最大法力加成 |
| 魔王阵营法力上限加成 | +0 | 魔王阵营单位最大法力加成 |
| 英雄家族继承率 | 30% | 英雄死亡后，后代继承能力的概率 |

#### 7.1.5 世界停战与共同讨伐

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 停战复查间隔 | 3 年 | 多久清一次王国之间的战争，保证停战持续 |
| 共同讨伐触发_降临阶段开关 | 开启 | 开启后魔王进入“降临”就触发停战（任一条件满足即可） |
| 共同讨伐触发_城市摧毁比例 | 20% | 魔王摧毁城市占比达到这个值触发停战（任一条件满足即可） |
| 共同讨伐触发_人口阈值 | 500 | 世界人口低于这个值触发停战（任一条件满足即可，保底） |

#### 7.1.7 技能/遗产/装备（大表可调项）

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 主动技能法力消耗（固定-普通） | 5 | 普通主动技能一次消耗的法力 |
| 主动技能法力消耗（固定-终极） | 10 | 终极技一次消耗的法力 |
| 主动技能触发上限（普通） | 2 次/年 | 普通技能每年最多触发次数 |
| 终极技触发间隔 | 2 年 | 终极技至少间隔几年才能再触发 |

#### 7.1.8 体验与显示

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 自动收藏：魔王 | 开启 | 魔王生成时是否自动加入收藏夹 |
| 自动收藏：将领 | 开启 | 将领生成时是否自动加入收藏夹 |
| 自动收藏：命定英雄 | 开启 | 命定英雄晋升时是否自动加入收藏夹 |
| 自动收藏：军团 | 关闭 | 军团单位是否自动收藏（开启会刷屏） |

#### 7.1.9 主角单位属性随机范围（生成时一次）

> 目标：让魔王/将领/军团/英雄在“固定模板 + 轮回成长”的基础上，产生可控的个体差异。

- **适用对象**：仅对本 MOD 自定义单位生效（魔王/将领/军团/英雄），不影响随机特质池，也不影响其他 NPC。
- **随机时机**：**每生成一个单位就各自随机一次**（彼此独立，不共享模板）；结果写入单位自定义数据，读档不重抽。
- **作用字段**：`health/armor/damage/speed/attack_speed` 使用**倍率**；`critical_chance` 使用**加成百分比**。
- **叠加顺序**：随机倍率视为“基础属性的一部分”，再叠加轮回成长/特质加成。
- **参数规则**：统一见「[附录A](#conventions)」的“配置与容错”。

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| `随机倍率_魔王_生命` | 2.00~6.00 | 魔王生命倍率随机范围（生成时各自随机一次） |
| `随机倍率_魔王_防御` | 1.80~4.00 | 魔王防御倍率随机范围（生成时各自随机一次） |
| `随机倍率_魔王_伤害` | 2.20~5.00 | 魔王伤害倍率随机范围（生成时各自随机一次） |
| `随机加成_魔王_暴击率` | 12%~35% | 魔王暴击率额外加成范围（生成时各自随机一次） |
| `随机倍率_魔王_移速` | 1.10~1.40 | 魔王移速倍率随机范围（生成时各自随机一次） |
| `随机倍率_魔王_攻速` | 1.10~1.40 | 魔王攻速倍率随机范围（生成时各自随机一次） |
| `随机倍率_将领_生命` | 1.50~3.50 | 将领生命倍率随机范围（生成时各自随机一次） |
| `随机倍率_将领_防御` | 1.30~2.80 | 将领防御倍率随机范围（生成时各自随机一次） |
| `随机倍率_将领_伤害` | 1.60~3.50 | 将领伤害倍率随机范围（生成时各自随机一次） |
| `随机加成_将领_暴击率` | 10%~25% | 将领暴击率额外加成范围（生成时各自随机一次） |
| `随机倍率_将领_移速` | 1.05~1.25 | 将领移速倍率随机范围（生成时各自随机一次） |
| `随机倍率_将领_攻速` | 1.05~1.25 | 将领攻速倍率随机范围（生成时各自随机一次） |
| `随机倍率_军团_生命` | 1.20~2.00 | 军团单位生命倍率随机范围（生成时各自随机一次） |
| `随机倍率_军团_防御` | 1.10~1.80 | 军团单位防御倍率随机范围（生成时各自随机一次） |
| `随机倍率_军团_伤害` | 1.20~2.00 | 军团单位伤害倍率随机范围（生成时各自随机一次） |
| `随机加成_军团_暴击率` | 6%~18% | 军团单位暴击率额外加成范围（生成时各自随机一次） |
| `随机倍率_军团_移速` | 1.00~1.20 | 军团单位移速倍率随机范围（生成时各自随机一次） |
| `随机倍率_军团_攻速` | 1.00~1.20 | 军团单位攻速倍率随机范围（生成时各自随机一次） |
| `随机倍率_英雄_生命` | 1.60~3.20 | 英雄生命倍率随机范围（生成时各自随机一次） |
| `随机倍率_英雄_防御` | 1.30~2.50 | 英雄防御倍率随机范围（生成时各自随机一次） |
| `随机倍率_英雄_伤害` | 1.60~3.00 | 英雄伤害倍率随机范围（生成时各自随机一次） |
| `随机加成_英雄_暴击率` | 10%~25% | 英雄暴击率额外加成范围（生成时各自随机一次） |
| `随机倍率_英雄_移速` | 1.05~1.25 | 英雄移速倍率随机范围（生成时各自随机一次） |
| `随机倍率_英雄_攻速` | 1.05~1.25 | 英雄攻速倍率随机范围（生成时各自随机一次） |

#### 7.1.10 遗产档位与王国掌握度（UI 可调）

**档位推进与下限**

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 遗产档位上限 | 10 | 遗产最高档位，达到后不再提升 |
| 每轮回档位提升 | +1 | 每完成一轮回提升多少档 |
| 新王国档位下限 | 1 | 新建王国最低继承档位 |
| 掌握度刷新间隔 | 10 年 | 新王国多久重新计算一次掌握度 |

**掌握度阈值（分母）**
> 达到阈值记满分，超过不再加分。

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 文化书籍阈值 | 5 | 文化书籍达到这个数就满分（影响新王国开局遗产档位） |
| 宗教书籍阈值 | 5 | 宗教书籍达到这个数就满分（影响新王国开局遗产档位） |
| 语言书籍阈值 | 5 | 语言书籍达到这个数就满分（影响新王国开局遗产档位） |
| 首都书籍阈值 | 5 | 首都书籍达到这个数就满分（影响新王国开局遗产档位） |
| 全城书籍阈值 | 20 | 全城书籍总数达到这个数就满分（影响新王国开局遗产档位） |
| 城市规模阈值 | 5 | 城市数量达到这个数就满分（影响新王国开局遗产档位） |
| 人口规模阈值 | 688 | 总人口达到这个数就满分（影响新王国开局遗产档位） |
| 军力规模阈值 | 200 | 战士数量达到这个数就满分（影响新王国开局遗产档位） |
| 建筑规模阈值 | 6 | 建筑总数达到这个数就满分（影响新王国开局遗产档位） |

**掌握度权重（合计 1.00）**

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 基础掌握度权重 | 0.20 | 基础分在总分中的占比 |
| 文化书籍权重 | 0.10 | 文化书籍分的占比 |
| 宗教书籍权重 | 0.10 | 宗教书籍分的占比 |
| 语言书籍权重 | 0.05 | 语言书籍分的占比 |
| 首都书籍权重 | 0.10 | 首都书籍分的占比 |
| 全城书籍权重 | 0.05 | 全城书籍分的占比 |
| 城市规模权重 | 0.15 | 城市规模分的占比 |
| 人口规模权重 | 0.10 | 人口规模分的占比 |
| 军力规模权重 | 0.10 | 军力规模分的占比 |
| 建筑规模权重 | 0.05 | 建筑规模分的占比 |

#### 7.1.11 封印遗迹与仪式封印（UI 可调）

| 参数 | 默认值（UI可调） | 说明 |
|------|-----|------|
| 封印遗迹数量 | 10 | 封印战生成的遗迹数量 |
| 占领遗迹数量封印阈值 | 8 | 同时占领达到这个数才开始推进仪式 |
| 仪式占领_人数阈值 | 20 | 遗迹范围内友军达到这个人数才算占领 |
| 仪式进度速度 | 10%/年 | 仪式进度每年累积多少 |
| 仪式中断处理 | 暂停 | 打断后如何处理进度（暂停/回退/清零） |
| 仪式回退速度 | 5%/年 | 选择回退时每年倒退多少 |
| 封印胜利模式 | 双通道 | 封印胜利方式：击杀/仪式/双通道（任一达成即胜） |
| 遗迹战场热点 | 开启 | 是否定期派小队去争夺遗迹（让战斗集中） |
| 热点调度间隔 | 5 年 | 派遣小队的间隔 |
| 热点派遣_王国人数 | 30 | 每次从王国派出的单位数量 |
| 热点派遣_魔王人数 | 30 | 每次从魔王阵营派出的单位数量 |
| 热点目标策略 | 优先敌占领 | 优先派往“敌方占领”的遗迹 |
| 遗迹占领收益_半径 | 12 | 占领收益生效的范围半径 |
| 遗迹占领收益_友军增益 | 攻防 +10% | 占领方范围内友军加成 |
| 遗迹占领收益_叠加上限 | 1 | 同阵营占领收益最多叠加层数 |

---

<a id="appendix"></a>
## 附录（大表与口径）

说明：统一口径细则、技能/名册/字段等大表集中在此，便于只查不读。

<a id="conventions"></a>
## 附录A：统一口径与实现约定

（速览入口已前移到《阅读与口径约定》，此处提供完整细则。）

<details>
<summary>展开详细约定（触发/存储/数值/配置/生成/技能资源/排版）</summary>

**A. 时间与触发**
- 时间单位（系统节奏）：轮回/阶段/事件池用游戏内“年份”（对齐 `Date.getCurrentYear()`）。
- 技能部分不做“每年结算/定时器轮询”：系统层面（轮回推进/波次调度）可用低频定时，但技能效果只允许走三种 action 钩子触发（命中/受击/死亡点）。

**B. 事件触发与状态存储**
- 命中触发：攻击者挂自定义 `ActorTrait`，用 `ActorTrait.action_attack_target`（`AttackAction`）拿到 `pSelf/pTarget/pTile`。
- 受击触发：受击者挂 trait/status，用 `action_get_hit`（`GetHitAction`）拿到 `pSelf/pAttackedBy/pTile`。
- 死亡点触发：命中时给目标挂短时标记 trait → 标记 trait 的 `action_on_object_remove`（`WorldActionTrait`）在目标死亡/移除时生成效果。
- 实现手段：优先用 `ActionLibrary.*`（眩晕/传送/雷击等）；没有现成动作时再用自定义 trait + `custom_data_*` 存状态。
- 层数/持续/限频存储：优先写 `custom_data_int/custom_data_float`（如 `era_contract_stacks` / `era_contract_expire_year` / `era_proc_count`）。到期清理与限频检查只在三钩子里做（不额外开“每年扫描名单/定时器表”）。
- 范围效果（允许但要写清楚）：只能在钩子触发时，以 `pTile` 为中心枚举有限半径对象（如半径 8/12）；禁止全图扫描/维护感染名单/每年遍历全体单位。

**C. 数值口径（UI 与配置统一）**
- 百分比：`0%-100%` 或 `+15%`。
- 伤害：百分比伤害统一按 `当前生命（health_current）` 计算，默认“最低扣到 1HP”（除非另有说明）。
- 伤害倍率口径：技能里出现“伤害 ×N / 伤害 +X%”时，默认以基础伤害 `damage` 为基准（可配置）。
- 基础属性字段：`damage`=伤害，`damage_range`=伤害浮动，`armor`=防御，`health`=最大生命，`speed`=移速，`attack_speed`=攻速，`critical_chance`=暴击率，`critical_damage_multiplier`=暴击倍率，`lifespan`=寿命（年），`multiplier_lifespan`=寿命倍率。
- HP 百分比口径：`health_current_pct`=当前生命百分比，`health_max_pct`=最大生命百分比。技能描述中的“当前生命/最大生命”默认对应这两个字段。

**D. 配置与容错**
- 技能参数可配置：伤害、百分比、持续、半径、次数、概率、上限、法力消耗、频率等默认进入配置与 UI，可随时调整。
- 可配置口径：标记为“默认值/可配置”的参数进入配置文件与控制面板；非法值（空/NaN/≤0 间隔/超大值）需做安全修正并提示（不崩溃）。
- 配置文件导入/导出：JSON 格式；必须包含 `config_version`；缺字段用默认、未知字段忽略；非法值按本节规则修正；版本不兼容时尝试迁移，失败则回退并提示；导出时不包含敏感字段（如 API Key）。
- 时间/数量类：小于 0 的输入按 0 处理。
- 间隔类：`<= 0` 自动修正为最小正值（避免除零/死循环）。
- 概率类：按 `0%-100%` 归一（超过 100% 视为 100%；小于 0 按 0 处理）。
- 任意数值：若出现 `NaN/Infinity` 自动回退为安全值。
- 范围类（min/max）：若 `min > max` 自动互换。
- 倍率类：`<= 0` 修正为最小正值。
- 暴击率：`critical_chance` 最终按 `0%-100%` 归一。
- 数值上限：默认不强制上限，但极端值可能影响平衡与性能。

**E. 生成与挂载**
- 技能槽位口径：魔王为 `P0 + S1-S6`；将领为 `P0 + S1`；军团无技能。除特别声明外，控制类效果应短时且有上限。
- 生成口径：魔王/将领/军团使用独立 `ActorAsset` 模板自定义生成（可从原生模板 clone 后改 `id/sprite/数值`）；不直接改写原生资产，不通过“现场改造现存单位”的方式生成。
- 模板基底：选用“原生可见法力条”的基底（避免 `force_hide_mana`），以便技能直接使用游戏内置 `Actor.mana`。
- 技能挂载口径（防误扩散）：技能 trait 不加入随机特质池/出生池，仅在生成魔王/将领或晋升命定英雄时由代码手动 `addTrait(...)` 挂载；军团不挂技能 trait。
- 随机特质口径：魔王/将领/军团/命定英雄与原版单位一致，生成后按 `rate_birth/rate_acquire_grow_up/rate_inherit` 自动获得特质；默认开启，可配置关闭。
- 寿命口径（魔王阵营）：魔王/将领/军团默认“不老死”，在 `ActorAsset.base_stats` 上将 `lifespan` 设为超大值（或 `multiplier_lifespan` 设为超高倍率）。

**F. 技能资源与消耗**
- 主动技能不使用传统冷却条，统一用法力作为资源，并有触发频率上限。
- 默认消耗与频率参数见「[7.1.7](#s71)」，此处不重复；可按技能单独覆盖。

**G. 收藏与排版**
- 收藏标记口径（便于定位）：生成魔王/将领/命定英雄时可自动加入收藏夹（配置开关）；默认不收藏普通军团。实现：`ActorAsset.can_be_favorited=true` + `Actor.switchFavorite()`。单位死亡后会被销毁，不需要额外“手动取消收藏”。

</details>
<a id="appendix-demon-skills"></a>
## 附录E：魔王技能表（P0+S1-S6，原 2.2.4）

> 建议把“机制”直接落成技能表，方便实现与调参。  
> 统一规则：每个魔王 **1 个被动（P0）+ 6 个技能（S1-S6）**；数值可配置；触发与存储口径见「[统一口径](#conventions)」。  
> 主动技能消耗与频率统一见「[统一口径](#conventions)」与「[7.1.7](#s71)」，本节不再重复。

**实现方式通用约定（本表默认）**
- 触发/存储/清理/限频/范围等遵循「[附录A](#conventions)」
- 实现方式栏只写该技能的特有动作与关键字段，不再重复“非全图/非定时器/到期清理”等口径
- 持续/次数/标记默认写入 `custom_data_*` 并在三钩子里推进/清理

**魔王索引**： [1 虚无](#demon-1-void) · [2 瘟疫](#demon-2-plague) · [3 机械](#demon-3-mech) · [4 时空](#demon-4-time) · [5 火焰](#demon-5-fire) · [6 深渊](#demon-6-abyss) · [7 亡灵](#demon-7-death) · [8 契约](#demon-8-soul) · [9 自然](#demon-9-nature) · [10 终焉](#demon-10-judge)

<a id="demon-1-void"></a>
##### 魔王1：虚无之主（虚空）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 虚空领域 | 战斗光环 | 常驻；命中/受击触发 | 当虚无之主**命中或受击**时：以当前 `pTile` 为中心半径 `R=12`，对敌对单位结算一次“虚空侵蚀”：损失 `5% 当前生命（health_current）`（最低扣到 `1HP`），并附加 2 年“虚空迟滞”（移速 -20%）。 | 给虚无之主挂 `trait_era_void_aura`：在 `action_attack_target` 与 `action_get_hit` 中调用同一逻辑；用 `pTile` 半径枚举单位并 `changeHealth(-dmg)`；迟滞=给目标加 `trait_era_void_slow` 并刷新 `custom_data_int["era_void_slow_expire_year"]`。 |
| S1 | 存在抹除 | 死亡点触发（标记） | 无消耗；目标死亡触发 | 虚无之主命中目标时：给目标附加 5 年“残响标记”。带标记的目标死亡时：在死亡点爆发一次“残响冲击”（半径 6）：敌对单位损失 `20% 当前生命（health_current）`（最低扣到 `1HP`），并附加 2 年“迟滞”（移速 -20%）。 | 命中：在虚无之主 trait 的 `action_attack_target` 给目标 `addTrait("trait_era_void_echo_mark")` 并写入到期年。死亡：在 `trait_era_void_echo_mark.action_on_object_remove` 里用目标 tile 枚举半径 6 单位并处理（扣血+迟滞）。 |
| S2 | 世界收缩 | 累计触发 | 每累计击杀 `K=100` 触发 | 每当虚无之主累计击杀达到 `K=100`：在世界边缘随机选 `N=8` 个地块“虚空化”（危险地形/贫瘠地形），并对附近单位附加 2 年减速。`N` 随阶段提升（苏醒 6 / 降临 8 / 全盛 12）。 | “击杀计数”严格走三钩子：命中时给目标写 `custom_data_int["era_last_hit_attacker"] = self.id` 并挂短时 `trait_era_last_hit_mark`；在该标记的 `action_on_object_remove` 若确认 `last_hit_attacker==虚无之主` 则给虚无之主 `custom_data_int["era_void_kill_count"]++`；达到 `K` 后触发一次地形改造并清零。 |
| S3 | 相位突袭 | 主动（命中施放） | 命中英雄时尝试 | 当虚无之主**命中英雄**且法力足够时：瞬移到该英雄附近 3 格内，追加一次 `200% 基础伤害（damage）` 的打击；并给目标附加 10 年“破碎”（受伤 +25%）。 | 在 `action_attack_target` 中：若 `pTarget` 为英雄则 `spendMana(cost)` → `singularityTeleportation(...)` → 直接扣血；“破碎”用 `trait_era_void_shatter` + `custom_data_int["era_void_shatter_expire_year"]`。 |
| S4 | 虚空裂隙 | 主动（命中施放） | 命中时尝试 | 当虚无之主命中目标且法力足够时：以 `pTarget` 的 `pTile` 为中心半径 10 触发一次“裂隙撕扯”：敌对单位损失 `20% 当前生命（health_current）`，并有 `15%` 概率被随机传送到附近荒地。 | 在 `action_attack_target` 中：`spendMana(cost)` 后以 `pTarget` 的 tile 枚举半径 10 单位；扣血 `changeHealth`；传送用 `ActionLibrary.teleportRandom(self, target, tile)`。 |
| S5 | 虚无洪流 | 主动（直线） | 命中时尝试 | 当虚无之主命中目标且法力足够时：沿“自身→目标”方向取 30 格直线，对路径附近半径 3 的敌对单位造成 `35% 当前生命（health_current）` 伤害；并附加 3 年“压制标记”：带标记单位**每次受击**都会额外触发一次 眩晕（默认时长）（最多触发 6 次）。 | `action_attack_target`：`spendMana` 后取 tile 串并半径枚举；给命中的单位加 `trait_era_void_suppress`，在该 trait 的 `action_get_hit` 里做“剩余触发次数”递减并调用 `addStunnedEffectOnTarget(...)`。 |
| S6 | 反物质爆发 | 终极技 | 仅全盛可用 | 以自身为中心半径 18：单位受到 `50% 当前生命（health_current）` 伤害（对英雄减半）；建筑受到高额伤害（优先打到“废墟阈值”）；并清空区域内 30% 地块植被。 | 单位用 `changeHealth`；建筑用 `Building.getHit(...)` 口径可替换为 `changeHealth(-dmg)`（Building 继承 BaseSimObject）；植被用 `WorldTile.removeTrees/removeGrass`。 |

</details>

<a id="demon-2-plague"></a>
##### 魔王2：瘟疫母神（瘟疫）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 瘟疫传播 | 被动 | 常驻；命中触发 | 普攻命中时：给目标叠加 1 层“感染”（最多 5 层）；感染目标在**后续每次受击**时会额外损失 `2% 当前生命（health_current） × 层数`，并在感染存在期间降低攻击/移速。 | 给瘟疫母神挂 `ActorTrait`：`action_attack_target` 命中时给目标加 `trait_era_infection` 并写入 `custom_data_int["era_infection_stacks"]`（上限 5）与 `custom_data_int["era_infection_expire_year"]`。感染的额外伤害在 `trait_era_infection.action_get_hit` 里结算。 |
| S1 | 感染转化 | 条件触发（感染者） | 感染满层后触发 | 单位感染层数达到 5 后：在其接下来累计受击 `H=20` 次时触发一次“转化判定”：`60%` 概率转化为瘟疫仆从（替换模板）；失败则进入“重病期”（接下来 10 次受击额外掉血）。 | 在 `trait_era_infection.action_get_hit` 中维护 `custom_data_int["era_infection_hits_taken"]` 与 `custom_data_int["era_infection_sick_hits_left"]`；满足阈值时执行 `metamorphInto(...)` 或 spawn+remove。 |
| S2 | 毒雾弥漫 | 死亡点触发（感染者） | 无消耗；目标死亡触发 | 任意感染单位死亡：在死亡点触发一次“毒雾冲击”（半径 8）：敌对单位损失 `12% 当前生命（health_current）`（最低扣到 `1HP`），并附加 5 年中毒效果。 | 用 `trait_era_infection.action_on_object_remove`：拿到目标 tile 后枚举半径 8 敌对单位，扣血 + `addPoisonedEffectOnTarget(...)`（或中毒 trait + 到期年）。 |
| S3 | 基因突变 | 随机触发（感染者） | 感染者受击时判定 | 感染者每次受击有 `10%` 概率触发一次突变：获得随机强化（攻击/防御/移速 +20%）或随机弱化（-20%），持续到其后续 `M=20` 次受击结束（最多叠 2 层）。 | 在 `trait_era_infection.action_get_hit` 里做概率判定；把突变层数与剩余受击次数写入 `custom_data_int`，并用 buff/debuff trait 表现（只靠受击事件推进与清理）。 |
| S4 | 疫病领主 | 累计触发 | 每转化 `T=50` 单位触发 | 每累计成功转化 50 个仆从：在母神据点生成 1 个“疫病领主”（精英单位），并立即带队向最近城市进军。 | `ActorManager.spawnNewUnit("plague_lord", tile, ...)`；用 `Actor.goTo(...)` 引导其移动到城市周边。 |
| S5 | 疫潮爆发 | 主动（命中施放） | 命中时尝试 | 当母神命中目标且法力足够时：以目标 tile 半径 20 内敌对单位立刻获得 1 层感染，并附加“疫潮标记”（持续 20 年）：带标记单位在其后续 **3 次受击**时，每次额外 +1 层感染；感染满层时每次受击有 `25%` 概率触发转化。 | 在母神 `action_attack_target`：`spendMana` 后以 `pTarget` tile 枚举半径 20 单位并加 `trait_era_infection` + `trait_era_plague_tide_mark`；在 `trait_era_plague_tide_mark.action_get_hit` 里做“剩余触发次数”递减并加层/判定转化。 |
| S6 | 黑疫降临 | 终极技（命中施放） | 仅全盛可用；命中时尝试 | 当母神命中目标且法力足够时：以母神当前位置半径 30 触发一次“黑疫浪潮”：敌对单位获得 50 年“虚弱”（攻击/防御 -15%），并立刻在该范围内随机 5 个点各触发一次“毒雾冲击”（同 S2 的爆发效果）。 | 逻辑放在母神 `action_attack_target`：`spendMana` 后枚举半径 30 敌对单位加 debuff trait；随机点用 tile 抽样并复用 S2 的爆发扣血/中毒。 |

</details>

<a id="demon-3-mech"></a>
##### 魔王3：机械暴君（机械）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 反魔法立场 | 战斗光环 | 常驻；命中/受击触发 | 当机械暴君**命中或受击**时：以当前 `pTile` 半径 `R=14`，对“有法力/施法倾向”的敌对单位刷新 3 年“干扰”（法力回复 -50%，攻击 -15%，且本 MOD 的“主动技能触发概率”降低）。 | 给机械暴君挂 `trait_era_mech_antimagic`：在 `action_attack_target`/`action_get_hit` 中以 `pTile` 半径枚举单位；判定“有 mana/法师 trait/职业”后给目标加 `trait_era_mech_jam` 并刷新 `custom_data_int["era_mech_jam_expire_year"]`。 |
| S1 | 纳米修复 | 受击触发（自愈） | 常驻；受击触发 | 当机械暴君受击时：立刻恢复 `5% 最大生命（health_max）`；若站在机械据点范围内则改为 `8%`。 | 在机械暴君的 `action_get_hit` 中：根据位置是否在据点半径内选择比例，然后 `restoreHealthPercent(...)`。 |
| S2 | 逻辑病毒 | 主动（命中施放） | 命中时尝试 | 当机械暴君命中目标且法力足够时：以目标 `pTile` 半径 12 触发一次“系统冲击”：随机打击最多 3 个建筑（若范围内存在），并使范围内敌军有 `20%` 概率眩晕（默认时长）。 | 在 `action_attack_target`：`spendMana(cost)` 后，建筑=在半径内抽样 `Building` 并 `changeHealth(-dmg)`；单位=枚举半径内敌对单位，概率调用 `addStunnedEffectOnTarget(...)`。 |
| S3 | 同化协议 | 主动（命中施放） | 命中时尝试 | 当机械暴君命中目标且法力足够时：以目标 `pTile` 半径 10，随机选择最多 5 个敌对单位进行“同化”（替换为机械仆从模板/变形为机械种）；英雄同化概率减半（默认只施加 debuff）。 | 在 `action_attack_target`：`spendMana` 后范围枚举 + 抽样；非英雄 `metamorphInto(...)` 或 spawn+remove；英雄默认给 `trait_era_mech_assimilated`（攻击/移速 -20% 等）而不强制替换。 |
| S4 | 轨道电弧 | 主动（点杀/清场） | 主动施放 | 对目标 tile 连续释放 3 次闪电：每次对半径 4 的单位造成固定伤害（例如 80/120/160，随轮回成长）。 | `ActionLibrary.castLightning(demonActor, null, tile)` 连发；若要稳定伤害，用 `changeHealth` 作为补偿。 |
| S5 | 生产线 | 主动（召唤） | 主动施放 | 在据点周围生成 20 个机械兵 + 2 个机械精英；优先补足军团上限。 | `ActorManager.spawnNewUnit(...)` 批量生成；生成数量受“同时存在上限”限制。 |
| S6 | 天网觉醒 | 终极技（命中施放） | 仅全盛可用；命中时尝试；持续 50 年 | 当机械暴君命中目标且法力足够时：以自身 `pTile` 半径 `R=35`（可配置）内机械单位获得 50 年“超载”（攻击/防御/移速 +30%）；并获得“战斗自修复”：机械单位**命中或受击**时额外恢复 `3% 最大生命（health_max）`。同时立即生成 1 波“天网军团”（规模=当前轮回×系数）。 | 在 `action_attack_target` 中枚举半径 `R` 内机械单位并加 `trait_era_mech_overload` 与 `trait_era_mech_repair_on_combat`（修复逻辑放在其 `action_attack_target`/`action_get_hit`）；军团生成复用 2.4 波次系统。 |

</details>

<a id="demon-4-time"></a>
##### 魔王4：时空扭曲者（时空）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 命运窥视 | 被动 | 常驻 | 获得“预知”：闪避率 +30%（用等效属性实现：受到伤害时有概率无效/减半）；并使控制持续时间 -50%（仅对 MOD 施加的控制生效）。 | 受击用 `action_get_hit`（`GetHitAction`）做概率“无效/减半”；控制缩短用 trait 标记，在 MOD 施加控制时按该标记缩短到期年（写入 `custom_data_int`）。 |
| S1 | 时间禁锢 | 主动（命中施放） | 命中时尝试；持续 10 年 | 当时空扭曲者命中英雄/将领且法力足够时：为其附加 10 年“禁锢标记”。带标记单位在其后续每次受击时都会被短暂眩晕（默认时长）（最多触发 8 次），并可被“净化”移除标记。 | 在 `action_attack_target`：判断目标类型后 `spendMana`，给目标加 `trait_era_time_prison` 并写入到期年与 `custom_data_int["era_time_prison_procs_left"]=8`；在 `trait_era_time_prison.action_get_hit` 里调用 `addStunnedEffectOnTarget(...)` 并递减次数，到期或次数归零即移除。 |
| S2 | 因果锚点 | 受击触发（保命） | 遭遇致命伤时触发 | 时空扭曲者会在战斗中不断“刷新锚点”（记录当前位置与当前 HP）。当其遭遇致命伤且法力足够时：触发一次“因果回避”——瞬移回锚点位置并恢复至锚点 HP（至少 30% 最大生命（health_max）），清除负面控制。 | 锚点只在三钩子里刷新：例如在自身 `action_attack_target`/`action_get_hit` 中把 `custom_data_float["era_time_anchor_hp"]` 与 `custom_data_int["era_time_anchor_x/y"]` 更新为当前值。致死判定在 `action_get_hit`：若 `incomingDamage >= currentHP` 且有锚点且 `spendMana` 成功，则 `singularityTeleportation(anchorTile, self)` + `setHealth(...)`/`restoreHealthPercent(...)` + 移除控制类 trait。 |
| S3 | 时空裂隙 | 主动（命中施放） | 命中时尝试 | 当时空扭曲者命中目标且法力足够时：以目标 `pTile` 半径 12，随机选择最多 10 个敌对单位立刻“错位传送”到附近荒野（距离原地 30-80 格）。 | 在 `action_attack_target`：`spendMana` 后范围枚举+抽样；每个被选中的目标调用 `teleportRandom(self, target, tile)`。 |
| S4 | 时间加速 | 主动（增益） | 主动施放；持续 20 年 | 自身与 2 名最近将领获得“加速”：移速 +40%，攻击速度/攻击力 +20%，并获得“回蓝加速”（法力恢复 +50%）。 | 增益用 trait（含法力恢复加成）。 |
| S5 | 时间悖论 | 主动（命中施放） | 命中时尝试；持续 15 年 | 当时空扭曲者命中目标且法力足够时：以目标 `pTile` 半径 10 触发一次“悖论爆发”：对范围内敌军随机施加一种效果（眩晕（默认时长） / 随机传送 / 固定伤害）。并给被命中的敌军附加 15 年“悖论标记”：其后续每次受击都有 `25%` 概率再次触发一次（最多触发 3 次）。 | 在 `action_attack_target`：`spendMana` 后范围枚举并对每个敌军 roll；标记用 `trait_era_time_paradox_mark`，在该 trait 的 `action_get_hit` 里做概率与“剩余次数”递减。 |
| S6 | 永恒回环 | 终极技（命中施放） | 仅全盛可用；命中时尝试；持续 50 年 | 当时空扭曲者命中目标且法力足够时：以自身 `pTile` 半径 25 给敌军附加 50 年“时间风暴标记”（受击时更易触发短眩晕（默认时长）或随机传送，最多各触发 5 次）；同时获得“回环”：在下一次遭遇致命伤时，可额外触发一次“小型因果回避”（复用 S2 的致死回避，但恢复量减半）。 | 标记= `trait_era_time_storm`（逻辑在其 `action_get_hit`，只靠受击推进次数）；“回环”= 在时空扭曲者自身 `custom_data_int` 写一个“可用次数=1”，在其 `action_get_hit` 的致死分支里优先消耗并执行简化 S2。 |

</details>

<a id="demon-5-fire"></a>
##### 魔王5：混沌炎魔（火焰）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 烈焰灼烧 | 战斗光环 | 常驻；命中/受击触发 | 当炎魔**命中或受击**时：以当前 `pTile` 半径 `R=12`，对敌对单位结算一次：损失 `6% 当前生命（health_current）`（最低扣到 `1HP`），并叠 1 层“灼烧”（最多 5 层；灼烧存在期间该单位在受击时会额外承受 +10% 火焰系追加伤害）。 | 给炎魔挂 `trait_era_fire_aura`：在 `action_attack_target`/`action_get_hit` 中范围枚举并 `changeHealth`；灼烧=给目标加 `trait_era_burn_stack` + 写入 `custom_data_int["era_burn_stacks"]`（上限 5）。追加伤害逻辑放在 `trait_era_burn_stack.action_get_hit`：若攻击者为火焰阵营/带火焰 trait，则额外扣一段 `10%×层数` 的“追加伤害”。 |
| S1 | 地狱化 | 累计触发（地形） | 每累计击杀 `K=30` 触发 | 炎魔累计击杀达到 `K=30` 时：将据点周围半径 8 的随机 20 个地块变为熔岩/焦土，并清除树木与草（每次触发都有改造上限，避免破图）。 | “击杀计数”走“命中标记 → 目标 `action_on_object_remove` 记账”的三钩子口径；触发后做一次性地形改造：`setTileType` + `removeTrees/removeGrass`。 |
| S2 | 爆燃 | 死亡点触发（标记） | 不耗法力 | 炎魔命中目标时：给目标附加 3 年“爆燃标记”。带标记的目标死亡时：在死亡点半径 5 造成爆炸伤害（目标当前生命（health_current） 的 20%），并对附近地块施加“烧焦”。 | 命中：炎魔 trait 的 `action_attack_target` 给目标 `addTrait("trait_era_fire_burst_mark")`。死亡：`trait_era_fire_burst_mark.action_on_object_remove` 里做 AOE 扫描 + `changeHealth`；地形用 `WorldTile.setTileType(...)` 或 `removeTrees/removeGrass` 表现“焦化”。 |
| S3 | 熔岩喷发 | 主动（区域） | 主动施放 | 目标区域半径 10：立即喷发，单位受到固定伤害并被击退/眩晕（默认时长）；同时生成 12 个熔岩地块。 | 伤害 `changeHealth`；眩晕用 `ActionLibrary.addStunnedEffectOnTarget(...)`；地形用 `setTileType`。 |
| S4 | 灼烧烙印 | 主动（命中施放） | 命中英雄时尝试；持续 20 年 | 当炎魔命中英雄且法力足够时：给目标施加 20 年“烙印”。带烙印目标在其后续每次受击时额外损失 `8% 当前生命（health_current）`；若其所在 tile 为熔岩/火焰地形则该次额外伤害翻倍。 | 在炎魔 `action_attack_target`：`spendMana` 后给目标加 `trait_era_fire_brand` 并写到期年；在 `trait_era_fire_brand.action_get_hit` 里做额外扣血（含地形判定）。 |
| S5 | 火焰复苏 | 受击触发（地形条件） | 受击时判定 | 当炎魔受击时：若其所在 tile 为熔岩/焦土，则立刻恢复 `10% 最大生命（health_max）`，并刷新 5 年“火焰护体”（减伤 20%）。 | 在炎魔 `action_get_hit` 里检测 tile type：满足则 `restoreHealthPercent` + 给自身加 `trait_era_fire_guard` 并刷新到期年；减伤逻辑放在 `trait_era_fire_guard.action_get_hit`。 |
| S6 | 末日烈焰 | 终极技（命中施放） | 仅全盛可用；命中时尝试；持续 30 年 | 当炎魔命中目标且法力足够时：以炎魔当前位置半径 30 随机选 8 个点各触发一次“烈焰雨爆发”（对范围内单位/建筑造成一次性伤害）；并给该范围内敌军附加 30 年“热浪”（移速 -10%）。 | 在炎魔 `action_attack_target` 中 `spendMana` 后做 tile 抽样，表现可用 `castFire/throwTorchAtTile`，伤害用 `changeHealth`；热浪用 trait + 到期年。 |

</details>

<a id="demon-6-abyss"></a>
##### 魔王6：深渊邪神（疯狂）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 精神污染 | 战斗光环 | 常驻；命中/受击触发 | 当深渊邪神**命中或受击**时：以当前 `pTile` 半径 `R=16`，对敌对单位结算一次“疯狂侵染”：疯狂值 +10（0-100）。疯狂值越高，单位越容易失控（用后续技能触发表达）。 | 疯狂值写入单位 `custom_data_int["era_madness"]`（0-100）；逻辑放在深渊邪神的 `action_attack_target`/`action_get_hit`：范围枚举后对敌军 `madness = clamp(madness+10)`。 |
| S1 | 疯狂爆发 | 条件触发（标记） | 疯狂 ≥ 100 时触发；持续 10 年 | 单位疯狂值达到 100 时触发一次“失控标记”（持续 10 年）：带标记单位在其后续每次受击时都会短暂 `makeConfused(...)`（最多触发 8 次），并获得“狂乱”（攻击 +30%，防御 -20%）。标记结束时将疯狂值降到 40。 | 触发点放在深渊邪神的 `action_attack_target`：把目标疯狂值加到阈值后，给目标加 `trait_era_madness_burst`（写到期年 + `procs_left`）。在 `trait_era_madness_burst.action_get_hit` 里调用 `makeConfused(...)` 并递减次数；移除时把 `era_madness` 设回 40。 |
| S2 | 邪教渗透 | 主动（命中施放/召唤） | 命中时尝试 | 当深渊邪神命中目标且法力足够时：在目标 `pTile` 周围召唤 8-12 个邪教徒（临时单位），邪教徒会优先传播疯狂值并攻击英雄。 | 在 `action_attack_target`：`spendMana` 后以 `pTile` 为中心找可行走点并 `spawnNewUnit("cultist", tile, ...)`；必要时对邪教徒做一次性 `goTo(...)` 引导靠近最近英雄。 |
| S3 | 深渊凝视 | 主动（单体） | 主动施放 | 指定英雄：立刻 +50 疯狂值，并眩晕（默认时长）；若目标疯狂值 ≥ 80，则额外掉血 `20% 当前生命（health_current）`。 | 疯狂值加成由 MOD；眩晕用 `ActionLibrary.addStunnedEffectOnTarget`；掉血 `changeHealth`。 |
| S4 | 低语诱导 | 主动（命中施放/牵引标记） | 命中英雄时尝试；持续 15 年 | 当深渊邪神命中英雄且法力足够时：为其附加 15 年“低语标记”。带标记英雄在其后续每次受击时会被强制 `goTo(深渊据点附近 tile)` 并短暂 `makeConfused(...)`（最多触发 10 次），稳定把其拖向战场核心。 | 在 `action_attack_target`：`spendMana` 后给目标加 `trait_era_abyss_lure`（到期年+次数）。在该 trait 的 `action_get_hit`：调用 `goTo(...)` + `makeConfused(...)` 并递减次数。 |
| S5 | 深渊触手 | 主动（命中施放/召唤） | 命中时尝试 | 当深渊邪神命中目标且法力足够时：在目标 `pTile` 周围召唤 4 个“深渊触手”（临时单位），优先攻击建筑与密集人群；触手持续到深渊邪神被封印/死亡或触手被击杀。 | 在 `action_attack_target`：`spendMana` 后 `spawnNewUnit("abyss_tentacle", tile, ...)`。清理由深渊邪神自身的 `action_on_object_remove`（死亡/移除时批量移除仍存活的触手）。 |
| S6 | 旧日降临 | 终极技（命中施放） | 仅全盛可用；命中时尝试 | 当深渊邪神命中目标且法力足够时：以自身 `pTile` 半径 35 内所有敌对单位疯狂值 +30；并在附近生成 1 个“旧日化身”（Boss）加入战场，持续到封印战结束或被击杀。 | 在 `action_attack_target` 中 `spendMana` 后枚举半径 35 单位并加疯狂；Boss 用 `spawnNewUnit`，并通过 trait 强化其高血/范围伤害。 |

</details>

<a id="demon-7-death"></a>
##### 魔王7：死亡君王（亡灵）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 亡灵领域 | 战斗光环 | 常驻；命中/受击触发 | 当死亡君王**命中或受击**时：以当前 `pTile` 半径 `R=14`，范围内亡灵单位攻击 +30%（刷新 5 年），敌对单位附加 5 年“凋零”（生命回复 -50%）。 | 给死亡君王挂 `trait_era_death_domain`：在 `action_attack_target`/`action_get_hit` 中范围枚举；对亡灵单位加 buff trait（到期年），对敌军加 `trait_era_death_withered`（到期年）。 |
| S1 | 死亡审判 | 死亡点触发（范围标记） | 不耗法力 | 当死亡君王发生命中/受击时：会“宣判”其周围半径 25 的敌对单位（刷新 2 年标记）。带标记单位死亡时：有 `25%` 概率在原地生成 1 个骷髅仆从；若死者是“命定英雄”（`trait_era_destined`）则概率减半但生成精英骷髅。 | 在死亡君王的 `action_attack_target`/`action_get_hit`：以 `pTile` 半径 25 枚举敌军并加 `trait_era_death_sentence_mark`（写到期年）。在该标记的 `action_on_object_remove`：按概率 `spawnNewUnit("skeleton_thrall"/"skeleton_elite", tile, ...)`。 |
| S2 | 灵魂收割 | 累计触发 | 每击杀 10 单位叠 1 层；最多 20 层 | 每层：死亡君王自身最大生命（health_max） +1%，攻击 +1%（可衰减）；封印后保留 30% 层数作为下一轮起始。 | 计数器 + 给魔王添加 trait（或 MOD 内部倍率）；结算时固化层数。 |
| S3 | 死亡凋零 | 主动（命中施放） | 命中时尝试 | 当死亡君王命中目标且法力足够时：以目标 `pTile` 半径 12，敌对单位立刻损失 `30% 当前生命（health_current）`（英雄减半），并附加 10 年“凋零标记”：带标记单位在其后续每次受击时额外损失 `5% 当前生命（health_current）`。 | 在 `action_attack_target`：`spendMana` 后范围枚举并立刻扣血；给目标加 `trait_era_death_decay`（到期年）。额外伤害逻辑放在 `trait_era_death_decay.action_get_hit`。 |
| S4 | 冥界大门 | 主动（命中施放/召唤波次） | 命中时尝试 | 当死亡君王命中目标且法力足够时：立刻额外生成一波亡灵（规模=当前轮回×系数），从据点或死亡君王附近集结出发。 | 在 `action_attack_target`：`spendMana` 后直接调用 2.4 波次生成（单位模板换亡灵）。 |
| S5 | 死亡印记 | 主动（单体） | 主动施放；持续 20 年 | 给目标英雄施加“死亡印记”：20 年内若目标掉血到 30% 以下，则触发一次“处决”（直接击杀/或强制掉到 1HP 并眩晕（默认时长）），并消耗该印记。 | 施放时给目标添加 `trait_era_death_mark` 并写入到期年；目标受击用 `trait_era_death_mark.action_get_hit` 检查血量阈值，达成则执行 `ActionLibrary.deathMark(target)`（若可用）或 `setHealth(1)` + 眩晕，并移除标记。 |
| S6 | 终末审判 | 终极技（命中施放） | 仅全盛可用；命中时尝试 | 当死亡君王命中目标且法力足够时：以自身 `pTile` 半径 35 触发一次死亡能量爆发：敌对单位获得 50 年“恐惧”（移速 -15%）；并立刻在该范围内 3 个“战场热点”（按密集单位 tile 抽样）生成亡灵大军。 | 在 `action_attack_target` 中 `spendMana` 后枚举半径 35 单位加恐惧 trait；热点=从半径内 tile 抽样；亡灵生成用 `spawnNewUnit`。 |

</details>

<a id="demon-8-soul"></a>
##### 魔王8：灵魂编织者（契约）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 契约烙印 | 被动 | 常驻；命中触发 | 普攻命中英雄时：叠加 1 层“契约”（最多 5 层）。带契约的英雄在其后续每次受击时额外损失 `2% 当前生命（health_current） × 层数`，并在契约存在期间降低抗性。 | 在灵魂编织者 `action_attack_target`：把层数写入英雄 `custom_data_int["era_contract_stacks"]`（上限 5）并刷新到期年；额外掉血逻辑放在英雄身上的 `trait_era_contract.action_get_hit`。 |
| S1 | 侵蚀冲动 | 条件触发（契约者） | 契约≥3 的英雄受击时判定 | 契约层数 ≥3：英雄每次受击有 `5% × 层数` 概率触发“侵蚀冲动”（持续 5 年）：立刻短暂混乱（默认时长），并获得 5 年易伤（受伤 +15%）。 | 在 `trait_era_contract.action_get_hit`：读取 `era_contract_stacks`，按概率触发 `makeConfused(...)` + 给目标加易伤 trait（写到期年）。 |
| S2 | 傀儡牵引 | 主动（命中施放） | 命中英雄时尝试；持续 10 年 | 当灵魂编织者命中英雄且法力足够时：为其附加 10 年“牵引标记”。带标记英雄在其后续每次受击时会被强制 `goTo(魔王据点/魔王本体附近 tile)`，并刷新“攻击 +20% / 防御 -20%”（用于表现其被操控冲锋）。 | 在灵魂编织者 `action_attack_target`：`spendMana` 后给目标加 `trait_era_soul_pull`（到期年+次数）。在 `trait_era_soul_pull.action_get_hit`：调用 `goTo(...)` 并刷新 buff/debuff trait。 |
| S3 | 灵魂吸取 | 击杀计数触发 | 不耗法力 | 灵魂编织者击杀单位时：恢复 `5% 最大生命（health_max）`；若击杀英雄则额外恢复 `15%` 并获得 1 层“灵魂护盾”（减伤 10% 持续 30 年）。 | “击杀检测”不做全局监听：在灵魂编织者的 `action_attack_target`（以及必要时 `action_get_hit`）里对比 `Actor.kills` 与 `custom_data_int["era_last_kills"]` 的增量；若有增量则执行 `restoreHealthPercent` 并叠加护盾 trait（写到期年/上限）。英雄击杀用“命中标记 → 目标 `action_on_object_remove` 回写到施法者”口径更稳定。 |
| S4 | 灵魂链接 | 主动（区域） | 主动施放；持续 30 年 | 选定区域半径 14：随机 3 名英雄被“链接”。链接期间其中任意一个受到伤害，会有 30% 伤害分摊到其余两人。 | 给被链接者挂链接标记 trait，并用链接标记的 `action_get_hit`（`GetHitAction`）做分摊：对另外两人 `changeHealth(-share)`。 |
| S5 | 意志粉碎 | 主动（命中施放） | 命中英雄时尝试 | 当灵魂编织者命中英雄且法力足够时：目标立刻损失 50% 体力/法力，并短暂硬控（默认时长）；随后获得“虚弱”（攻击 -15%，移速 -10%）基础 10 年，且契约层数每层额外 +2 年（上限 20 年）。 | 在 `action_attack_target`：`spendMana` 后对目标 `setStamina/setMana` + `addStunnedEffectOnTarget(...)`；虚弱用 `trait_era_weakened` 并写入到期年。 |
| S6 | 契约终裁 | 终极技（命中施放） | 仅全盛可用；命中时尝试 | 当灵魂编织者命中目标且法力足够时：以自身 `pTile` 半径 35 内所有“已带契约”的英雄：契约层数立即 +2，并触发一次“契约爆发”（大额掉血或短暂失控（默认时长），二选一）。 | 在 `action_attack_target` 中 `spendMana` 后枚举半径 35 单位，筛选 `era_contract_stacks>0` 的英雄并统一处理：层数 +2、扣血 `changeHealth` 或 `makeConfused`，并弹 UI 事件提示。 |

</details>

<a id="demon-9-nature"></a>
##### 魔王9：自然之怒（自然）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 自然亲和 | 被动（地形加成） | 常驻；命中/受击触发刷新 | 当自然之怒**命中或受击**时：检测自身所在 tile。若为森林/草地：自身与附近自然军团攻击/防御 +25%（刷新 5 年）；若为荒地/熔岩：改为 -10%（刷新 5 年）。 | 在自然之怒的 `action_attack_target`/`action_get_hit` 中做 tile type 判定，并给自身/半径内自然军团刷新 buff/debuff trait。 |
| S1 | 植物暴走 | 主动（命中施放/地形） | 命中时尝试 | 当自然之怒命中目标且法力足够时：以目标 `pTile` 半径 18 随机选择 30 个地块转为森林/荆棘顶层，并给半径 18 敌军附加 10 年减速（移速 -15%）。 | 在 `action_attack_target`：`spendMana` 后做 tile 抽样并 `setTileType/setTopTileType`；减速用 trait（写到期年）。 |
| S2 | 巨兽召唤 | 主动（召唤） | 主动施放 | 召唤 1 只自然巨兽（Boss 单位）+ 6 只护卫兽，目标为最近城市。 | `ActorManager.spawnNewUnit(...)`；用 `goTo` 引导。 |
| S3 | 大地震颤 | 主动（建筑打击） | 主动施放 | 目标区域半径 16：建筑有 `50%` 概率受到重创（按当前生命（health_current）比例扣血），并使区域内单位眩晕（默认时长）。 | 对 `city.buildings` 或区域内 building 列表抽样 `changeHealth(-dmg)`；眩晕用 `ActionLibrary.addStunnedEffectOnTarget`。 |
| S4 | 生命缠绕 | 主动（命中施放/范围控制） | 命中时尝试；持续 15 年 | 当自然之怒命中目标且法力足够时：以目标 `pTile` 半径 12 的敌军获得 15 年“缠绕”（移速 -40%，攻击 -15%）。 | 在 `action_attack_target`：`spendMana` 后范围枚举并给敌军加 `trait_era_entangle`。 |
| S5 | 风暴召唤 | 主动（命中施放/灾害） | 命中时尝试 | 当自然之怒命中目标且法力足够时：在目标 `pTile` 触发一次风暴表现（龙卷风/落雷/击退任选其一），并对半径 14 敌军施加 5 年减速（移速 -10%）与短暂眩晕（默认时长，小概率）。 | 表现优先 `ActionLibrary.castTornado(self, null, tile)`（或雷击/击退等）；核心效果只做一次性范围枚举（减速 trait + 概率短控）。 |
| S6 | 盖亚之怒 | 终极技（命中施放） | 仅全盛可用；命中时尝试 | 当自然之怒命中目标且法力足够时：以自身 `pTile` 半径 40 触发一次“自然灾变连锁”：执行 S1/S3/S5 的强化版各 1 次（地形改造+震颤+风暴），但每项都有严格“单次影响上限”。 | 在 `action_attack_target` 里 `spendMana` 后复用 S1/S3/S5 的一次性逻辑（范围与数量上调，但有硬上限）。 |

</details>

<a id="demon-10-judge"></a>
##### 魔王10：终焉审判者（终焉）
<details>
<summary>展开技能表（P0+S1-S6）</summary>

| 槽位 | 技能名 | 类型 | 触发（默认消耗：普通 5 点 / 终极 10 点） | 能力详细描述（默认） | 实现方式（建议） |
|------|--------|------|-------------------|----------------------|------------------|
| P0 | 神罚印记 | 被动 | 常驻；命中触发 | 普攻命中英雄：叠 1 层“审判”（最多 3 层）。每层使其受到的伤害 +10%。 | 用终焉审判者的 `ActorTrait.action_attack_target` 命中时：把层数写入英雄 `custom_data_int["era_judgement_stacks"]`（上限 3），并用 trait 表现易伤。 |
| S1 | 天堂审判 | 条件触发 | 审判层数=3 且法力足够时触发 | 审判满 3 层：触发一次“裁决”——目标立刻损失 `60% 当前生命（health_current）`（若已低于 30% 则处决/击杀），并清空其审判层数。 | 由 MOD 检测层数；伤害 `changeHealth`；处决可用 `ActionLibrary.deathMark(...)`（若适配）或 `setHealth(1)` + 眩晕。 |
| S2 | 末日号角 | 主动（命中施放/范围压制） | 命中时尝试；持续 30 年 | 当终焉审判者命中目标且法力足够时：以自身 `pTile` 半径 40 的王国单位获得 30 年“末日压迫”（攻击 -10%，防御 -10%，移速 -5%）。 | 在 `action_attack_target`：`spendMana` 后范围枚举并给目标加 `trait_era_doom_pressure`（到期年）。 |
| S3 | 圣裁光束 | 主动（点杀） | 主动施放 | 对目标英雄位置降下“圣裁”：连续 3 次雷击，外加 5 年易伤（受伤 +20%）。 | `ActionLibrary.castLightning(...)` + 易伤 trait。 |
| S4 | 终焉波次 | 主动（命中施放/军团强化） | 命中时尝试；持续 30 年 | 当终焉审判者命中目标且法力足够时：立刻追加 1 波“终焉军团”（数量=当前波次×系数），并给终焉军团附加 30 年“终焉增幅”：军团单位在其后续每次命中时追加 +15% 伤害。 | 在 `action_attack_target`：`spendMana` 后调用 2.4 生成；增幅用 `trait_era_judge_legion_amp`（逻辑放在军团单位的 `action_attack_target`：追加扣血），并用到期年控制持续。 |
| S5 | 诸神黄昏 | 条件技能（战斗燃命） | HP < 50% 时生效 | HP < 50%：自身进入“黄昏形态”——攻击/防御 +50%，法力恢复 +30%；代价是每次**命中或受击**都会损失 `2% 当前生命（health_current）`（燃命）。 | 条件检查写在终焉审判者的 `action_attack_target`/`action_get_hit`：满足 HP 条件则刷新 `trait_era_judge_twilight`；燃命=在这两个钩子里按当前生命扣血（`changeHealth(-curHp*0.02)`）。 |
| S6 | 世界终结（末日计量） | 终极技（累计触发） | 仅全盛可用；累计“末日值”触发 | 终焉审判者在全盛期会累积“末日值”：每次命中/受击/处决都会增加。末日值达到阈值（例如 300）时：触发一次“终焉事件”——在自身附近连续执行 3 个灾害爆发（雷暴/瘟疫/地形崩坏各 1 次）并追加 1 次终极波次。 | 末日值写入 `custom_data_int["era_doom_meter"]` 并只在三钩子里递增；达到阈值时清零并触发：灾害爆发全部限定在半径内（范围枚举 + tile 抽样），终极波次复用 2.4。 |

</details>

---

<a id="appendix-general-skills"></a>
## 附录F：将领名册与技能表（P0+S1，原 2.4.2.3）

> 表中“实现抓手”是建议路线：优先 `ActionLibrary.*`，没有现成动作时用自定义 trait + `custom_data_*` 存状态，并在三钩子里推进与清理（见「[统一口径](#conventions)」）。

说明：本节所有 S1 默认消耗 5 点法力，触发频率见「[统一口径](#conventions)」与「[7.1.7](#s71)」，表格不再重复标注。

**实现方式通用约定（本表默认）**
- 触发/存储/清理/限频/范围等遵循「[附录A](#conventions)」
- 实现方式栏只写差异与关键字段，不再重复“非全图/非定时器/到期清理”等口径

**将领索引**： [虚无](#generals-void) · [瘟疫](#generals-plague) · [机械](#generals-mech) · [时空](#generals-time) · [火焰](#generals-fire) · [深渊](#generals-abyss) · [亡灵](#generals-death) · [契约](#generals-soul) · [自然](#generals-nature) · [终焉](#generals-judge)

<a id="generals-void"></a>
### 虚无之主将领（领域：虚空）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_void_dps` | 虚空剑圣·塞拉斯 | 输出 | **虚空剑意**：普攻命中时附加一次“裂伤”（目标 10 年内受到伤害 +15%）；对英雄额外 +10%。实现：给塞拉斯挂 `ActorTrait`，用 `action_attack_target` 命中时给目标 `addTrait("trait_era_void_bleed")` 并刷新剩余年限。 | **相位穿行**：持续 5 年；自身移速 +100%，并在施放时瞬移到最近英雄附近（3 格内）。实现：`ActionLibrary.singularityTeleportation(...)` + 速度 trait。 |
| `general_void_vanguard` | 虚空行者·米拉 | 先锋 | **虚空潜行（相位）**：自身累计命中 `N=5` 次后获得 10 年“相位潜行”（移速 +20%）；相位潜行期间受击时有 10% 概率随机传送脱离（每累计受击 `M=20` 次最多触发 1 次）。实现：命中计数写入 `custom_data_int`；受击用 `action_get_hit` 判定传送，并用 `custom_data_int` 做“受击次数限频”。 | **相位突袭**：瞬移到最近英雄附近（3 格内），首次命中基础伤害（damage）×3，并施加短暂硬控（默认时长）。实现：`singularityTeleportation(...)` + 命中用 `action_attack_target` + `addStunnedEffectOnTarget(...)`。 |
| `general_void_tank` | 虚空守卫·奥古斯 | 坦克 | **存在锚定**：当奥古斯命中/受击时：以当前 `pTile` 半径 10 刷新友军“锚定”（控制时间 -30%，不受“抹除类效果”影响，持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 buff trait（到期年）。 | **虚空护盾**：持续 10 年；获得护盾值 `500 + 50×轮回数`，护盾存在时减伤 30%。实现：自定义护盾数值（MOD 内部）+ 护盾 trait 的 `action_get_hit`（`GetHitAction`）减伤。 |
| `general_void_support` | 虚空祭司·艾莉娅 | 辅助 | **虚空祝福**：当艾莉娅命中/受击时：以当前 `pTile` 半径 12 刷新友军攻击 +15%（持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 buff trait（到期年）。 | **虚空召唤**：在自身周围召唤 3 个“虚空行者”（军团先锋单位），并命令其向最近城市移动。实现：`spawnNewUnit(...)` + `goTo(...)` 引导（受军团上限约束）。 |
| `general_void_elite` | 虚空领主·塞拉菲姆 | 精英 | **虚空领域（弱化版）**：当塞拉菲姆命中/受击时：以当前 `pTile` 半径 10 对敌军结算一次 `3% 当前生命（health_current）` 伤害（最低扣到 1HP）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `changeHealth(-dmg)`。 | **相位打击**：瞬移到目标英雄身边并造成一次“无视护甲”的高伤害（基础伤害（damage）×2.0）。实现：传送 + 直接扣血 `changeHealth(-dmg)`。 |

</details>

<a id="generals-plague"></a>
### 瘟疫母神将领（领域：瘟疫）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_plague_vanguard` | 疫病使者·莫格拉 | 先锋 | **疫源携带**：普攻命中给目标叠 1 层感染（最多 5 层）；感染额外伤害统一走“感染者受击触发”（与瘟疫母神同口径）。实现：`action_attack_target` 命中时写 `custom_data_int["era_infection_stacks"]` 并刷新到期年。 | **感染冲锋**：直线冲锋 20 格，路径上每隔若干格触发一次“毒雾冲击”（半径 4：敌军损失 `6% 当前生命（health_current）` 并中毒 5 年）。实现：路径取样 + 在命中钩子里做多次“半径枚举爆发”（不做毒雾区列表）。 |
| `general_plague_tank` | 腐烂巨兽·格罗尔 | 坦克 | **毒素护甲**：受击时反施加“中毒”（10 年内攻击 -10%）。实现：在自身 `action_get_hit` 对攻击者加中毒 trait（到期年）。 | **腐烂光环**：持续 30 年；持续期内当格罗尔命中/受击时：以当前 `pTile` 半径 12 对敌军结算一次 `10% 当前生命（health_current）` 伤害（最低扣到 1HP）。实现：施放时给自身加 `trait_era_plague_rot_aura`（写到期年），该 trait 在 `action_attack_target/action_get_hit` 做半径枚举扣血。 |
| `general_plague_dps` | 变异领主·泽克斯 | 输出 | **突变体质**：每次命中有 `5%` 概率获得 20 年增益（攻击/防御/移速 +20%），最多叠 2 层。实现：在 `action_attack_target` 里做概率 + 层数写入 `custom_data_int` + buff trait。 | **毒素爆发**：以目标 `pTile` 半径 8 立刻造成固定伤害（默认 100）并给敌军叠感染 2 层。实现：范围扣血 + 写入感染层数（不做区域持续）。 |
| `general_plague_support` | 瘟疫祭司·娜塔莎 | 辅助 | **感染仪式**：当娜塔莎命中/受击时：以当前 `pTile` 半径 12，对感染者把 `custom_data_int["era_infection_hits_taken"]` 额外 +5（上限不超过转化阈值），等效“加速转化”。实现：在钩子里半径枚举感染者并写字段。 | **瘟疫治愈**：治疗周围友军 30% 最大生命（health_max），并移除 1 个负面状态。实现：`restoreHealthPercent(...)` + 移除 trait。 |
| `general_plague_elite` | 疫病之母·玛拉凯 | 精英 | **超级感染**：感染上限提高到 6 层；感染层数 ≥5 的目标在其受击时有 `5%` 概率直接转化为仆从。实现：把规则合并到 `trait_era_infection.action_get_hit` 的判定里（仅对“超级感染来源”生效）。 | **疫病领主召唤**：召唤 2 个“疫病领主”，优先围攻最近大城。实现：批量 `spawnNewUnit(...)` + `goTo(...)`（受军团上限约束）。 |

</details>

<a id="generals-mech"></a>
### 机械暴君将领（领域：机械）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_mech_dps` | 歼灭者·阿尔法 | 输出 | **火控校准**：攻击 +15%，并获得“攻城加压”：当阿尔法普攻命中建筑时，额外造成一次固定伤害（默认 30）。实现：在 `action_attack_target` 里检测 `pTarget` 为建筑并 `changeHealth(-30)`。 | **离子炮**：对目标点半径 4 造成固定伤害（默认 120）并短暂眩晕（默认时长）。实现：范围扣血 + `addStunnedEffectOnTarget(...)`。 |
| `general_mech_vanguard` | 侦察者·贝塔 | 先锋 | **扫描协议**：当贝塔命中/受击时：在半径 20 内选择最近英雄作为“扫描目标”（用于 UI），并刷新 10 年“受伤 +10%”标记。实现：在 `action_attack_target/action_get_hit` 里做局部选目标 + 加 debuff trait。 | **扫描锁定**：持续 20 年；给目标英雄施加“锁定”（受伤 +30%）。实现：命中时施放并写到期年。 |
| `general_mech_tank` | 堡垒·伽马 | 坦克 | **反击协议**：受击时有 15% 概率使攻击者短暂眩晕（默认时长）并减速 5 年（-15%）；为避免刷屏，改为“每累计受击 `H=10` 次最多触发 1 次”。实现：在 `action_get_hit` 里用 `custom_data_int` 做受击计数与触发重置。 | **能量护盾**：持续 12 年；获得护盾值 `600 + 60×轮回数`，护盾期间减伤 20%（不做控制免疫）。实现：护盾数值（MOD 内部）+ 护盾 trait 的 `action_get_hit`（`GetHitAction`）减伤。 |
| `general_mech_support` | 工程师·德尔塔 | 辅助 | **维修协议**：当德尔塔命中/受击时：以当前 `pTile` 半径 12 为机械友军恢复 `3% 最大生命（health_max）`（单次处理上限）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `restoreHealthPercent(...)`。 | **生产线**：召唤 3 个机械兵（军团先锋/主力按比例），并立即集结到自身。实现：批量 `spawnNewUnit(...)` + `goTo(...)`（受军团上限约束）。 |
| `general_mech_elite` | 终结者·欧米茄二号 | 精英 | **歼灭协议**：对建筑伤害（damage）×3；对军团单位伤害 +30%。实现：命中加成。 | **同化光束**：对目标单位施加“同化标记”：目标累计受击 `H=20` 次后转化为机械单位；英雄仅施加虚弱（不强制同化）。实现：命中时加 `trait_era_mech_assimilate_mark`，在其 `action_get_hit` 里计数到阈值后 spawn+remove。 |

</details>

<a id="generals-time"></a>
### 时空扭曲者将领（领域：时空）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_time_vanguard` | 时空游侠·克洛诺 | 先锋 | **时间漂移**：受到致命伤害时有 20% 概率瞬移脱离；为限制频率，改为“每累计受击 `H=80` 次最多触发 1 次”。实现：在自身 `action_get_hit` 里做致命判定 + 受击计数阈值，触发后清零计数并 `teleportRandom(...)`。 | **闪烁**：瞬移到目标附近随机位置，并获得 5 年移速 +50%。实现：传送 + trait。 |
| `general_time_tank` | 时间守护者·埃恩 | 坦克 | **时间护盾（被动触发）**：HP < 30% 受击时自动获得 5 年减伤 60%；为限制频率，改为“每累计受击 `H=100` 次最多触发 1 次”。实现：在 `action_get_hit` 里用受击计数阈值 + 到期年控制。 | **时间静止**：半径 10 敌军获得 1 年“静止标记”：带标记敌军在其后续每次受击时触发眩晕（默认时长），最多触发 3 次。实现：范围加标记 trait，并在标记的 `action_get_hit` 里短控+次数递减。 |
| `general_time_dps` | 命运编织者·莫伊拉 | 输出 | **命运之箭**：对英雄命中必定暴击（用基础伤害（damage）×1.5 近似）。实现：命中加成。 | **因果链**：对 1 个目标造成伤害后，弹射到最近 3 个敌人（每次伤害衰减 20%）。实现：邻近搜索 + 多段扣血。 |
| `general_time_support` | 时空祭司·泰姆拉 | 辅助 | **时间加速光环**：当泰姆拉命中/受击时：以当前 `pTile` 半径 12 为友军额外恢复法力 `5% MaxMana`（不超过 MaxMana，仅对有法力者生效）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `addMana(...)`。 | **时间回溯**：在据点召唤 1 名“时间幻影将领”（属性为本体 50%），持续到泰姆拉被移除/死亡或幻影被击杀。实现：`spawnNewUnit(...)`；清理由泰姆拉的 `action_on_object_remove` 批量移除仍存活的幻影。 |
| `general_time_elite` | 永恒者·伊特尼提 | 精英 | **时间循环（阈值触发）**：HP < 20% 时触发一次“回环”：瞬移到安全位置并恢复 20% 最大生命（health_max）；为限制频率，改为“每累计受击 `H=120` 次最多触发 1 次”。实现：在 `action_get_hit` 里做阈值检测 + 受击计数阈值，触发后清零计数并 `teleportRandom(...)` + `restoreHealthPercent(...)`。 | **命运改写**：目标英雄 10 年内伤害 -30%，并短暂眩晕（默认时长）。实现：debuff trait + 短控。 |

</details>

<a id="generals-fire"></a>
### 混沌炎魔将领（领域：火焰）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_fire_vanguard` | 火焰使者·伊格尼斯 | 先锋 | **炽焰印记**：普攻命中时点燃目标 5 年；点燃的额外伤害改为“目标后续每次受击触发”（每次受击额外 -3% 当前生命（health_current））。实现：命中加 `trait_era_burn_mark`，在标记的 `action_get_hit` 里扣血。 | **火焰冲锋**：冲锋到目标并造成一次基础伤害（damage）×1.5，点燃目标 10 年。实现：位移引导 + 点燃标记（受击触发扣血）。 |
| `general_fire_tank` | 熔岩巨人·玛格玛 | 坦克 | **熔岩护甲**：近战攻击者受到固定反伤（默认 50）。实现：用护甲 trait 的 `action_get_hit`（`GetHitAction`）对攻击者 `changeHealth(-dmg)`。 | **岩浆喷发**：目标区域半径 10 转熔岩地形（每次最多改 20 格），并短暂眩晕敌军（默认时长）。实现：`WorldTile.setTileType(...)` + 范围眩晕。 |
| `general_fire_dps` | 地狱火焰·因菲诺 | 输出 | **焚烧**：普攻命中附加 10 年“焚烧标记”；带标记目标后续每次受击额外损失 `4% 当前生命（health_current）`。实现：命中加 `trait_era_burn_dot`，额外伤害在其 `action_get_hit` 里结算（不按年 DOT）。 | **烈焰风暴**：半径 12 造成固定火焰伤害（默认 300）。实现：范围扣血 + 表现用 `ActionLibrary.castFire(...)`。 |
| `general_fire_support` | 火焰祭司·辛德拉 | 辅助 | **火焰祝福**：当辛德拉命中/受击时：以当前 `pTile` 半径 12 刷新友军“火焰附魔”（持续 5 年）：友军命中会给目标附加 5 年燃烧标记（燃烧额外伤害走受击触发）。实现：在钩子里半径枚举给友军加 trait。 | **火焰复苏**：持续 20 年；持续期内当友军命中/受击且站在火焰/熔岩地形上时，恢复量 ×1.5，并获得 20 年火抗。实现：给友军刷新“复苏标记 trait”，在其命中/受击钩子里做地形判定与回血倍率。 |
| `general_fire_elite` | 炎魔之子·弗雷格 | 精英 | **火焰抗性**：火焰伤害 -80%（口径：仅对 MOD 的火焰伤害/燃烧触发伤害生效）；并在受击时若站在熔岩上恢复 `5% 最大生命（health_max）`。实现：在自身 `action_get_hit` 里做地形判定 + `restoreHealthPercent(...)`。 | **末日烈焰（局部）**：持续 20 年；以目标 `pTile` 半径 14 触发一次火雨爆发（一次性范围伤害），并给敌军附加 20 年“热浪标记”：其后续每次受击额外掉血（小额）。实现：范围扣血 + 标记的 `action_get_hit` 追加伤害（不做区域列表）。 |

</details>

<a id="generals-abyss"></a>
### 深渊邪神将领（领域：疯狂）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_abyss_vanguard` | 低语者·萨尔 | 先锋 | **精神侵蚀**：命中英雄使其疯狂值 +20（0-100）。实现：用 `ActorTrait.action_attack_target` 命中时把疯狂值写入目标 `custom_data_int["era_madness"]`（0-100）。 | **低语突袭**：使目标英雄立即短暂混乱（默认时长），并降低其攻击 10 年。实现：混乱 + trait。 |
| `general_abyss_tank` | 深渊守卫·达贡 | 坦克 | **深渊护甲**：当达贡命中/受击时：以当前 `pTile` 半径 10 对疯狂值 ≥50 的敌军刷新“攻击 -15%”（持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举 + debuff trait。 | **恐惧光环**：持续 20 年；持续期内当达贡命中/受击时：以当前 `pTile` 半径 12 刷新敌军攻击 -25%。实现：施放后给自身加光环 trait（到期年），光环 trait 在 `action_attack_target/action_get_hit` 做半径枚举。 |
| `general_abyss_dps` | 疯狂使者·尼亚拉 | 输出 | **疯狂共鸣**：对疯狂值越高的目标伤害越高（满 100 时 +100%）。实现：按疯狂值加成。 | **精神爆破**：对目标区域半径 8 造成伤害，对疯狂值 ≥50 的单位基础伤害（damage）×3。实现：范围扣血 + 条件倍率。 |
| `general_abyss_support` | 邪教主教·海德拉 | 辅助 | **深渊祝福**：当海德拉命中/受击时：以当前 `pTile` 半径 12 刷新友军“疯狂值增长 -50%”标记，并使混乱/恐惧持续时间 -50%（仅对 MOD 精神控制生效）。实现：在钩子里半径枚举给友军加 trait 标记；写入到期年时按标记缩短。 | **邪教仪式**：在目标 `pTile` 周围召唤邪教徒 3 名，并命令其优先靠近英雄传播疯狂。实现：`spawnNewUnit(...)` + `goTo(...)`（受军团上限约束）。 |
| `general_abyss_elite` | 触手领主·肖格斯 | 精英 | **触手横扫**：普攻命中时对目标周围 2 个敌人造成 50% 溅射伤害（每累计命中 `H=20` 次最多触发 1 次）。实现：在 `action_attack_target` 里用命中计数阈值限频，并邻近搜索 2 个敌人 `changeHealth(-dmg)`。 | **深渊凝视（弱化版）**：对目标英雄疯狂值 +30，并短暂眩晕（默认时长）。实现：疯狂值写入 `custom_data_int["era_madness"]` + `addStunnedEffectOnTarget(...)`。 |

</details>

<a id="generals-death"></a>
### 死亡君王将领（领域：亡灵）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_death_vanguard` | 死亡骑士·莫德雷德 | 先锋 | **尸骸践踏**：普攻命中时有 15% 概率在目标附近生成 1 个骷髅兵；为限频，改为“每累计命中 `H=20` 次最多触发 1 次”，且总触发上限 6 次。实现：在 `action_attack_target` 里做计数与上限控制，触发时 `spawnNewUnit("skeleton_thrall", tile, ...)`。 | **亡灵冲锋**：冲锋到目标并造成基础伤害（damage）×1.3，同时给目标附加 3 年“冲锋标记”；目标在标记期内死亡则额外召唤 1 个骷髅。实现：冲锋引导 + 命中标记 trait → 标记 trait `action_on_object_remove` 生成骷髅（受军团上限约束）。 |
| `general_death_tank` | 骨龙·尼德霍格 | 坦克 | **骨甲**：减伤 30%，并使控制持续时间 -30%（仅对 MOD 控制生效）。实现：骨甲 trait 的 `action_get_hit`（`GetHitAction`）减伤；控制缩短按“到期年”规则处理。 | **亡灵光环**：持续 20 年；当尼德霍格命中/受击时：以当前 `pTile` 半径 12 为亡灵单位恢复 4% 最大生命（health_max）（可限频）。实现：光环 trait 在 `action_attack_target/action_get_hit` 里范围恢复。 |
| `general_death_dps` | 死灵法师·凯尔苏斯 | 输出 | **灵魂收割**：击杀单位恢复 50% 最大生命（health_max）；为限频，改为“每累计击杀 `K=3` 次最多触发 1 次”。实现：用 `Actor.kills` 增量触发（上次 kills 记到 `custom_data_int`）+ 次数上限。 | **死亡射线**：对目标英雄造成固定伤害（默认 100）且“无视护甲”。实现：直接扣血。 |
| `general_death_support` | 亡灵祭司·莉莉丝 | 辅助 | **死亡祝福**：半径 12 友军死亡后有 20% 概率转化为亡灵仆从。实现：当莉莉丝命中/受击时，以当前 `pTile` 半径 12 为友军刷新标记 `trait_era_death_bless_mark`；标记 trait 的 `action_on_object_remove` 在友军死亡时按概率 `spawnNewUnit("undead_thrall", tile, ...)`。 | **复活仪式**：在据点召唤 3 个亡灵单位（优先主力/精锐）。实现：批量生成（受军团上限约束）。 |
| `general_death_elite` | 死神使者·桑纳托斯 | 精英 | **死亡审判（弱化版）**：附近死亡事件触发骷髅生成概率 +15%。实现：全局/区域加成。 | **冥界召唤**：召唤“死神分身”2 个（临时精英单位，持续 50 年）。实现：生成 + 计时清理。 |

</details>

<a id="generals-soul"></a>
### 灵魂编织者将领（领域：契约）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_soul_vanguard` | 诱惑者·莉莉丝 | 先锋 | **魅影**：对英雄命中时有 10% 概率触发“短暂魅惑”（目标短暂停止攻击，默认时长）。实现：用 `ActorTrait.action_attack_target` 命中时做概率判定，对目标 `makeWait(...)` 或短眩晕（按“<1 年”规则）。 | **魅惑**：目标英雄短暂停止攻击（默认时长），并获得“易伤”10 年。实现：`makeWait(...)`/短控 + 易伤 trait（到期年写入 `custom_data_int`）。 |
| `general_soul_tank` | 契约守护者·巴弗灭 | 坦克 | **契约壁垒**：当巴弗灭命中/受击时：以当前 `pTile` 半径 8 刷新友军减伤 10% 与“控制持续时间 -20%”（持续 5 年，仅对 MOD 控制生效）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 buff trait。 | **灵魂守护（近似嘲讽）**：持续 20 年；指定 1 名友军获得护盾值（默认 300）与减伤 20%。守护期间当巴弗灭命中/受击时：对半径 8 敌军执行一次 `startFightingWith(巴弗灭)`，实现近似嘲讽（带数量上限）。 |
| `general_soul_dps` | 灵魂猎手·阿撒兹勒 | 输出 | **灵魂吸取**：击杀永久 +5% 攻击（上限 10 层）。实现：用 `Actor.kills` 增量触发并把层数写入 `custom_data_int` + trait 表现。 | **灵魂撕裂**：对“有契约层数”的目标基础伤害（damage）×3。实现：读取目标 `custom_data_int["era_contract_stacks"]` + 条件倍率。 |
| `general_soul_support` | 契约祭司·贝利亚尔 | 辅助 | **契约解除**：普攻命中时有概率移除目标 1 个增益状态；为限制频率，改为“每累计命中 `H=20` 次最多触发 1 次”。实现：在 `action_attack_target` 里计数到阈值后对目标移除 1 个增益 trait。 | **批量契约**：半径 12 内敌军全部获得 1 层契约。实现：范围施加契约层数。 |
| `general_soul_elite` | 傀儡大师·墨菲斯托二世 | 精英 | **契约侵蚀**：当带契约层数 ≥3 的英雄受击时：触发短暂混乱（默认时长）并附加 1 年易伤（受伤 +15%）；为限制频率，改为“每累计受击 `H=10` 次最多触发 1 次”。实现：逻辑写在英雄的 `trait_era_contract.action_get_hit`（读 `era_contract_stacks` + 受击计数阈值）。 | **契约牵引**：持续 30 年；给目标英雄施加“牵引标记”：其后续每次受击都会 `goTo(魔王据点/魔王本体附近 tile)` 并短暂混乱（默认时长）（最多触发 10 次）。实现：命中施放标记 trait，在标记的 `action_get_hit` 里牵引+混乱+次数递减。 |

</details>

<a id="generals-nature"></a>
### 自然之怒将领（领域：自然）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_nature_vanguard` | 荆棘使者·索恩 | 先锋 | **荆棘印记**：命中目标附加 10 年减速（-20%），并在其脚下生成 1 个荆棘地块。实现：trait + 小范围地形改造（严格上限）。 | **荆棘冲锋**：冲锋路径留下荆棘（仅做地形表现，持续 15 年），并对路径附近敌军触发一次“荆棘爆刺”（半径 2：损失 `3% 当前生命（health_current）`，最低扣到 1HP）。实现：路径取样 + 每个采样点做一次性半径枚举扣血。 |
| `general_nature_tank` | 古树守护者·伊格德拉西尔 | 坦克 | **树皮护甲**：减伤 40%。实现：护甲 trait 的 `action_get_hit`（`GetHitAction`）减伤。 | **根系缠绕**：半径 10 敌军减速 -40% 持续 10 年，并短暂眩晕（默认时长）。实现：范围 debuff + 短控。 |
| `general_nature_dps` | 野兽之王·芬里尔 | 输出 | **狂暴**：HP < 50% 时攻击 +30%，移速 +15%。实现：阈值 trait。 | **召唤狼群**：召唤 3 只狼加入战斗并围攻最近城市。实现：生成单位（受军团上限约束）。 |
| `general_nature_support` | 自然祭司·德鲁伊娜 | 辅助 | **自然治愈**：当德鲁伊娜命中/受击时：以当前 `pTile` 半径 12 为友军恢复 `3% 最大生命（health_max）`（单次处理上限）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并 `restoreHealthPercent(...)`。 | **植物召唤**：召唤食人花/植物怪 6 个守卫据点。实现：生成单位。 |
| `general_nature_elite` | 盖亚之子·泰坦 | 精英 | **大地之躯**：控制持续时间 -30%（仅对 MOD 控制生效）；建筑伤害 +30%。实现：trait。 | **地震**：目标区域半径 16，建筑 40% 概率受重创，单位短暂眩晕（默认时长）。实现：建筑抽样扣血 + 范围短控。 |

</details>

<a id="generals-judge"></a>
### 终焉审判者将领（领域：审判）
<details>
<summary>展开将领表（5 名，P0+S1）</summary>

| 将领ID | 名称 | 定位 | P0 被动（常驻） | S1 主动 |
|---|---|---|---|---|
| `general_judge_vanguard` | 天使长·米迦勒 | 先锋 | **圣痕**：命中英雄叠加 1 层审判（最多 3 层；每层受伤 +10%）。实现：用 `ActorTrait.action_attack_target` 命中时写入目标 `custom_data_int["era_judgement_stacks"]`（上限 3）并刷新到期年。 | **神圣冲锋**：无视地形冲锋到目标并造成基础伤害（damage）×1.5。实现：路径忽略用传送近似 + 扣血。 |
| `general_judge_tank` | 守护天使·乌列尔 | 坦克 | **神圣护盾**：减伤 25%，并使控制持续时间 -30%（仅对 MOD 施加的控制生效）。实现：护盾 trait 的 `action_get_hit`（`GetHitAction`）减伤；控制缩短按“到期年”规则处理。 | **神罚反击**：持续 20 年；受击时给攻击者叠 1 层审判（持续期内最多触发 20 次）。实现：用反击 trait 的 `action_get_hit`（`GetHitAction`）做 `procs_left` 递减与叠层写入 `custom_data_int["era_judgement_stacks"]`。 |
| `general_judge_dps` | 审判天使·加百列 | 输出 | **神圣之剑**：对“被审判目标”伤害 +80%（不做 ×3）。实现：按审判层数/标记加成。 | **审判执行**：对审判 ≥3 层的英雄造成一次重击（当前生命（health_current） -35%），并短暂眩晕（默认时长），随后清空其审判层数。实现：条件扣血 + 控制 + 清层。 |
| `general_judge_support` | 救赎天使·拉斐尔 | 辅助 | **净化**：半径 12 友军负面状态持续时间 -40%（仅对 MOD 负面状态生效）。实现：trait 标记 + 对应负面效果写入到期年（`custom_data_int`）时按该标记缩短。 | **神圣治愈**：治疗半径 12 友军 60% 最大生命（health_max），并移除 1 个负面状态。实现：`restoreHealthPercent(0.6)` + 移除。 |
| `general_judge_elite` | 末日天使·亚兹拉尔 | 精英 | **末日宣告（局部）**：当亚兹拉尔命中/受击时：以当前 `pTile` 半径 14 刷新敌军攻击/防御 -5%（持续 5 年）。实现：在 `action_attack_target/action_get_hit` 里半径枚举并刷新 debuff trait。 | **最终审判（局部）**：目标区域半径 16 敌军当前生命（health_current） -15%（英雄减半），并施加 10 年移速 -10%。实现：范围扣血 + debuff。 |

</details>

---

<a id="appendix-base-stats"></a>
## 附录G：可用属性字段速查（BaseStats）

说明：
- 以下字段来自 BaseStatsLibrary，是“确实存在且可用”的基础属性字段清单。
- **数值类**写入 `base_stats`，**百分比类**写入 `base_stats_meta`（详见附录A）。
- 标注“隐藏”的字段是引擎内部参数，**不建议直接做成玩家可调项**。

### G.1 单位/英雄可用（48）

| 属性ID | 简单说明 | 备注 |
|---|---|---|
| `lifespan` | 寿命（能活多少年） | - |
| `mutation` | 变异率（更容易出现变异/特质） | - |
| `offspring` | 繁殖数量（一次出生的子代数量） | - |
| `multiplier_offspring` | 繁殖数量倍率 | - |
| `range` | 攻击范围 | - |
| `damage` | 基础伤害 | - |
| `speed` | 移动速度 | - |
| `health` | 最大生命 | - |
| `armor` | 防御 | - |
| `stamina` | 体力上限（行动相关） | - |
| `mana` | 法力上限（施法资源） | - |
| `accuracy` | 命中率 | - |
| `targets` | 一次攻击可命中的目标数 | 隐藏 |
| `projectiles` | 一次攻击发射的弹道数量 | 隐藏 |
| `experience` | 经验获取/成长速度 | - |
| `happiness` | 心情/幸福度 | - |
| `critical_chance` | 暴击率 | - |
| `critical_damage_multiplier` | 暴击伤害倍率 | - |
| `size` | 体型/碰撞体大小 | 隐藏 |
| `area_of_effect` | 攻击/技能影响范围半径 | 隐藏 |
| `attack_speed` | 攻速 | - |
| `throwing_range` | 投掷/远程射程 | - |
| `construction_speed` | 建造速度 | - |
| `birth_rate` | 出生率 | - |
| `maturation` | 成熟速度（长大快慢） | - |
| `age_adult` | 成年年龄 | 隐藏 |
| `age_breeding` | 繁殖年龄 | 隐藏 |
| `max_nutrition` | 最大饱食度 | - |
| `metabolic_rate` | 代谢速度（饥饿消耗快慢） | 隐藏 |
| `skill_combat` | 战斗熟练度 | - |
| `skill_spell` | 法术熟练度 | - |
| `knockback` | 击退相关（造成或承受的击退强度） | - |
| `recoil` | 反冲相关（攻击后的后退/僵直） | - |
| `mass` | 质量/重量（影响碰撞与被推） | - |
| `mass_2` | 质量（第二权重，内部用） | - |
| `multiplier_mass` | 质量倍率 | 隐藏 |
| `limit_population` | 该种族/单位的数量上限 | - |
| `multiplier_health` | 生命倍率 | - |
| `multiplier_lifespan` | 寿命倍率 | - |
| `multiplier_stamina` | 体力倍率 | - |
| `multiplier_mana` | 法力倍率 | - |
| `multiplier_damage` | 伤害倍率 | - |
| `multiplier_crit` | 暴击效果倍率 | - |
| `multiplier_speed` | 移速倍率 | - |
| `multiplier_attack_speed` | 攻速倍率 | - |
| `scale` | 模型缩放（视觉体型） | - |
| `status_chance` | 状态效果触发概率 | - |
| `damage_range` | 伤害浮动范围 | 隐藏 |

### G.2 王国专用（17）

| 属性ID | 简单说明 | 备注 |
|---|---|---|
| `personality_aggression` | 王国好战倾向 | 隐藏 |
| `personality_administration` | 王国内政倾向 | 隐藏 |
| `personality_diplomatic` | 王国外交倾向 | 隐藏 |
| `personality_rationality` | 王国理性倾向 | 隐藏 |
| `diplomacy` | 王国外交能力 | - |
| `warfare` | 王国军事能力 | - |
| `stewardship` | 王国治理/管理能力 | - |
| `intelligence` | 王国智略/研究能力 | - |
| `army` | 王国军力规模/动员能力 | - |
| `cities` | 城市发展能力/城市上限相关 | - |
| `bonus_towers` | 防御塔相关加成 | - |
| `loyalty_traits` | 王国忠诚特质倾向 | - |
| `loyalty_mood` | 王国忠诚/士气情绪 | - |
| `opinion` | 王国对外好感/评价 | - |
| `multiplier_diplomacy` | 外交效果倍率 | - |
| `multiplier_supply_timer` | 补给计时倍率 | 隐藏 |
| `limit_clan_members` | 氏族/家族成员上限 | - |
