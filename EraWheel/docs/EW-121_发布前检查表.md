# EW-121 发布前检查表

## 使用方式
- 这份清单对应 `EW-121`。
- 它的原则很简单：已验证就打勾，没验证就留空，不把空白伪装成完成。
- 每次准备发版前，从上到下过一遍。

## 当前状态摘要
- `Observed`：`EW-114`、`EW-115`、`EW-116` 已有对应文档。
- `Observed`：`EW-117`、`EW-118`、`EW-119` 的脚本与记录模板现已补齐。
- `Observed`：`EW-120` 发布说明和 `EraWheel/README.md` 现已补齐。
- `Unknown`：真实 WorldBox 实机结果还没有在仓库里全部回填。

## 发布阻断项
- [x] `mod.json`、`icon.png`、`default_config.json`、`Assets/Art/`、`Locales/`、`docs/`、`src/` 都在 `EraWheel/` 根目录下。
- [x] `EraWheel/README.md` 已说明目录结构、构建闸门和兼容口径。
- [x] `EW-114` 到 `EW-120` 的文档入口已经齐全。
- [ ] 已在真实 `WorldBox 0.51.2+` + `NeoModLoader` 环境跑完 `EW-115` 到 `EW-119`。
- [ ] 已把本次实机结果回填到对应文档，而不是只留空模板。

## 风险检查
- [x] 启动校验有独立清单，缺图、重 ID、参数非法会拦截启动。
- [x] 存档有 `mod_id` 与版本号校验，不会把别的模组数据当成 EraWheel 存档硬读。
- [ ] 多魔王长局、持续出波、区域技能叠加的实机稳定性已完成长时间观察。
- [ ] 当前版本的已知问题已经写进发布说明或本清单，没有藏起来。

## 回滚准备
- [ ] 发布前已备份上一版发布包。
- [ ] 发布前已备份用于联调的世界存档。
- [ ] 如果新版本出问题，团队已经明确“先回旧包，再回看文档和日志”的回滚顺序。

## 已知未完成项
- `Observed`：正式打包工程和一键发布脚本还没在仓库里落地。
- `Observed`：仓库内没有现成的 WorldBox 实机环境，所以很多结果仍停留在“脚本已写好，等待回填”。
- `Unknown`：不同机器配置下的长时间性能差异，目前还没有统一采样记录。
