# EW-008 空模组加载冒烟记录

## 记录目的
- 这份记录只覆盖 `EW-004~008` 第一轮落地后的空模组冒烟。
- 重点是把“仓库里已经验证的东西”和“还需要进 WorldBox 实机确认的东西”分开写清楚。
- 当前仓库没有可直接启动的本地 `WorldBox` 可执行环境，所以实机结果暂时还是待确认状态。

## 当前状态
- `Observed`：`EraWheel/src/` 已补上统一日志、本地化目录骨架、开发模式配置、调试总面板和反射适配层。
- `Observed`：`BuildingManager.addBuilding(...)`、`MapBox.map_stats`、`MoveCamera.focusOn(...)` 都已在本地 API 文档或反编译结果里找到对应签名，并已接成反射包装。
- `Observed`：使用 `.codex/tmp/neomod_smoke/EraWheel.NeomodSmoke.csproj` 对 `EraWheel/src/**/*.cs` 做了编译冒烟，结果为 `Build succeeded, 0 Warning(s), 0 Error(s)`。
- `Unknown`：`新建世界后无报错；暂停、加速、切世界界面不崩` 还没有在真实游戏进程里跑过。

## 本轮已验证内容
- 验证时间：`2026-04-10`
- 验证命令：`dotnet build .codex/tmp/neomod_smoke/EraWheel.NeomodSmoke.csproj`
- 验证结果：编译通过，说明当前源码目录、命名空间、NeoModLoader 引用关系和新增文件结构是自洽的。
- 验证边界：这次只证明“代码能编出来”，还不能证明“进游戏后 UI 和反射调用一定工作”。

## 进游戏后的最短手工脚本
1. 启动带 `EraWheel` 的 NeoModLoader 环境，确认启动日志里能看到带 `[启动]`、`[配置]`、`[反射]`、`[文本]` 前缀的日志。
2. 打开 NeoModLoader 的模组配置窗口，确认 `开启开发模式` 开关可见。
3. 先保持开发模式关闭，确认 `main` 页签里没有 EraWheel 调试按钮。
4. 开启开发模式并重新加载模组，确认 `main` 页签出现 EraWheel 调试按钮，打开后能看到运行态摘要和 4 个调试动作按钮。
5. 新建一个世界，确认调试面板里的 `世界是否已加载`、`MapBox.map_stats`、`MoveCamera.focusOn` 状态变为可用。
6. 在世界内分别测试：暂停、加速、切回主界面再进入世界，确认没有新增报错。

## 结论
- 这轮可以确认 `EW-004`、`EW-005`、`EW-006`、`EW-007` 的代码骨架已经落地，且当前源码能编译通过。
- `EW-008` 的“实机无报错”部分仍是阻塞项，因为仓库内缺少真实游戏运行环境。
- 下一步最值钱的动作不是继续补代码，而是按上面的 6 步进游戏做一次真实加载验证，再把结果回填到这份记录里。
