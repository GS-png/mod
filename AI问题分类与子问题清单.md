# AI问题分类与子问题清单

这份文档用于研究 AI 在代码理解、设计、实现、验证和交付中常见的问题。
文档按问题根因重新分类，并合并重复表达。
每个子问题都尽量写成可观察、可讨论、可治理的具体问题。

## 1. 事实理解与证据问题

这类问题的核心是：AI 没有先确认事实，就开始回答、设计或修改。
它会导致后续所有判断都建立在不稳定基础上。
最典型的表现是把猜测说成事实。

- 上下文理解不足，没搞清项目、文件、入口和真实需求。
- 幻觉，把不存在的路径、函数、字段、版本、测试结果说成真的。
- 无证据假设，用猜测代替源码、配置、文档或命令结果。
- 把未知当已知，不区分已观察事实、推断、假设和未知项。
- 没有先调查就设计，导致实现方向一开始就是错的。

## 2. 需求、目标与边界问题

这类问题的核心是：AI 不清楚到底要解决什么，却已经开始动手。
需求边界不清时，小问题很容易被扩大成大改造。
目标不清也会让验证失去标准。

- 目标行为不明确，就开始改代码。
- 真实入口不清，不知道功能从哪里触发。
- 调用链和数据流不清，不知道影响哪些地方。
- 输入、输出、副作用、失败方式没有定义。
- 任务范围失控，把小问题扩成大改造。
- 非目标没写清，顺手改了不该改的东西。

## 3. 架构设计与复杂度问题

这类问题的核心是：AI 用看起来专业的结构制造额外复杂度。
复杂度本身就是维护成本。
如果新增结构不能解决当前真实问题，它就可能变成技术债。

- 架构设计不贴合现有项目。
- 代码结构混乱，职责混在一起。
- 过度抽象，为假想未来提前加层。
- 过度包装，新增 helper、adapter、manager，但没有真实收益。
- 代码膨胀，简单逻辑被写成大段复杂实现。
- 技术债扩散，复杂度越来越高。
- 公共接口、共享类型、配置、协议改动没有影响面说明。

## 4. 实现质量与代码一致性问题

这类问题的核心是：AI 写出的代码不像这个项目里的代码。
代码也许暂时能跑，但难读、难改、难排查。
长期看，它会拉低整个项目的一致性。

- 低质量代码，能跑但难读、难改、难查错。
- 半成品实现，留下占位、空实现或没补完的关键分支。
- 无意义代码，只是为了看起来更完整。
- 代码风格漂移，命名、目录、错误处理、测试写法和项目不一致。
- 复制粘贴扩散，同类逻辑到处复制。
- 生成代码漂移，手改生成产物，不改真正源头。
- 无关重构，把任务外的代码也顺手动了。

## 5. 正确性、边界与异常处理问题

这类问题的核心是：AI 只让正常路径看起来能跑。
真实系统还会遇到空值、失败、重入、回退和部分成功。
这些路径不处理，问题通常会在运行时暴露。

- 逻辑错误，程序做了不该做的事。
- Bug 修复没有复现证据，靠猜修。
- 只测正常路径，不测边界、空值、失败和回退。
- 异常处理粗暴，吞异常、扩大捕获范围或伪造成功。
- 错误缺少上下文，后续无法定位。
- 失败后状态不一致。
- 部分失败、重入、回滚没有处理。
- 不确定状态被当成成功结果继续传播。

## 6. 重复、旧逻辑与演进失控问题

这类问题的核心是：AI 新增一个实现，却没有收敛旧实现。
结果项目里出现两套逻辑、多个权威来源或长期并存的临时分支。
这会让后续维护者不知道该相信哪一处。

- 冗余逻辑，同一规则多处实现。
- 重复代码，同一段逻辑复制多份。
- 新实现落地后，不删除旧实现。
- 死代码、死参数、废弃开关长期保留。
- 补丁式修复，一层补丁盖一层补丁。
- 越修越乱，没有回到根因分析。
- 影子状态、双写、重复缓存、多处权威来源。
- 新旧逻辑短期并存但没有收口条件。

## 7. 数据、Schema 与迁移问题

这类问题的核心是：AI 改了数据形状，却没有处理旧数据和失败恢复。
这类问题通常风险更高，因为它可能影响存档、数据库、配置或消息格式。
数据问题一旦扩散，回滚成本会很高。

- 数据结构改了，但没有迁移方案。
- schema 或字段含义不清就改。
- 持久化格式变化没有兼容策略。
- 数据边界不清，不知道谁负责读写。
- 缓存、文件、数据库、消息状态不一致。
- 失败后没有数据恢复方式。
- 删除字段、配置或旧数据时没有回滚办法。

## 8. 安全、隐私与权限问题

这类问题的核心是：AI 把能解析、能运行、能访问当成可信。
外部输入、文件、网络、权限和日志都可能成为风险入口。
安全问题不能靠经验判断，必须有明确边界和验证。

- 安全边界处理不足，把安全风险、安全漏洞和安全隐患混在一起轻视。
- 外部输入没有校验格式、范围和业务含义。
- 权限校验缺失或权限边界不清。
- 敏感信息泄露到代码、日志、测试、文档或工件。
- 路径拼接导致路径遍历风险。
- SQL、命令、查询字符串用拼接方式构造。
- 反序列化、文件 IO、网络 IO 默认信任外部内容。
- 日志没有脱敏。

## 9. 性能、资源与并发问题

这类问题的核心是：AI 没有证据就说性能或并发没问题。
热路径、高频路径和共享状态都需要更谨慎。
资源释放、取消和重试策略也不能靠默认好运。

- 性能瓶颈没有分析。
- 高频循环、大对象、大数据量路径没有复杂度说明。
- 重复 IO、重复查询、重复计算。
- 资源泄漏，文件、连接、订阅、句柄没有释放。
- 并发与线程安全问题，没分析共享状态和竞态。
- 锁、取消、超时、重试、背压没有策略。
- 重试风暴，失败后越重试越糟。
- 幂等缺失，同一操作重复执行会造成错误。

## 10. 依赖、环境与发布问题

这类问题的核心是：AI 改了运行基础，却没有证明它能在目标环境工作。
依赖、环境和发布链路属于高扩散风险。
它们需要版本依据、影响面说明和回滚方案。

- 依赖管理混乱，新增依赖没有必要性说明。
- 依赖兼容性靠猜，没有锁文件、清单或官方文档证据。
- 静默升级核心依赖。
- 环境漂移，本地能跑但目标环境不一定能跑。
- 构建链、运行环境、发布链路改动没有回滚方式。
- 发布债扩散，临时方案进入发布流程。
- 升级失败后没有降级或回退办法。

## 11. 测试、验证与质量门禁问题

这类问题的核心是：AI 写完就宣称完成，但没有真实验证。
测试不是为了好看，而是为了在代码坏掉时能失败。
没有验证结果，就不能把“能用”当成事实。

- 测试质量差，坏代码也能通过。
- 测试覆盖率虚高，只追数字。
- 只测正常路径，不测失败和边界。
- 缺少回归测试，同类 Bug 可能反复出现。
- 快照测试过多，但不验证真实行为。
- 没有运行最相关的构建、测试或静态检查。
- CI、lint、类型约束、运行时断言、发布检查缺失。
- 没有真实验证结果，却宣称已完成或测试通过。

## 12. 文档、注释、日志与可观测性问题

这类问题的核心是：AI 补了很多文字，但不能帮助理解和排障。
好文档解释行为和约束。
好日志能让人定位问题，而且不能泄露敏感信息。

- 缺少必要文档，行为、接口、配置变化没有说明。
- 文档过时，和真实实现不一致。
- 注释无意义，只是在复述代码。
- 复杂逻辑没有解释为什么这么做。
- 日志缺上下文，出了问题查不到原因。
- 日志失真，失败被记录成成功。
- 可观测性不足，缺少错误分类、上下文标识、指标或告警。
- 排障说明缺失，别人不知道怎么复现和定位。

## 总结

这 12 类问题可以作为研究 AI 编程能力缺陷的基础框架。
它们不是按表面现象分类，而是按问题根因分类。
后续研究解决方案时，可以为每一类分别设计约束、检查清单、验证命令和硬停止条件。
